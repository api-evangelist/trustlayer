openapi: 3.0.3
info:
  title: TrustLayer Platform API V2 (Early Access)
  description: "The following documentation sets out to provide everything you
    need to build your solutions using the TrustLayer API. This outlines all you
    should need from getting started to details on the calls and responses. As
    the API evolves, so will this space. Not every release will include updates
    to the API so keep an eye out for changes made and be sure to check out our
    whats new guides for the details of all the of changes each month.


    The TrustLayer API allows for you to connect to the TrustLayer platform
    through external applications, from here you'll be able to work with data
    stored and transact your account. The TrustLayer API is built on REST
    principles and returns a JSON payload. Each endpoint's documentation will
    show the expected parameters and details of the payload for each call you
    make.


    ## What is TrustLayer

    TrustLayer is a collaborative risk management application that helps reduce
    friction between businesses. The company allows users to automate the
    verification of insurance, licenses, and compliance documents of their
    business partners (i.e., vendors, subcontractors, suppliers, borrowers,
    tenants, ridesharing and franchisees). TrustLayer uses robotic process
    automation (RPA) and AI to automate this manual process. This allows
    companies to automatically verify the insurance and licenses of their
    partners. TrustLayer's pioneering solution is recognized by the industry and
    is a member of the BrokerTech Ventures (BTV) accelerator, BrushCreek Tech
    Insurance accelerator, winner of the 2019 R3 CordaCon Global Insurtech
    Challenge, and received top honors at The Institutes 2020 annual convention.


    ## Learn about TrustLayer

    The TrustLayer development API has exposed a number of entities which allows
    for you to extract and manipulate resources. The page seeks to explain them
    at a very high level and match them to the business workflows where
    possible.


    #### Compliance Profile

    The compliance profile entity houses the compliance requirements which a
    party is expected to meet. The profile is made up of a number of modules
    which details the requirements in a systematic way; such as General
    Liability, Licenses, etc. Creating the compliance profile is the basis of
    many actions on TrustLayer as each party type is given a default compliance
    profile.


    #### Documents

    The documents entity stores metadata, flags, and some extracted information
    after it has been uploaded and processed. When uploaded a document can exist
    at the Party and project level and is used to meet the compliance criteria
    defined in either the project or the base level compliance profile. The
    content of the document entity is used to evidence compliance to the
    requirments which is then used to measure the compliance score. When a
    document is uploaded an automated processing of its contents will be queued.


    #### Document Types

    When uploaded, a document is flagged by its document type, for example:
    Certificate of Insurance, W-9 etc. The document types are stored in the
    document type entity which each document is then associated to.


    #### Contacts

    The contacts of a party are stored in the contacts entity. The contacts are
    associated with the party and are called during actions such as sending
    document requests.


    #### Tags

    Tags are applied to parties and are stored in the tags entity. When applied
    to the PARTY they are not linked to a party in a project setting so these
    will not change based on any project.


    ## List Endpoints

    List endpoints accept filter criteria as querystring parameters. Each
    endpoint documents the fields it supports as filters — unsupported fields
    are rejected with a `400`.


    ### Operators

    Append an operator suffix to a field name to control the comparison.


    | Suffix | Meaning | Example |

    | ------ | ------- | ------- |

    | `=`  | equals (default) | `createdAt=2024-01-15` |

    | `!=` | not equal | `status!=archived` |

    | `>`  | greater than | `createdAt>2024-01-01` |

    | `<`  | less than | `createdAt<2024-12-31` |

    | `>=` | greater than or equal | `createdAt>=2024-01-01` |

    | `<=` | less than or equal | `createdAt<=2024-12-31` |


    ### Multiple values

    Pass a comma-separated list to match any value (`$in`):
    `_id=507f1f77bcf86cd799439011,507f1f77bcf86cd799439012`.

    Repeat the key with `!=` for not-in (`$nin`):
    `status!=archived&status!=deleted`.


    ### Value formats

    The format required for each filter field is noted in its parameter
    description on each endpoint. Three types are common:


    - **String** — URL-encoded string, compared as-is (e.g. `name=Acme`).

    - **Date** — ISO-8601 date or datetime (e.g. `createdAt=2024-01-15` or
    `createdAt>=2024-01-15T00:00:00Z`).

    - **ObjectId** — 24-character hex string (e.g.
    `_id=507f1f77bcf86cd799439011`).


    ### Combining filters

    Multiple filter parameters on the same request are combined with logical
    **AND**: `?status=active&complianceStatus=compliant` returns records that
    match both. To match any of several values on a single field, use the
    comma-list `$in` syntax shown above (`status=active,pending`). Cross-field
    OR is not supported.


    ### Nested fields

    Filters on nested objects use dot notation
    (`reference.id=507f1f77bcf86cd799439011`). Each endpoint's parameter list
    documents which nested paths are accepted.


    ### Pagination, sort, and projection

    Alongside filters, list endpoints accept:

    - `limit` — page size (default `20`, max `100`).

    - `skip` — number of results to skip (default `0`).

    - `sort` — comma-separated field list; prefix with `-` for descending (e.g.
    `sort=-createdAt,name`). Some endpoints disable sorting; each endpoint's
    description states whether `sort` is accepted and lists the allowed fields.

    - `fields` — comma-separated field list to return (e.g. `fields=_id,name`).
    Some endpoints disable projection and always return the full record.


    List endpoints will return `meta` alongside their data, which will indicate
    the total `count` of records for this list; clients may use this plus
    `limit` and `skip` to paginate the results. For ease of use, `meta` will
    also include `next` and `prev` relative paths, when applicable, that will
    return the next and previous page of results for this query.


    \ **NOTE**: In order to use the TrustLayer API, you need to have a
    TrustLayer account. If you don't currently have an account you should talk
    to our sales team at _sales@trustlayer.io_

    \ "
  termsOfService: https://trustlayer.io/terms-of-use/
  version: 2.0.0-beta
  contact:
    name: TrustLayer API Services
    email: api@trustlayer.io
  license:
    name: Apache 2.0
    url: https://apache.org/licenses/LICENSE-2.0
components:
  securitySchemes:
    Bearer Auth:
      type: http
      scheme: bearer
      description: "Enter your API token:"
  schemas:
    objectIdInput:
      title: ObjectId
      type: string
      format: ObjectId
      examples:
        - 507f1f77bcf86cd799439011
    objectId:
      title: ObjectId
      type: string
      format: ObjectId
      examples:
        - 507f1f77bcf86cd799439011
paths:
  /auth/sessions:
    post:
      tags:
        - Auth
      description: Create a 24-hour authorization session token for the
        workspace-management endpoints. The returned token grants `workspaces`
        write access for the authenticated user and is required as a Bearer
        credential on the `/v2/workspaces` routes. Returns 404 if no user
        matches the email and 401 if the password is invalid or the user is not
        a workspaces admin.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  description: Email address of the user creating the session.
                  type: string
                password:
                  type: string
                  description: Password of the user creating the session.
              required:
                - email
                - password
              description: Credentials for creating an authorization session. The user must be
                flagged as a workspaces admin.
        description: Credentials for creating an authorization session. The user must be
          flagged as a workspaces admin.
      responses:
        "201":
          description: Newly issued 24-hour authorization session token granting
            workspaces write access.
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    description: "Bearer authorization token. Send as `Authorization: Bearer
                      <token>` on subsequent calls. Expires in 24 hours."
                required:
                  - token
                additionalProperties: false
                description: Newly issued 24-hour authorization session token granting
                  workspaces write access.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /compliance-profiles:
    post:
      tags:
        - Compliance Profiles
      description: Create a new Global compliance profile in the caller's
        organization. The owning organization is derived from the access token.
        Returns the created profile.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable name of the compliance profile.
              required:
                - name
              additionalProperties: false
              description: Request body for creating a global compliance profile.
        description: Request body for creating a global compliance profile.
      responses:
        "201":
          description: A compliance profile with its requirements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the compliance profile.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the compliance profile.
                  context:
                    type: string
                    enum:
                      - global
                      - customField
                      - party
                      - project
                      - requestRecord
                    description: Context scope of the compliance profile.
                  requirements:
                    type: array
                    items:
                      type: object
                      properties:
                        moduleCode:
                          type: string
                          minLength: 1
                          description: Identifier of the module this requirement belongs to.
                        moduleLabel:
                          type: string
                          minLength: 1
                          description: Display label of the module.
                        subjectCode:
                          type: string
                          minLength: 1
                          description: Identifier of the subject this requirement belongs to.
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Display label of the subject.
                        attributeCode:
                          type: string
                          minLength: 1
                          description: Attribute code (maps to attributeId in storage).
                        attributeLabel:
                          type: string
                          minLength: 1
                          description: Display label of the attribute.
                        attributeType:
                          type: string
                          enum:
                            - boolean
                            - number
                            - fillableForm
                            - amBestRating
                            - amBestFinancialStrength
                            - connected
                            - list
                            - ai
                          description: Data type of the attribute (e.g. boolean, number).
                        attributeDescription:
                          description: Optional description of the attribute.
                          type: string
                          minLength: 1
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: Matching criteria for the requirement.
                        operator:
                          description: Comparison operator for the requirement.
                          type: string
                          enum:
                            - must be present
                            - must be greater or equal to
                            - must be less than or equal to
                            - must be completed by
                            - at least one must be present from list
                        targetValue:
                          description: Target value for the requirement comparison.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: array
                              items:
                                type: string
                                minLength: 1
                      required:
                        - moduleCode
                        - moduleLabel
                        - subjectCode
                        - subjectLabel
                        - attributeCode
                        - attributeLabel
                        - attributeType
                        - matchingCriteria
                      additionalProperties: false
                      description: A single requirement within a compliance profile.
                    description: Requirements that make up the compliance profile.
                required:
                  - _id
                  - name
                  - context
                  - requirements
                additionalProperties: false
                description: A compliance profile with its requirements.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /compliance-profiles/{id}:
    patch:
      tags:
        - Compliance Profiles
      description: Rename a Global compliance profile in the caller's organization.
        Only profiles with context `Global` may be renamed via this endpoint.
        Returns the updated profile.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: New human-readable name for the compliance profile.
              required:
                - name
              additionalProperties: false
              description: Request body for renaming a global compliance profile.
        description: Request body for renaming a global compliance profile.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile to update.
      responses:
        "200":
          description: A compliance profile with its requirements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the compliance profile.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the compliance profile.
                  context:
                    type: string
                    enum:
                      - global
                      - customField
                      - party
                      - project
                      - requestRecord
                    description: Context scope of the compliance profile.
                  requirements:
                    type: array
                    items:
                      type: object
                      properties:
                        moduleCode:
                          type: string
                          minLength: 1
                          description: Identifier of the module this requirement belongs to.
                        moduleLabel:
                          type: string
                          minLength: 1
                          description: Display label of the module.
                        subjectCode:
                          type: string
                          minLength: 1
                          description: Identifier of the subject this requirement belongs to.
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Display label of the subject.
                        attributeCode:
                          type: string
                          minLength: 1
                          description: Attribute code (maps to attributeId in storage).
                        attributeLabel:
                          type: string
                          minLength: 1
                          description: Display label of the attribute.
                        attributeType:
                          type: string
                          enum:
                            - boolean
                            - number
                            - fillableForm
                            - amBestRating
                            - amBestFinancialStrength
                            - connected
                            - list
                            - ai
                          description: Data type of the attribute (e.g. boolean, number).
                        attributeDescription:
                          description: Optional description of the attribute.
                          type: string
                          minLength: 1
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: Matching criteria for the requirement.
                        operator:
                          description: Comparison operator for the requirement.
                          type: string
                          enum:
                            - must be present
                            - must be greater or equal to
                            - must be less than or equal to
                            - must be completed by
                            - at least one must be present from list
                        targetValue:
                          description: Target value for the requirement comparison.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: array
                              items:
                                type: string
                                minLength: 1
                      required:
                        - moduleCode
                        - moduleLabel
                        - subjectCode
                        - subjectLabel
                        - attributeCode
                        - attributeLabel
                        - attributeType
                        - matchingCriteria
                      additionalProperties: false
                      description: A single requirement within a compliance profile.
                    description: Requirements that make up the compliance profile.
                required:
                  - _id
                  - name
                  - context
                  - requirements
                additionalProperties: false
                description: A compliance profile with its requirements.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /compliance-profiles/{id}/requirements:
    post:
      tags:
        - Compliance Profiles
      description: Add requirements to a compliance profile. Each item is either a
        library reference (`libraryRequirementId` or `attributeCode`) or a
        custom definition (module/subject by code or label, attribute by label).
        Idempotent on the (moduleLabel, subjectLabel, attributeLabel) tuple. The
        response contains only the requirements newly added by this call;
        duplicates of existing rules or earlier items in the batch are omitted,
        so the array is empty when every input was a duplicate.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                requirements:
                  minItems: 1
                  maxItems: 200
                  type: array
                  items:
                    anyOf:
                      - type: object
                        properties:
                          libraryRequirementId:
                            description: Identifier of the requirement entry in the workspace's requirements
                              library to clone onto this request record.
                            allOf:
                              - $ref: "#/components/schemas/objectIdInput"
                        required:
                          - libraryRequirementId
                        additionalProperties: false
                        description: Reference an existing library requirement by its identifier. The
                          persisted requirement is a verbatim copy
                          (matchingCriteria, operator, targetValue,
                          attributeType, etc.) of the library entry.
                      - type: object
                        properties:
                          attributeCode:
                            type: string
                            minLength: 1
                            description: Stable code of the library requirement to clone (alternative to
                              `libraryRequirementId`). Must match an entry in
                              the workspace requirements library.
                          matchingCriteria:
                            type: string
                            enum:
                              - matchOnce
                              - matchRepeatedly
                            description: How the requirement matches against documents (e.g. `MatchOnce`,
                              `MatchRepeatedly`). Overrides the library entry
                              value.
                          operator:
                            type: string
                            enum:
                              - must be present
                              - must be greater or equal to
                              - must be less than or equal to
                              - must be completed by
                              - at least one must be present from list
                            description: Comparison operator applied between the document data and
                              `targetValue`. Must be valid for the library
                              entry's `attributeType`.
                          targetValue:
                            description: Value(s) the document data is compared against. String for scalar
                              attribute types; string array for `list` attribute
                              types. Optional for operators that do not require
                              a target value (e.g. `MustBePresent`).
                            anyOf:
                              - type: string
                                minLength: 1
                              - type: array
                                items:
                                  type: string
                                  minLength: 1
                        required:
                          - attributeCode
                          - matchingCriteria
                          - operator
                        additionalProperties: false
                        description: Reference an existing library requirement by its `attributeCode`.
                          The persisted requirement uses the library entry for
                          module/subject/attribute identity but takes
                          `matchingCriteria`, `operator`, and `targetValue` from
                          the input — supports requirement-template entries that
                          may not carry these fields.
                      - oneOf:
                          - allOf:
                              - type: object
                                properties:
                                  subjectCode:
                                    type: string
                                    minLength: 1
                                    description: Existing subject code on the compliance profile. When provided, the
                                      subject (and its parent module) are
                                      reused. Mutually exclusive with
                                      `subjectLabel`.
                                  moduleCode:
                                    description: Existing module code on the compliance profile. When provided, the
                                      requirement is anchored to that module.
                                      Mutually exclusive with `moduleLabel`.
                                    type: string
                                    minLength: 1
                                  subjectLabel:
                                    not: {}
                                  moduleLabel:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectCode
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                          - allOf:
                              - type: object
                                properties:
                                  subjectLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new subject. The subject code is derived
                                      from this label. Requires `moduleCode` or
                                      `moduleLabel` so the new subject can be
                                      attached to a module. Mutually exclusive
                                      with `subjectCode`.
                                  moduleCode:
                                    type: string
                                    minLength: 1
                                    description: Existing module code on the compliance profile. When provided, the
                                      requirement is anchored to that module.
                                      Mutually exclusive with `moduleLabel`.
                                  subjectCode:
                                    not: {}
                                  moduleLabel:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectLabel
                                  - moduleCode
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                          - allOf:
                              - type: object
                                properties:
                                  subjectLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new subject. The subject code is derived
                                      from this label. Requires `moduleCode` or
                                      `moduleLabel` so the new subject can be
                                      attached to a module. Mutually exclusive
                                      with `subjectCode`.
                                  moduleLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new compliance module. The module code
                                      is derived from this label. Mutually
                                      exclusive with `moduleCode`.
                                  subjectCode:
                                    not: {}
                                  moduleCode:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectLabel
                                  - moduleLabel
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                        description: Define a brand-new requirement inline. The module/subject can
                          target an existing slot on the compliance profile
                          (`moduleCode` / `subjectCode`) or create a new one
                          (`moduleLabel` / `subjectLabel`). The attribute side
                          (`attributeType`, `attributeLabel`, `operator`,
                          `targetValue`, …) follows the shared
                          RequirementAttribute discriminated union — invalid
                          `(attributeType, operator, targetValue)` combinations
                          are rejected.
                    description: "A single requirement to add to the request record. One of: library
                      reference by id, library reference by attributeCode (with
                      overrides), or a custom definition."
                  description: Requirements to add. Items are processed in order; library lookups
                    happen at the workspace scope. Duplicates against existing
                    rules or earlier items in the same batch are silently
                    dropped (idempotent on the (moduleLabel, subjectLabel,
                    attributeLabel) tuple).
              required:
                - requirements
              additionalProperties: false
              description: Request body for adding requirements to a compliance profile in a
                single batch.
        description: Request body for adding requirements to a compliance profile in a
          single batch.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile to add requirements to.
      responses:
        "200":
          description: An array of compliance profile requirements.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    moduleCode:
                      type: string
                      minLength: 1
                      description: Identifier of the module this requirement belongs to.
                    moduleLabel:
                      type: string
                      minLength: 1
                      description: Display label of the module.
                    subjectCode:
                      type: string
                      minLength: 1
                      description: Identifier of the subject this requirement belongs to.
                    subjectLabel:
                      type: string
                      minLength: 1
                      description: Display label of the subject.
                    attributeCode:
                      type: string
                      minLength: 1
                      description: Attribute code (maps to attributeId in storage).
                    attributeLabel:
                      type: string
                      minLength: 1
                      description: Display label of the attribute.
                    attributeType:
                      type: string
                      enum:
                        - boolean
                        - number
                        - fillableForm
                        - amBestRating
                        - amBestFinancialStrength
                        - connected
                        - list
                        - ai
                      description: Data type of the attribute (e.g. boolean, number).
                    attributeDescription:
                      description: Optional description of the attribute.
                      type: string
                      minLength: 1
                    matchingCriteria:
                      type: string
                      enum:
                        - matchOnce
                        - matchRepeatedly
                      description: Matching criteria for the requirement.
                    operator:
                      description: Comparison operator for the requirement.
                      type: string
                      enum:
                        - must be present
                        - must be greater or equal to
                        - must be less than or equal to
                        - must be completed by
                        - at least one must be present from list
                    targetValue:
                      description: Target value for the requirement comparison.
                      anyOf:
                        - type: string
                          minLength: 1
                        - type: array
                          items:
                            type: string
                            minLength: 1
                  required:
                    - moduleCode
                    - moduleLabel
                    - subjectCode
                    - subjectLabel
                    - attributeCode
                    - attributeLabel
                    - attributeType
                    - matchingCriteria
                  additionalProperties: false
                  description: A single requirement within a compliance profile.
                description: An array of compliance profile requirements.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Compliance Profiles
      description: List the requirements of a compliance profile, paginated.
        Requirements are returned regardless of the profile context. Each rule
        is serialized as a requirement response alongside pagination metadata. A
        missing profile or cross-org access returns 404.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile whose requirements to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        moduleCode:
                          type: string
                          minLength: 1
                          description: Identifier of the module this requirement belongs to.
                        moduleLabel:
                          type: string
                          minLength: 1
                          description: Display label of the module.
                        subjectCode:
                          type: string
                          minLength: 1
                          description: Identifier of the subject this requirement belongs to.
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Display label of the subject.
                        attributeCode:
                          type: string
                          minLength: 1
                          description: Attribute code (maps to attributeId in storage).
                        attributeLabel:
                          type: string
                          minLength: 1
                          description: Display label of the attribute.
                        attributeType:
                          type: string
                          enum:
                            - boolean
                            - number
                            - fillableForm
                            - amBestRating
                            - amBestFinancialStrength
                            - connected
                            - list
                            - ai
                          description: Data type of the attribute (e.g. boolean, number).
                        attributeDescription:
                          description: Optional description of the attribute.
                          type: string
                          minLength: 1
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: Matching criteria for the requirement.
                        operator:
                          description: Comparison operator for the requirement.
                          type: string
                          enum:
                            - must be present
                            - must be greater or equal to
                            - must be less than or equal to
                            - must be completed by
                            - at least one must be present from list
                        targetValue:
                          description: Target value for the requirement comparison.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: array
                              items:
                                type: string
                                minLength: 1
                      required:
                        - moduleCode
                        - moduleLabel
                        - subjectCode
                        - subjectLabel
                        - attributeCode
                        - attributeLabel
                        - attributeType
                        - matchingCriteria
                      additionalProperties: false
                      description: A single requirement within a compliance profile.
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /compliance-profiles/{id}/requirements/{attributeCode}:
    get:
      tags:
        - Compliance Profiles
      description: Read a single requirement from a compliance profile by its
        attributeCode, regardless of the profile context. A missing profile, a
        missing requirement, or cross-org access returns 404.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile.
        - schema:
            type: string
            minLength: 1
          in: path
          name: attributeCode
          required: true
          description: Attribute code identifying the requirement to operate on.
      responses:
        "200":
          description: A single requirement within a compliance profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  moduleCode:
                    type: string
                    minLength: 1
                    description: Identifier of the module this requirement belongs to.
                  moduleLabel:
                    type: string
                    minLength: 1
                    description: Display label of the module.
                  subjectCode:
                    type: string
                    minLength: 1
                    description: Identifier of the subject this requirement belongs to.
                  subjectLabel:
                    type: string
                    minLength: 1
                    description: Display label of the subject.
                  attributeCode:
                    type: string
                    minLength: 1
                    description: Attribute code (maps to attributeId in storage).
                  attributeLabel:
                    type: string
                    minLength: 1
                    description: Display label of the attribute.
                  attributeType:
                    type: string
                    enum:
                      - boolean
                      - number
                      - fillableForm
                      - amBestRating
                      - amBestFinancialStrength
                      - connected
                      - list
                      - ai
                    description: Data type of the attribute (e.g. boolean, number).
                  attributeDescription:
                    description: Optional description of the attribute.
                    type: string
                    minLength: 1
                  matchingCriteria:
                    type: string
                    enum:
                      - matchOnce
                      - matchRepeatedly
                    description: Matching criteria for the requirement.
                  operator:
                    description: Comparison operator for the requirement.
                    type: string
                    enum:
                      - must be present
                      - must be greater or equal to
                      - must be less than or equal to
                      - must be completed by
                      - at least one must be present from list
                  targetValue:
                    description: Target value for the requirement comparison.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: array
                        items:
                          type: string
                          minLength: 1
                required:
                  - moduleCode
                  - moduleLabel
                  - subjectCode
                  - subjectLabel
                  - attributeCode
                  - attributeLabel
                  - attributeType
                  - matchingCriteria
                additionalProperties: false
                description: A single requirement within a compliance profile.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Compliance Profiles
      description: "Partially update a requirement on a compliance profile. Only the
        supplied fields are changed; omitted fields retain their current values.
        Updates are accepted on profiles of any context, with context-based
        limitations: library-sourced requirements and requirements inherited
        from a custom field cannot be modified. Returns the updated
        requirement."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                operator:
                  description: Comparison operator for the rule.
                  type: string
                  enum:
                    - must be present
                    - must be greater or equal to
                    - must be less than or equal to
                    - must be completed by
                    - at least one must be present from list
                targetValue:
                  description: Target value for the rule comparison.
                  anyOf:
                    - type: string
                      minLength: 1
                    - type: array
                      items:
                        type: string
                        minLength: 1
                matchingCriteria:
                  description: Matching criteria for the rule.
                  type: string
                  enum:
                    - matchOnce
                    - matchRepeatedly
                attributeDescription:
                  description: Optional description of the attribute.
                  type: string
                  minLength: 1
              additionalProperties: false
              description: Request body for partially updating a requirement on a compliance
                profile. Only the supplied fields are changed; omitted fields
                retain their current values.
        description: Request body for partially updating a requirement on a compliance
          profile. Only the supplied fields are changed; omitted fields retain
          their current values.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile.
        - schema:
            type: string
            minLength: 1
          in: path
          name: attributeCode
          required: true
          description: Attribute code identifying the requirement to operate on.
      responses:
        "200":
          description: A single requirement within a compliance profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  moduleCode:
                    type: string
                    minLength: 1
                    description: Identifier of the module this requirement belongs to.
                  moduleLabel:
                    type: string
                    minLength: 1
                    description: Display label of the module.
                  subjectCode:
                    type: string
                    minLength: 1
                    description: Identifier of the subject this requirement belongs to.
                  subjectLabel:
                    type: string
                    minLength: 1
                    description: Display label of the subject.
                  attributeCode:
                    type: string
                    minLength: 1
                    description: Attribute code (maps to attributeId in storage).
                  attributeLabel:
                    type: string
                    minLength: 1
                    description: Display label of the attribute.
                  attributeType:
                    type: string
                    enum:
                      - boolean
                      - number
                      - fillableForm
                      - amBestRating
                      - amBestFinancialStrength
                      - connected
                      - list
                      - ai
                    description: Data type of the attribute (e.g. boolean, number).
                  attributeDescription:
                    description: Optional description of the attribute.
                    type: string
                    minLength: 1
                  matchingCriteria:
                    type: string
                    enum:
                      - matchOnce
                      - matchRepeatedly
                    description: Matching criteria for the requirement.
                  operator:
                    description: Comparison operator for the requirement.
                    type: string
                    enum:
                      - must be present
                      - must be greater or equal to
                      - must be less than or equal to
                      - must be completed by
                      - at least one must be present from list
                  targetValue:
                    description: Target value for the requirement comparison.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: array
                        items:
                          type: string
                          minLength: 1
                required:
                  - moduleCode
                  - moduleLabel
                  - subjectCode
                  - subjectLabel
                  - attributeCode
                  - attributeLabel
                  - attributeType
                  - matchingCriteria
                additionalProperties: false
                description: A single requirement within a compliance profile.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Compliance Profiles
      description: Remove a requirement from a Global compliance profile. Returns 204
        with no body on success.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the compliance profile.
        - schema:
            type: string
            minLength: 1
          in: path
          name: attributeCode
          required: true
          description: Attribute code identifying the requirement to operate on.
      responses:
        "204":
          description: Default Response
          content:
            application/json:
              schema:
                type: string
                nullable: true
                enum:
                  - null
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /contacts:
    get:
      tags:
        - Contacts
      description: >-
        List contacts in the caller's organization. Results are always scoped to
        the caller's organization. Supports filtering, sorting, and pagination
        via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `email` (string), `contactPersonName` (string), `companyName` (string).
        See "List Endpoints" in the API overview for operator syntax and
        combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: email, contactPersonName, companyName,
            createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id, email,
            contactPersonName, companyName, title, phone, fax, address,
            createdAt, updatedAt"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: email
          required: false
          description: Filter by email. String value (e.g. `email=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: contactPersonName
          required: false
          description: Filter by contactPersonName. String value (e.g.
            `contactPersonName=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: companyName
          required: false
          description: Filter by companyName. String value (e.g. `companyName=example`).
            See "List Endpoints" in the API overview for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the contact.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        contactPersonName:
                          description: Full name of the contact person.
                          type: string
                        companyName:
                          description: Company or organization name for this contact.
                          type: string
                        title:
                          description: Job title of the contact person.
                          type: string
                        email:
                          description: Email address of the contact.
                        phone:
                          description: Phone number of the contact.
                          type: string
                        fax:
                          description: Fax number of the contact.
                          type: string
                        address:
                          description: Postal address of the contact. All sub-fields are optional.
                          type: object
                          properties:
                            type:
                              type: string
                            rawAddress:
                              type: string
                            line1:
                              type: string
                            line2:
                              type: string
                            postalCode:
                              type: string
                            city:
                              type: string
                            region:
                              type: string
                            country:
                              type: string
                            latitude:
                              type: number
                            longitude:
                              type: number
                          additionalProperties: false
                        createdAt:
                          description: Timestamp at which the contact was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the contact was last updated.
                          type: string
                          format: date-time
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Contacts
      description: Create a new contact in the caller's organization. The owning
        organization is derived from the access token and cannot be set in the
        body. Returns the new contact's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  description: Email address of the contact. Required. Normalized to lowercase;
                    duplicate detection is case-insensitive.
                  type: string
                contactPersonName:
                  description: Full name of the contact person.
                  type: string
                companyName:
                  description: Company or organization name for this contact.
                  type: string
                title:
                  description: Job title of the contact person.
                  type: string
                phone:
                  description: Phone number of the contact.
                  type: string
                fax:
                  description: Fax number of the contact.
                  type: string
                address:
                  description: Postal address of the contact. All sub-fields are optional.
                  type: object
                  properties:
                    type:
                      type: string
                    rawAddress:
                      type: string
                    line1:
                      type: string
                    line2:
                      type: string
                    postalCode:
                      type: string
                    city:
                      type: string
                    region:
                      type: string
                    country:
                      type: string
                    latitude:
                      type: number
                    longitude:
                      type: number
              required:
                - email
              additionalProperties: false
              description: Request body for creating a contact. The owning organization is
                always derived from the caller's token and cannot be set here.
        description: Request body for creating a contact. The owning organization is
          always derived from the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response returned by contact write operations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the contact.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response returned by contact write operations.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /contacts/{id}:
    get:
      tags:
        - Contacts
      description: Read a single contact by id, scoped to the caller's organization.
        Use the `fields` query parameter to limit the response to a projection
        of the contact.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            contactPersonName, companyName, title, email, phone, fax, address,
            createdAt, updatedAt"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the contact to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the contact.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contactPersonName:
                    description: Full name of the contact person.
                    type: string
                  companyName:
                    description: Company or organization name for this contact.
                    type: string
                  title:
                    description: Job title of the contact person.
                    type: string
                  email:
                    description: Email address of the contact.
                  phone:
                    description: Phone number of the contact.
                    type: string
                  fax:
                    description: Fax number of the contact.
                    type: string
                  address:
                    description: Postal address of the contact. All sub-fields are optional.
                    type: object
                    properties:
                      type:
                        type: string
                      rawAddress:
                        type: string
                      line1:
                        type: string
                      line2:
                        type: string
                      postalCode:
                        type: string
                      city:
                        type: string
                      region:
                        type: string
                      country:
                        type: string
                      latitude:
                        type: number
                      longitude:
                        type: number
                    additionalProperties: false
                  createdAt:
                    description: Timestamp at which the contact was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the contact was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Contacts
      description: Partially update a contact. Any subset of writable fields may be
        provided; omitted fields are left unchanged. Returns the contact's
        identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  description: Email address of the contact. Required. Normalized to lowercase;
                    duplicate detection is case-insensitive.
                  type: string
                contactPersonName:
                  description: Full name of the contact person.
                  type: string
                companyName:
                  description: Company or organization name for this contact.
                  type: string
                title:
                  description: Job title of the contact person.
                  type: string
                phone:
                  description: Phone number of the contact.
                  type: string
                fax:
                  description: Fax number of the contact.
                  type: string
                address:
                  description: Postal address of the contact. All sub-fields are optional.
                  type: object
                  properties:
                    type:
                      type: string
                    rawAddress:
                      type: string
                    line1:
                      type: string
                    line2:
                      type: string
                    postalCode:
                      type: string
                    city:
                      type: string
                    region:
                      type: string
                    country:
                      type: string
                    latitude:
                      type: number
                    longitude:
                      type: number
              additionalProperties: false
              description: Request body for partially updating a contact. All fields are
                optional; omitted fields are left unchanged. The owning
                organization is always derived from the caller's token and
                cannot be set here.
        description: Request body for partially updating a contact. All fields are
          optional; omitted fields are left unchanged. The owning organization
          is always derived from the caller's token and cannot be set here.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the contact to update.
      responses:
        "200":
          description: Identifier-only response returned by contact write operations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the contact.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response returned by contact write operations.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Contacts
      description: "Delete a contact by ID. The contact must not be assigned to any
        primary records — if it is still referenced, the request returns 409
        with the referencing record ids in the error details. Note: deleting a
        contact does not cascade to legacy party contact data
        (PartyData.partyContacts); API-created contacts have no party
        association."
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the contact to delete.
      responses:
        "200":
          description: Identifier-only response returned by contact write operations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the contact.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response returned by contact write operations.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "409":
          description: The request conflicts with the current state of the resource — for
            example, attempting to delete a contact that is still assigned to
            one or more primary records.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 409
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request conflicts with the current state of the resource — for
                  example, attempting to delete a contact that is still assigned
                  to one or more primary records.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-objects:
    post:
      tags:
        - Context Objects
      description: Create a new context object (context type definition) in the
        caller's organization. Returns the new context object's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable singular name of the context object type (e.g.
                    "Project").
                pluralName:
                  type: string
                  minLength: 1
                  description: Human-readable plural name of the context object type (e.g.
                    "Projects").
                icon:
                  default: shop
                  description: Icon identifier shown in the UI for context records of this type.
                    Defaults to `shop`.
                  type: string
                  minLength: 1
              required:
                - name
                - pluralName
              description: Request body for creating a context object (context type) in the
                caller's organization. The owning organization is always derived
                from the caller's token and cannot be set here.
        description: Request body for creating a context object (context type) in the
          caller's organization. The owning organization is always derived from
          the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            context object but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  context object but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Context Objects
      description: >-
        List context objects (context type definitions) in the caller's
        organization. Results are always scoped to the caller's organization.
        Supports pagination via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `icon` (string), `name` (string), `pluralName` (string), `slug`
        (string). See "List Endpoints" in the API overview for operator syntax
        and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: icon, name, pluralName, slug, createdAt,
            updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, icon, name, pluralName, slug, createdAt, updatedAt"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: icon
          required: false
          description: Filter by icon. String value (e.g. `icon=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: pluralName
          required: false
          description: Filter by pluralName. String value (e.g. `pluralName=example`). See
            "List Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: slug
          required: false
          description: Filter by slug. String value (e.g. `slug=example`). See "List
            Endpoints" in the API overview for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the context object.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the context
                            object. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        icon:
                          type: string
                          minLength: 1
                          description: Icon identifier shown in the UI for context records of this type.
                        name:
                          type: string
                          minLength: 1
                          description: Human-readable singular name of the context object type.
                        pluralName:
                          type: string
                          minLength: 1
                          description: Human-readable plural name of the context object type.
                        slug:
                          type: string
                          minLength: 1
                          description: URL-safe slug derived from the name; unique within the
                            organization.
                        createdAt:
                          description: Timestamp at which the context object was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the context object was last updated.
                          type: string
                          format: date-time
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-objects/{id}:
    delete:
      tags:
        - Context Objects
      description: Delete a context object (context type definition). Returns the
        deleted context object id on success. Deleting an object that does not
        exist returns 404 with the shared NotFoundError shape.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context object to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            context object but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  context object but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Context Objects
      description: Read a single context object (context type definition) by id,
        scoped to the caller's organization. Use the `fields` query parameter to
        limit the response to a projection.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, icon, name, pluralName, slug, createdAt, updatedAt"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context object to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the context
                      object. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  icon:
                    type: string
                    minLength: 1
                    description: Icon identifier shown in the UI for context records of this type.
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable singular name of the context object type.
                  pluralName:
                    type: string
                    minLength: 1
                    description: Human-readable plural name of the context object type.
                  slug:
                    type: string
                    minLength: 1
                    description: URL-safe slug derived from the name; unique within the
                      organization.
                  createdAt:
                    description: Timestamp at which the context object was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the context object was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Context Objects
      description: Partially update a context object (context type definition). Any
        subset of writable fields may be provided; omitted fields are left
        unchanged. Returns the full updated context object.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable singular name of the context object type (e.g.
                    "Project").
                pluralName:
                  type: string
                  minLength: 1
                  description: Human-readable plural name of the context object type (e.g.
                    "Projects").
                icon:
                  type: string
                  minLength: 1
                  description: Icon identifier shown in the UI for context records of this type.
                    Defaults to `shop`.
              description: Request body for partially updating a context object. Any subset of
                the create-body fields may be provided.
        description: Request body for partially updating a context object. Any subset of
          the create-body fields may be provided.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context object to update.
      responses:
        "200":
          description: A context object — a configurable context type definition in the
            workspace (e.g. Project, Job).
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the context
                      object. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  icon:
                    type: string
                    minLength: 1
                    description: Icon identifier shown in the UI for context records of this type.
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable singular name of the context object type.
                  pluralName:
                    type: string
                    minLength: 1
                    description: Human-readable plural name of the context object type.
                  slug:
                    type: string
                    minLength: 1
                    description: URL-safe slug derived from the name; unique within the
                      organization.
                  createdAt:
                    default: 2026-07-21T13:34:49.143Z
                    description: Timestamp at which the context object was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.143Z
                    description: Timestamp at which the context object was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                  - organization
                  - icon
                  - name
                  - pluralName
                  - slug
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A context object — a configurable context type definition in the
                  workspace (e.g. Project, Job).
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records:
    post:
      tags:
        - Context Records
      description: Create a new context record in the caller's organization. The
        owning organization is derived from the access token. Returns the new
        record's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contextObjectId:
                  description: Identifier of the context object (type) this record belongs to. See
                    `/v2/context-objects`.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                status:
                  default: active
                  description: Lifecycle status of the context record. Defaults to `active` on
                    creation.
                  type: string
                  enum:
                    - active
                    - archived
                name:
                  type: string
                  minLength: 1
                  description: Human-readable name of the context record.
                description:
                  description: Free-form description of the context record.
                  type: string
                  minLength: 1
                startDate:
                  description: Optional start date of the context record (e.g. project start).
                endDate:
                  description: Optional end date of the context record (e.g. project end).
                externalCodes:
                  default: []
                  description: External identifiers (e.g. ERP / accounting codes) attached to this
                    context record.
                  type: array
                  items:
                    type: string
                    minLength: 1
                archivedAt:
                  description: Timestamp at which the context record was archived, if applicable.
              required:
                - contextObjectId
                - name
              description: Request body for creating a context record. The owning organization
                is always derived from the caller's token and cannot be set
                here.
        description: Request body for creating a context record. The owning organization
          is always derived from the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            context record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  context record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Context Records
      description: >-
        List context records in the caller's organization. Results are always
        scoped to the caller's organization. Supports pagination via `limit` and
        `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `contextObjectId` (objectId), `name` (string), `status` (string),
        `externalCodes` (string), `startDate` (date), `endDate` (date),
        `archivedAt` (date). See "List Endpoints" in the API overview for
        operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: status, name, description, startDate,
            endDate, archivedAt, createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, contextObjectId, status, name, description, startDate,
            endDate, externalCodes, archivedAt, createdAt, updatedAt"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: contextObjectId
          required: false
          description: Filter by contextObjectId. 24-character hex ObjectId string (e.g.
            `contextObjectId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: status
          required: false
          description: Filter by status. String value (e.g. `status=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: externalCodes
          required: false
          description: Filter by externalCodes. String value (e.g.
            `externalCodes=example`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: startDate
          required: false
          description: Filter by startDate. ISO-8601 date or datetime string (e.g.
            `startDate=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: endDate
          required: false
          description: Filter by endDate. ISO-8601 date or datetime string (e.g.
            `endDate=2024-01-15`). See "List Endpoints" in the API overview for
            operator syntax.
        - schema:
            type: string
          in: query
          name: archivedAt
          required: false
          description: Filter by archivedAt. ISO-8601 date or datetime string (e.g.
            `archivedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the context record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the context
                            record. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        contextObjectId:
                          description: Identifier of the context object (type) this record belongs to. See
                            `/v2/context-objects`.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        status:
                          type: string
                          enum:
                            - active
                            - archived
                          description: Lifecycle status of the context record. Defaults to `active` on
                            creation.
                        name:
                          type: string
                          minLength: 1
                          description: Human-readable name of the context record.
                        description:
                          description: Free-form description of the context record.
                        startDate:
                          description: Optional start date of the context record (e.g. project start).
                          type: string
                          format: date-time
                        endDate:
                          description: Optional end date of the context record (e.g. project end).
                          type: string
                          format: date-time
                        externalCodes:
                          type: array
                          items:
                            type: string
                            minLength: 1
                          description: External identifiers (e.g. ERP / accounting codes) attached to this
                            context record.
                        archivedAt:
                          description: Timestamp at which the context record was archived, if applicable.
                          type: string
                          format: date-time
                        createdAt:
                          description: Timestamp at which the context record was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the context record was last updated.
                          type: string
                          format: date-time
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}:
    delete:
      tags:
        - Context Records
      description: Delete a context record. Returns the deleted record id on success.
        Deleting a record that does not exist returns 404 with the shared
        NotFoundError shape.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            context record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  context record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Context Records
      description: Read a single context record by id, scoped to the caller's
        organization. Use the `fields` query parameter to limit the response to
        a projection of the record.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, contextObjectId, status, name, description, startDate,
            endDate, externalCodes, archivedAt, createdAt, updatedAt"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the context
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextObjectId:
                    description: Identifier of the context object (type) this record belongs to. See
                      `/v2/context-objects`.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  status:
                    type: string
                    enum:
                      - active
                      - archived
                    description: Lifecycle status of the context record. Defaults to `active` on
                      creation.
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable name of the context record.
                  description:
                    description: Free-form description of the context record.
                  startDate:
                    description: Optional start date of the context record (e.g. project start).
                    type: string
                    format: date-time
                  endDate:
                    description: Optional end date of the context record (e.g. project end).
                    type: string
                    format: date-time
                  externalCodes:
                    type: array
                    items:
                      type: string
                      minLength: 1
                    description: External identifiers (e.g. ERP / accounting codes) attached to this
                      context record.
                  archivedAt:
                    description: Timestamp at which the context record was archived, if applicable.
                    type: string
                    format: date-time
                  createdAt:
                    description: Timestamp at which the context record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the context record was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Context Records
      description: Partially update a context record. Any subset of writable fields
        may be provided; omitted fields are left unchanged. Returns the full
        updated record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contextObjectId:
                  description: Identifier of the context object (type) this record belongs to. See
                    `/v2/context-objects`.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                status:
                  type: string
                  enum:
                    - active
                    - archived
                  description: Lifecycle status of the context record. Defaults to `active` on
                    creation.
                name:
                  type: string
                  minLength: 1
                  description: Human-readable name of the context record.
                description:
                  description: Free-form description of the context record.
                  type: string
                  minLength: 1
                startDate:
                  description: Optional start date of the context record (e.g. project start).
                endDate:
                  description: Optional end date of the context record (e.g. project end).
                externalCodes:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  description: External identifiers (e.g. ERP / accounting codes) attached to this
                    context record.
                archivedAt:
                  description: Timestamp at which the context record was archived, if applicable.
              description: Request body for partially updating a context record. Any subset of
                the create-body fields may be provided.
        description: Request body for partially updating a context record. Any subset of
          the create-body fields may be provided.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record to update.
      responses:
        "200":
          description: A context record — a grouping or scoping entity (e.g. a project)
            that request records can be attached to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the context record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the context
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextObjectId:
                    description: Identifier of the context object (type) this record belongs to. See
                      `/v2/context-objects`.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  status:
                    default: active
                    description: Lifecycle status of the context record. Defaults to `active` on
                      creation.
                    type: string
                    enum:
                      - active
                      - archived
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable name of the context record.
                  description:
                    description: Free-form description of the context record.
                  startDate:
                    description: Optional start date of the context record (e.g. project start).
                    type: string
                    format: date-time
                  endDate:
                    description: Optional end date of the context record (e.g. project end).
                    type: string
                    format: date-time
                  externalCodes:
                    default: []
                    description: External identifiers (e.g. ERP / accounting codes) attached to this
                      context record.
                    type: array
                    items:
                      type: string
                      minLength: 1
                  archivedAt:
                    description: Timestamp at which the context record was archived, if applicable.
                    type: string
                    format: date-time
                  createdAt:
                    default: 2026-07-21T13:34:49.149Z
                    description: Timestamp at which the context record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.149Z
                    description: Timestamp at which the context record was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                  - organization
                  - contextObjectId
                  - status
                  - name
                  - externalCodes
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A context record — a grouping or scoping entity (e.g. a project)
                  that request records can be attached to.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}/attributes/{attributeId}:
    delete:
      tags:
        - Context Records
      description: Delete an attribute (custom field value) from a context record.
        Returns 404 when the context record or the specific attribute does not
        exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Context Records
      description: Read a single attribute (custom field value) embedded in a context
        record. Returns 404 when the context record or the specific attribute
        does not exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: A single attribute (custom field value) embedded in a context
            record. Context-record attributes carry only scalar values;
            option-id selections are not supported on this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the custom field definition this attribute value is
                      associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  value:
                    description: Scalar value for text or numeric custom field types.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: number
                required:
                  - id
                additionalProperties: false
                description: A single attribute (custom field value) embedded in a context
                  record. Context-record attributes carry only scalar values;
                  option-id selections are not supported on this resource.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    put:
      tags:
        - Context Records
      description: Create or replace an attribute (custom field value) on a context
        record. This is a full replacement — omitting `value` or `optionIds`
        clears that field. Returns 404 when the context record or the referenced
        custom field definition does not exist.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                optionIds:
                  description: Selected option identifiers for dropdown / multi-select custom
                    field types.
                  minItems: 1
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                optionId:
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                value:
                  description: Scalar value for text or numeric custom field types.
                  anyOf:
                    - type: string
                      minLength: 1
                    - type: number
              description: Request body for creating or replacing an attribute value on a
                context record. Provide `value` for text/numeric fields or
                `optionIds` for dropdown fields.
        description: Request body for creating or replacing an attribute value on a
          context record. Provide `value` for text/numeric fields or `optionIds`
          for dropdown fields.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}/attributes:
    get:
      tags:
        - Context Records
      description: List attributes (custom field values) embedded in a context record.
        Attributes are stored as an embedded array on the record — there is no
        separate filtering beyond pagination. Results are scoped to the caller's
        organization.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record whose attributes to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: Identifier of the custom field definition this attribute value is
                            associated with.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        value:
                          description: Scalar value for text or numeric custom field types.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: number
                      required:
                        - id
                      additionalProperties: false
                      description: A single attribute (custom field value) embedded in a context
                        record. Context-record attributes carry only scalar
                        values; option-id selections are not supported on this
                        resource.
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}/request-records:
    get:
      tags:
        - Context Records
      description: >-
        List request records scoped to a specific context record. The
        `contextRecordId` filter is always fixed to the `:id` path parameter and
        cannot be overridden by the caller. Supports pagination via `limit` and
        `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `primaryRecordId` (objectId), `name` (string), `status` (string),
        `complianceTracking` (string), `complianceStatus` (string),
        `complianceProfile.complianceProfileId` (objectId). See "List Endpoints"
        in the API overview for operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: contextRecordId, name, status,
            complianceTracking, complianceStatus, createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: primaryRecordId
          required: false
          description: Filter by primaryRecordId. 24-character hex ObjectId string (e.g.
            `primaryRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: status
          required: false
          description: Filter by status. String value (e.g. `status=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceTracking
          required: false
          description: Filter by complianceTracking. String value (e.g.
            `complianceTracking=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceStatus
          required: false
          description: Filter by complianceStatus. String value (e.g.
            `complianceStatus=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceProfile.complianceProfileId
          required: false
          description: Filter by complianceProfile.complianceProfileId. 24-character hex
            ObjectId string (e.g.
            `complianceProfile.complianceProfileId=507f1f77bcf86cd799439011`).
            See "List Endpoints" in the API overview for operator syntax.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record whose request records to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the request record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the request
                            record. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        primaryRecordId:
                          description: Identifier of the primary record this request record is assigned
                            to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        contextRecordId:
                          description: Optional identifier of the context record (e.g. project) this
                            request record is scoped to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          description: Human-readable name of the request record.
                          type: string
                          minLength: 1
                        status:
                          type: string
                          enum:
                            - active
                            - archived
                          description: Lifecycle status of the request record.
                        complianceTracking:
                          type: boolean
                          description: Whether compliance tracking is enabled for this request record.
                        complianceProfile:
                          description: Compliance profile currently assigned to this request record, if
                            any. Manage assignments via the
                            `/compliance-profile` sub-routes.
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                            name:
                              type: string
                          required:
                            - id
                          additionalProperties: false
                        complianceStatus:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this request record, derived from its
                            assigned compliance profile and submitted documents.
                        createdAt:
                          description: Timestamp at which the request record was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the request record was last updated.
                          type: string
                          format: date-time
                        complianceModules:
                          description: Compliance modules expanded by subject and requirement, populated
                            from the assigned compliance profile. Returned only
                            when explicitly requested via
                            `?fields=complianceModules`.
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance module (e.g.
                                  general-liability).
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the module.
                              status:
                                type: string
                                enum:
                                  - non_compliant
                                  - compliant
                                description: Computed compliance status of this module.
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this module.
                              documentsCount:
                                type: number
                                description: Number of documents associated with this module.
                              subjects:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    code:
                                      type: string
                                      minLength: 1
                                      description: Stable identifier of this compliance subject.
                                    label:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the subject (e.g. coverage limit, named
                                        insured).
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                      description: Compliance status of this subject, computed across its
                                        requirements.
                                    notes:
                                      description: Free-form notes attached to the subject (internal use).
                                      type: string
                                      minLength: 1
                                    effectiveDate:
                                      description: Earliest effective date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        begins. May be `AlwaysValid` for
                                        documents with always-valid coverage.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    expirationDate:
                                      description: Latest expiration date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        ends. May be `AlwaysValid` or overridden
                                        by a cancellation.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    cancellation:
                                      description: Active cancellation that overrides this subject's expiration, if
                                        any.
                                      type: object
                                      properties:
                                        effectiveDate:
                                          type: string
                                          format: date-time
                                      required:
                                        - effectiveDate
                                      additionalProperties: false
                                    latestExpirationDate:
                                      description: Furthest future expiration date across all matching documents (not
                                        only those in force).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidEffectiveDate:
                                      description: Most recent valid effective date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidExpirationDate:
                                      description: Most recent valid expiration date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this subject.
                                    resetOn:
                                      description: Timestamp at which the subject's computed state was last reset.
                                      type: string
                                      format: date-time
                                    validityStatus:
                                      type: string
                                      enum:
                                        - active
                                        - expiring
                                        - cancelling
                                        - expired
                                        - cancelled
                                        - inactive
                                      description: Validity classification of the subject (e.g. valid, expiring,
                                        expired).
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this subject.
                                    requirements:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          complianceProfile:
                                            type: object
                                            properties:
                                              id:
                                                $ref: "#/components/schemas/objectId"
                                              name:
                                                type: string
                                            required:
                                              - id
                                            additionalProperties: false
                                            description: Compliance profile this requirement was sourced from at the time
                                              the request record was assigned.
                                          id:
                                            description: Stable identifier of this requirement instance.
                                            allOf:
                                              - $ref: "#/components/schemas/objectId"
                                          moduleCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent compliance module.
                                          moduleLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent compliance module.
                                          subjectCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent subject within the module.
                                          subjectLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent subject.
                                          attributeCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the attribute (specific check) this requirement
                                              evaluates.
                                          attributeLabel:
                                            description: Human-readable label of the attribute.
                                            type: string
                                            minLength: 1
                                          attributeDescription:
                                            description: Free-form description of the attribute, surfaced on document
                                              request pages.
                                            type: string
                                            minLength: 1
                                          matchingCriteria:
                                            type: string
                                            enum:
                                              - matchOnce
                                              - matchRepeatedly
                                            description: How the requirement matches against documents (e.g. once, on every
                                              document).
                                          operator:
                                            type: string
                                            enum:
                                              - must be present
                                              - must be greater or equal to
                                              - must be less than or equal to
                                              - must be completed by
                                              - at least one must be present
                                                from list
                                            description: Comparison operator applied between extracted document values and
                                              `targetValue`.
                                          targetValue:
                                            description: Target value(s) the document data is compared against.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: array
                                                items:
                                                  type: string
                                                  minLength: 1
                                          value:
                                            description: Resolved value extracted from a matching document, when one is
                                              present.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: number
                                          status:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                              - waived
                                              - overridden
                                              - invalidated
                                            description: Computed compliance status of this requirement.
                                          invalidatedBy:
                                            description: Identifiers of conditions that invalidate this requirement, if any.
                                            type: array
                                            items:
                                              type: string
                                              minLength: 1
                                          notes:
                                            description: Internal-only notes (e.g. context for a waiver or override).
                                            type: string
                                            minLength: 1
                                          publicNotes:
                                            description: Public-facing notes shown alongside the requirement on the document
                                              request page.
                                            type: object
                                            properties:
                                              content:
                                                type: string
                                                minLength: 1
                                              removeWhenCompliant:
                                                type: boolean
                                            required:
                                              - content
                                              - removeWhenCompliant
                                            additionalProperties: false
                                          documents:
                                            type: array
                                            items:
                                              type: object
                                              properties:
                                                id:
                                                  $ref: "#/components/schemas/objectId"
                                                origin:
                                                  type: string
                                                  enum:
                                                    - upload
                                                    - makeCompliant
                                                    - connectedDocument
                                                    - fillableForm
                                                    - webForm
                                                complianceStatus:
                                                  type: string
                                                  enum:
                                                    - compliant
                                                    - non_compliant
                                              required:
                                                - id
                                                - complianceStatus
                                              additionalProperties: false
                                            description: Documents associated with this requirement, with each document's
                                              contribution to compliance.
                                          documentsCount:
                                            type: number
                                            description: Number of documents associated with this requirement.
                                          resetOn:
                                            description: Timestamp at which the requirement's computed state was last reset.
                                            type: string
                                            format: date-time
                                          custom:
                                            type: boolean
                                            description: Whether this requirement was added as a workspace customization
                                              rather than from a built-in
                                              module.
                                        required:
                                          - complianceProfile
                                          - moduleCode
                                          - moduleLabel
                                          - subjectCode
                                          - subjectLabel
                                          - attributeCode
                                          - matchingCriteria
                                          - operator
                                          - status
                                          - documents
                                          - documentsCount
                                          - custom
                                        additionalProperties: false
                                        description: A single compliance requirement under a subject of a compliance
                                          module.
                                      description: Requirements grouped under this subject.
                                  required:
                                    - code
                                    - label
                                    - status
                                    - documentsCount
                                    - validityStatus
                                    - documents
                                    - requirements
                                  additionalProperties: false
                                  description: A subject within a compliance module (e.g. coverage limit, named
                                    insured).
                                description: Subjects expanded under this compliance module.
                            required:
                              - code
                              - label
                              - status
                              - documents
                              - documentsCount
                              - subjects
                            additionalProperties: false
                            description: A single compliance module from the assigned profile, expanded with
                              its subjects and requirements.
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Context Records
      description: Create a new request record and assign it to a context record. The
        context record is identified by the `:id` path parameter. Returns the
        new request record's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryRecordId:
                  description: Identifier of the primary record (entity, e.g. a vendor) to assign
                    this request record to.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                name:
                  description: Human-readable name for the request record.
                  type: string
                  minLength: 1
                status:
                  default: active
                  description: Initial lifecycle status of the request record. Defaults to
                    `active`.
                  type: string
                  enum:
                    - active
                    - archived
                complianceTracking:
                  default: true
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
                  type: boolean
                complianceProfileId:
                  description: Optional identifier of the compliance profile to assign to this
                    request record.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
              required:
                - primaryRecordId
              description: Request body for creating a request record scoped to a context
                record. The `contextRecordId` is always derived from the `:id`
                path parameter and cannot be set here.
        description: Request body for creating a request record scoped to a context
          record. The `contextRecordId` is always derived from the `:id` path
          parameter and cannot be set here.
      parameters:
        - schema:
            type: string
          in: path
          name: id
          required: true
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}/request-records/{recordId}:
    get:
      tags:
        - Context Records
      description: Read a single request record scoped to a specific context record.
        Returns 404 if the request record does not exist or does not belong to
        the specified context record.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    type: string
                    enum:
                      - active
                      - archived
                    description: Lifecycle status of the request record.
                  complianceTracking:
                    type: boolean
                    description: Whether compliance tracking is enabled for this request record.
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                          description: Documents associated with this module.
                        documentsCount:
                          type: number
                          description: Number of documents associated with this module.
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                                description: Compliance status of this subject, computed across its
                                  requirements.
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                type: number
                                description: Number of documents associated with this subject.
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this subject.
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                      description: Computed compliance status of this requirement.
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this requirement.
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Context Records
      description: Partially update a request record scoped to a specific context
        record. Any subset of the request body fields may be provided; omitted
        fields are left unchanged. Returns the full updated request record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  description: Human-readable name of the request record.
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                    - active
                    - archived
                  description: Lifecycle status of the request record. Defaults to `active` on
                    creation.
                complianceTracking:
                  type: boolean
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
              description: Request body for partially updating a request record. Any subset of
                the writable fields (name, status, complianceTracking) may be
                provided. Re-parenting (primaryRecordId / contextRecordId) and
                compliance-profile assignment are handled via separate
                endpoints.
        description: Request body for partially updating a request record. Any subset of
          the writable fields (name, status, complianceTracking) may be
          provided. Re-parenting (primaryRecordId / contextRecordId) and
          compliance-profile assignment are handled via separate endpoints.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to update.
      responses:
        "200":
          description: A request record — the compliance assignment between a primary
            record (entity) and an optional context record (e.g. project), with
            its own compliance profile and computed status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    default: active
                    description: Lifecycle status of the request record.
                    type: string
                    enum:
                      - active
                      - archived
                  complianceTracking:
                    default: true
                    description: Whether compliance tracking is enabled for this request record.
                    type: boolean
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    default: 2026-07-21T13:34:49.161Z
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.161Z
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          default: []
                          description: Documents associated with this module.
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                        documentsCount:
                          default: 0
                          description: Number of documents associated with this module.
                          type: number
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                default: non_compliant
                                description: Compliance status of this subject, computed across its
                                  requirements.
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                default: 0
                                description: Number of documents associated with this subject.
                                type: number
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                default: []
                                description: Documents associated with this subject.
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      default: matchOnce
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      default: non_compliant
                                      description: Computed compliance status of this requirement.
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          default: false
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      default: []
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                    documentsCount:
                                      default: 0
                                      description: Number of documents associated with this requirement.
                                      type: number
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                  - organization
                  - primaryRecordId
                  - status
                  - complianceTracking
                  - complianceStatus
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A request record — the compliance assignment between a primary
                  record (entity) and an optional context record (e.g. project),
                  with its own compliance profile and computed status.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Context Records
      description: Delete a request record scoped to a specific context record.
        Returns 404 if the request record does not exist or does not belong to
        the specified context record.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the context record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /context-records/{id}/request-records/{recordId}/compliance-certificate:
    get:
      tags: &a1
        - Context Records
      description: Get compliance certificate for a request record in a context record
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id, name,
            processing, url, flag, organization, reviewedAt, archivedAt, notes,
            status, expirationDate, appliesToAllProjects, data, metadata,
            issueDate, createdAt, updatedAt"
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the document.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the document, derived from its source.
                  processing:
                    nullable: true
                    description: Whether the document is still being processed (data extraction,
                      classification). When `true`, extracted fields may not yet
                      be populated.
                    type: boolean
                  url:
                    description: Signed URL to download the document file. Time-limited; regenerated
                      on each read.
                    nullable: true
                    type: string
                  flag:
                    nullable: true
                    description: Compliance flag attached to the document, if any. `null` when the
                      document is not flagged.
                    type: object
                    properties:
                      addedOn:
                        nullable: true
                        description: Timestamp at which the flag was added to the document.
                        type: string
                        format: date-time
                      level:
                        nullable: true
                        description: Severity of the flag (e.g. warning, error).
                        type: string
                      subjects:
                        nullable: true
                        description: Compliance subjects affected by this flag.
                        type: array
                        items: {}
                      notes:
                        nullable: true
                        description: Free-form notes attached to the flag.
                        type: string
                    required:
                      - addedOn
                      - level
                      - subjects
                      - notes
                    additionalProperties: false
                  organization:
                    description: Identifier of the organization (workspace) that owns the document.
                      Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  reviewedAt:
                    nullable: true
                    description: Timestamp at which the document was reviewed, if it has been
                      reviewed.
                    type: string
                    format: date-time
                  archivedAt:
                    nullable: true
                    description: Timestamp at which the document was archived, if archived. `null`
                      for active documents.
                    type: string
                    format: date-time
                  notes:
                    nullable: true
                    description: Free-form notes attached to the document.
                    type: string
                  status:
                    nullable: true
                    description: Lifecycle status of the document.
                    type: string
                  expirationDate:
                    nullable: true
                    description: Expiration date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  appliesToAllProjects:
                    nullable: true
                    description: Whether the document applies to all projects (context records) for
                      the owning primary record, rather than a specific one.
                    type: boolean
                  data:
                    description: Structured data extracted from the document during processing.
                    nullable: true
                  metadata:
                    description: Document metadata (e.g. originating upload source, file
                      properties).
                    nullable: true
                  issueDate:
                    nullable: true
                    description: Issue date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  createdAt:
                    description: Timestamp at which the document was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the document was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags: *a1
      description: Create compliance certificate for a request record in a context record
      requestBody:
        required: true
        content:
          application/json: &a2
            schema:
              type: object
              properties:
                effectiveDate:
                  description: ISO date-time for certificate effective date. It will default to
                    today if not provided
                expirationDate:
                  description: ISO date-time for certificate expiration date. It will default to
                    the earliest expiration date found in currently active
                    documents; if no document is present, it will default to 1
                    year from the effective date
                document:
                  description: The document to upload, only with multipart/form-data
          multipart/form-data: *a2
      parameters:
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
      responses:
        "201":
          description: "{ _id } of compliance certificate document"
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                additionalProperties: false
                description: "{ _id } of compliance certificate document"
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-objects:
    get:
      tags:
        - Primary Objects
      description: >-
        List primary objects (entity type definitions) in the caller's
        organization. Results are always scoped to the caller's organization.
        Supports pagination via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `icon` (string), `name` (string), `pluralName` (string), `slug`
        (string). See "List Endpoints" in the API overview for operator syntax
        and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: icon, name, pluralName, slug, createdAt,
            updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, icon, name, pluralName, slug, createdAt, updatedAt,
            defaultRequestRecords"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: icon
          required: false
          description: Filter by icon. String value (e.g. `icon=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: pluralName
          required: false
          description: Filter by pluralName. String value (e.g. `pluralName=example`). See
            "List Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: slug
          required: false
          description: Filter by slug. String value (e.g. `slug=example`). See "List
            Endpoints" in the API overview for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the primary object.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the primary
                            object. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        icon:
                          type: string
                          minLength: 1
                          description: Icon identifier shown in the UI for primary records of this type.
                        name:
                          type: string
                          minLength: 1
                          description: Human-readable singular name of the primary object type.
                        pluralName:
                          type: string
                          minLength: 1
                          description: Human-readable plural name of the primary object type.
                        slug:
                          type: string
                          minLength: 1
                          description: URL-safe slug derived from the name; unique within the
                            organization.
                        createdAt:
                          description: Timestamp at which the primary object was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the primary object was last updated.
                          type: string
                          format: date-time
                        defaultRequestRecords:
                          type: array
                          items:
                            type: object
                            properties:
                              _id:
                                allOf:
                                  - $ref: "#/components/schemas/objectId"
                              name:
                                type: string
                                minLength: 1
                              enabled:
                                type: boolean
                            additionalProperties: false
                          description: Templates for request records auto-created when a primary record of
                            this type is created.
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Primary Objects
      description: Create a new primary object (entity type definition) in the
        caller's organization. Returns the new primary object's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable singular name of the primary object type (e.g.
                    "Vendor").
                pluralName:
                  type: string
                  minLength: 1
                  description: Human-readable plural name of the primary object type (e.g.
                    "Vendors").
                icon:
                  default: shop
                  description: Icon identifier shown in the UI for primary records of this type.
                    Defaults to `shop`.
                  type: string
                  minLength: 1
                defaultRequestRecords:
                  default: []
                  description: Templates for request records auto-created when a primary record of
                    this type is created. Defaults to an empty list.
                  type: array
                  items:
                    type: object
                    properties:
                      _id:
                        default: 6a5f7579d3b44b030ef69d2c
                        allOf:
                          - $ref: "#/components/schemas/objectIdInput"
                      name:
                        type: string
                        minLength: 1
                      enabled:
                        default: true
                        type: boolean
                    required:
                      - name
              required:
                - name
                - pluralName
              description: Request body for creating a primary object (entity type) in the
                caller's organization. The owning organization is always derived
                from the caller's token and cannot be set here.
        description: Request body for creating a primary object (entity type) in the
          caller's organization. The owning organization is always derived from
          the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            primary object but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  primary object but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-objects/{id}:
    get:
      tags:
        - Primary Objects
      description: Read a single primary object (entity type definition) by id, scoped
        to the caller's organization. Use the `fields` query parameter to limit
        the response to a projection.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, icon, name, pluralName, slug, createdAt, updatedAt,
            defaultRequestRecords"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary object to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the primary
                      object. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  icon:
                    type: string
                    minLength: 1
                    description: Icon identifier shown in the UI for primary records of this type.
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable singular name of the primary object type.
                  pluralName:
                    type: string
                    minLength: 1
                    description: Human-readable plural name of the primary object type.
                  slug:
                    type: string
                    minLength: 1
                    description: URL-safe slug derived from the name; unique within the
                      organization.
                  createdAt:
                    description: Timestamp at which the primary object was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the primary object was last updated.
                    type: string
                    format: date-time
                  defaultRequestRecords:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          type: string
                          minLength: 1
                        enabled:
                          type: boolean
                      additionalProperties: false
                    description: Templates for request records auto-created when a primary record of
                      this type is created.
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Primary Objects
      description: Partially update a primary object (entity type definition). Any
        subset of writable fields may be provided; omitted fields are left
        unchanged. Returns the full updated primary object.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  description: Human-readable singular name of the primary object type (e.g.
                    "Vendor").
                pluralName:
                  type: string
                  minLength: 1
                  description: Human-readable plural name of the primary object type (e.g.
                    "Vendors").
                icon:
                  type: string
                  minLength: 1
                  description: Icon identifier shown in the UI for primary records of this type.
                    Defaults to `shop`.
                defaultRequestRecords:
                  type: array
                  items:
                    type: object
                    properties:
                      _id:
                        default: 6a5f7579d3b44b030ef69d2e
                        allOf:
                          - $ref: "#/components/schemas/objectIdInput"
                      name:
                        type: string
                        minLength: 1
                      enabled:
                        default: true
                        type: boolean
                    required:
                      - name
                  description: Templates for request records auto-created when a primary record of
                    this type is created. Defaults to an empty list.
              description: Request body for partially updating a primary object. Any subset of
                the create-body fields may be provided.
        description: Request body for partially updating a primary object. Any subset of
          the create-body fields may be provided.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary object to update.
      responses:
        "200":
          description: A primary object — a configurable entity type definition in the
            workspace (e.g. Vendor, Subcontractor).
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the primary
                      object. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  icon:
                    type: string
                    minLength: 1
                    description: Icon identifier shown in the UI for primary records of this type.
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable singular name of the primary object type.
                  pluralName:
                    type: string
                    minLength: 1
                    description: Human-readable plural name of the primary object type.
                  slug:
                    type: string
                    minLength: 1
                    description: URL-safe slug derived from the name; unique within the
                      organization.
                  createdAt:
                    default: 2026-07-21T13:34:49.168Z
                    description: Timestamp at which the primary object was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.168Z
                    description: Timestamp at which the primary object was last updated.
                    type: string
                    format: date-time
                  defaultRequestRecords:
                    default: []
                    description: Templates for request records auto-created when a primary record of
                      this type is created.
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          default: 6a5f7579d3b44b030ef69d2f
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          type: string
                          minLength: 1
                        enabled:
                          default: true
                          type: boolean
                      additionalProperties: false
                required:
                  - _id
                  - organization
                  - icon
                  - name
                  - pluralName
                  - slug
                  - createdAt
                  - updatedAt
                  - defaultRequestRecords
                additionalProperties: false
                description: A primary object — a configurable entity type definition in the
                  workspace (e.g. Vendor, Subcontractor).
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Objects
      description: Delete a primary object (entity type definition). Returns the
        deleted primary object id on success. Deleting an object that does not
        exist returns 404 with the shared NotFoundError shape.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary object to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            primary object but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  primary object but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records:
    get:
      tags:
        - Primary Records
      description: >-
        List primary records in the caller's organization. Results are always
        scoped to the caller's organization. Supports pagination via `limit` and
        `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `primaryObjectId` (objectId), `typeId` (objectId), `name` (string),
        `status` (string), `externalCodes` (string). See "List Endpoints" in the
        API overview for operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: typeId, status, name, additionalNotes,
            createdAt, updatedAt, website, portalMessage"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryObjectId, typeId, status, name, address,
            automationsEnabled, externalCodes, additionalNotes, createdAt,
            updatedAt, website, portalMessage, computedAttributes, contacts"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: primaryObjectId
          required: false
          description: Filter by primaryObjectId. 24-character hex ObjectId string (e.g.
            `primaryObjectId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: typeId
          required: false
          description: Filter by typeId. 24-character hex ObjectId string (e.g.
            `typeId=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: status
          required: false
          description: Filter by status. String value (e.g. `status=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: externalCodes
          required: false
          description: Filter by externalCodes. String value (e.g.
            `externalCodes=example`). See "List Endpoints" in the API overview
            for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the primary record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the primary
                            record. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        primaryObjectId:
                          description: Identifier of the primary object (entity type) this record belongs
                            to. See `/v2/primary-objects`.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        typeId:
                          description: Optional identifier of the record type within the primary object.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        status:
                          type: string
                          enum:
                            - active
                            - archived
                          description: Lifecycle status of the primary record. Defaults to `active` on
                            creation.
                        name:
                          description: Human-readable name of the primary record.
                          type: string
                          minLength: 1
                        address:
                          type: object
                          properties:
                            raw:
                              type: string
                              minLength: 1
                            line1:
                              type: string
                              minLength: 1
                            line2:
                              type: string
                              minLength: 1
                            postalCode:
                              type: string
                            city:
                              type: string
                              minLength: 1
                            region:
                              type: string
                              minLength: 1
                            country:
                              type: string
                              minLength: 1
                            latitude:
                              type: number
                            longitude:
                              type: number
                          additionalProperties: false
                          description: Postal address of the primary record. All sub-fields are optional;
                            provide only what is known.
                        automationsEnabled:
                          type: array
                          items:
                            $ref: "#/components/schemas/objectId"
                          description: Identifiers of automations enabled for this primary record.
                        externalCodes:
                          type: array
                          items:
                            type: string
                            minLength: 1
                          description: External identifiers (e.g. ERP / accounting codes) attached to this
                            primary record.
                        additionalNotes:
                          description: Free-form notes about the primary record.
                        createdAt:
                          description: Timestamp at which the primary record was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the primary record was last updated.
                          type: string
                          format: date-time
                        website:
                          description: Website URL for the primary record.
                          nullable: true
                          type: string
                        portalMessage:
                          description: Custom message shown to external parties on this primary record's
                            portal page. Stored and returned as the raw value;
                            template placeholders (e.g. {{primaryRecord.name}})
                            are not resolved by this API.
                          type: string
                          minLength: 1
                        computedAttributes:
                          type: object
                          properties:
                            documentsCount:
                              type: number
                              description: Total number of documents currently associated with this primary
                                record.
                            nonResponsiveSince:
                              description: Timestamp at which the primary record was first considered
                                non-responsive to compliance requests, if
                                applicable.
                              type: string
                              format: date-time
                            lastMessageSentOn:
                              description: Timestamp of the most recent compliance request message sent to
                                this primary record, if any.
                              type: string
                              format: date-time
                          required:
                            - documentsCount
                          additionalProperties: false
                          description: Server-computed, read-only attributes derived from the primary
                            record and its related compliance data. Not writable
                            via this API.
                        contacts:
                          type: array
                          items:
                            type: object
                            properties:
                              email:
                                description: Email address of the contact.
                              contactPersonName:
                                description: Display name of the contact person.
                                type: string
                              primary:
                                type: boolean
                                description: Whether this contact is the primary point of contact for the
                                  record.
                            required:
                              - primary
                            additionalProperties: false
                          description: Read-only summary of the contacts attached to this primary record.
                            To manage contacts use the
                            `/v2/primary-records/:id/contacts` endpoints.
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Primary Records
      description: Create a new primary record in the caller's organization. The
        owning organization is derived from the access token. Returns the new
        record's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryObjectId:
                  description: Identifier of the primary object (entity type) this record belongs
                    to. See `/v2/primary-objects`.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                typeId:
                  description: Optional identifier of the record type within the primary object.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                status:
                  default: active
                  description: Lifecycle status of the primary record. Defaults to `active` on
                    creation.
                  type: string
                  enum:
                    - active
                    - archived
                name:
                  description: Human-readable name of the primary record.
                  type: string
                  minLength: 1
                address:
                  type: object
                  properties:
                    raw:
                      type: string
                      minLength: 1
                    line1:
                      type: string
                      minLength: 1
                    line2:
                      type: string
                      minLength: 1
                    postalCode:
                      type: string
                    city:
                      type: string
                      minLength: 1
                    region:
                      type: string
                      minLength: 1
                    country:
                      type: string
                      minLength: 1
                    latitude:
                      type: number
                    longitude:
                      type: number
                  description: Postal address of the primary record. All sub-fields are optional;
                    provide only what is known.
                automationsEnabled:
                  default: []
                  description: Identifiers of automations enabled for this primary record.
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                externalCodes:
                  default: []
                  description: External identifiers (e.g. ERP / accounting codes) attached to this
                    primary record.
                  type: array
                  items:
                    type: string
                    minLength: 1
                additionalNotes:
                  description: Free-form notes about the primary record.
                  type: string
                  minLength: 1
                website:
                  description: Website URL for the primary record.
                  nullable: true
                  type: string
                portalMessage:
                  description: Custom message shown to external parties on this primary record's
                    portal page. Stored and returned as the raw value; template
                    placeholders (e.g. {{primaryRecord.name}}) are not resolved
                    by this API.
                  type: string
                  minLength: 1
              required:
                - primaryObjectId
              description: Request body for creating a primary record. The owning organization
                is always derived from the caller's token and cannot be set
                here.
        description: Request body for creating a primary record. The owning organization
          is always derived from the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            primary record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  primary record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}:
    get:
      tags:
        - Primary Records
      description: Read a single primary record by id, scoped to the caller's
        organization. Use the `fields` query parameter to limit the response to
        a projection of the record.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryObjectId, typeId, status, name, address,
            automationsEnabled, externalCodes, additionalNotes, createdAt,
            updatedAt, website, portalMessage, computedAttributes, contacts"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the primary
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryObjectId:
                    description: Identifier of the primary object (entity type) this record belongs
                      to. See `/v2/primary-objects`.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  typeId:
                    description: Optional identifier of the record type within the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  status:
                    type: string
                    enum:
                      - active
                      - archived
                    description: Lifecycle status of the primary record. Defaults to `active` on
                      creation.
                  name:
                    description: Human-readable name of the primary record.
                    type: string
                    minLength: 1
                  address:
                    type: object
                    properties:
                      raw:
                        type: string
                        minLength: 1
                      line1:
                        type: string
                        minLength: 1
                      line2:
                        type: string
                        minLength: 1
                      postalCode:
                        type: string
                      city:
                        type: string
                        minLength: 1
                      region:
                        type: string
                        minLength: 1
                      country:
                        type: string
                        minLength: 1
                      latitude:
                        type: number
                      longitude:
                        type: number
                    additionalProperties: false
                    description: Postal address of the primary record. All sub-fields are optional;
                      provide only what is known.
                  automationsEnabled:
                    type: array
                    items:
                      $ref: "#/components/schemas/objectId"
                    description: Identifiers of automations enabled for this primary record.
                  externalCodes:
                    type: array
                    items:
                      type: string
                      minLength: 1
                    description: External identifiers (e.g. ERP / accounting codes) attached to this
                      primary record.
                  additionalNotes:
                    description: Free-form notes about the primary record.
                  createdAt:
                    description: Timestamp at which the primary record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the primary record was last updated.
                    type: string
                    format: date-time
                  website:
                    description: Website URL for the primary record.
                    nullable: true
                    type: string
                  portalMessage:
                    description: Custom message shown to external parties on this primary record's
                      portal page. Stored and returned as the raw value;
                      template placeholders (e.g. {{primaryRecord.name}}) are
                      not resolved by this API.
                    type: string
                    minLength: 1
                  computedAttributes:
                    type: object
                    properties:
                      documentsCount:
                        type: number
                        description: Total number of documents currently associated with this primary
                          record.
                      nonResponsiveSince:
                        description: Timestamp at which the primary record was first considered
                          non-responsive to compliance requests, if applicable.
                        type: string
                        format: date-time
                      lastMessageSentOn:
                        description: Timestamp of the most recent compliance request message sent to
                          this primary record, if any.
                        type: string
                        format: date-time
                    required:
                      - documentsCount
                    additionalProperties: false
                    description: Server-computed, read-only attributes derived from the primary
                      record and its related compliance data. Not writable via
                      this API.
                  contacts:
                    type: array
                    items:
                      type: object
                      properties:
                        email:
                          description: Email address of the contact.
                        contactPersonName:
                          description: Display name of the contact person.
                          type: string
                        primary:
                          type: boolean
                          description: Whether this contact is the primary point of contact for the
                            record.
                      required:
                        - primary
                      additionalProperties: false
                    description: Read-only summary of the contacts attached to this primary record.
                      To manage contacts use the
                      `/v2/primary-records/:id/contacts` endpoints.
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Primary Records
      description: Partially update a primary record. Any subset of writable fields
        may be provided; omitted fields are left unchanged. Status changes are
        accepted on this endpoint and are applied via the dedicated status
        workflow. Returns the full updated record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryObjectId:
                  description: Identifier of the primary object (entity type) this record belongs
                    to. See `/v2/primary-objects`.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                typeId:
                  description: Optional identifier of the record type within the primary object.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                status:
                  type: string
                  enum:
                    - active
                    - archived
                  description: Lifecycle status of the primary record. Defaults to `active` on
                    creation.
                name:
                  description: Human-readable name of the primary record.
                  type: string
                  minLength: 1
                address:
                  type: object
                  properties:
                    raw:
                      type: string
                      minLength: 1
                    line1:
                      type: string
                      minLength: 1
                    line2:
                      type: string
                      minLength: 1
                    postalCode:
                      type: string
                    city:
                      type: string
                      minLength: 1
                    region:
                      type: string
                      minLength: 1
                    country:
                      type: string
                      minLength: 1
                    latitude:
                      type: number
                    longitude:
                      type: number
                  description: Postal address of the primary record. All sub-fields are optional;
                    provide only what is known.
                automationsEnabled:
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                  description: Identifiers of automations enabled for this primary record.
                externalCodes:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  description: External identifiers (e.g. ERP / accounting codes) attached to this
                    primary record.
                additionalNotes:
                  description: Free-form notes about the primary record.
                  type: string
                  minLength: 1
                website:
                  description: Website URL for the primary record.
                  nullable: true
                  type: string
                portalMessage:
                  description: Custom message shown to external parties on this primary record's
                    portal page. Stored and returned as the raw value; template
                    placeholders (e.g. {{primaryRecord.name}}) are not resolved
                    by this API.
                  type: string
                  minLength: 1
              description: Request body for partially updating a primary record. Any subset of
                the create-body fields may be provided.
        description: Request body for partially updating a primary record. Any subset of
          the create-body fields may be provided.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record to update.
      responses:
        "200":
          description: A primary record — the canonical representation of an entity (e.g.
            a vendor) tracked in the workspace.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the primary
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryObjectId:
                    description: Identifier of the primary object (entity type) this record belongs
                      to. See `/v2/primary-objects`.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  typeId:
                    description: Optional identifier of the record type within the primary object.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  status:
                    default: active
                    description: Lifecycle status of the primary record. Defaults to `active` on
                      creation.
                    type: string
                    enum:
                      - active
                      - archived
                  name:
                    description: Human-readable name of the primary record.
                    type: string
                    minLength: 1
                  address:
                    type: object
                    properties:
                      raw:
                        type: string
                        minLength: 1
                      line1:
                        type: string
                        minLength: 1
                      line2:
                        type: string
                        minLength: 1
                      postalCode:
                        type: string
                      city:
                        type: string
                        minLength: 1
                      region:
                        type: string
                        minLength: 1
                      country:
                        type: string
                        minLength: 1
                      latitude:
                        type: number
                      longitude:
                        type: number
                    additionalProperties: false
                    description: Postal address of the primary record. All sub-fields are optional;
                      provide only what is known.
                  automationsEnabled:
                    default: []
                    description: Identifiers of automations enabled for this primary record.
                    type: array
                    items:
                      $ref: "#/components/schemas/objectId"
                  externalCodes:
                    default: []
                    description: External identifiers (e.g. ERP / accounting codes) attached to this
                      primary record.
                    type: array
                    items:
                      type: string
                      minLength: 1
                  additionalNotes:
                    description: Free-form notes about the primary record.
                  createdAt:
                    default: 2026-07-21T13:34:49.174Z
                    description: Timestamp at which the primary record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.174Z
                    description: Timestamp at which the primary record was last updated.
                    type: string
                    format: date-time
                  website:
                    description: Website URL for the primary record.
                    nullable: true
                    type: string
                  portalMessage:
                    description: Custom message shown to external parties on this primary record's
                      portal page. Stored and returned as the raw value;
                      template placeholders (e.g. {{primaryRecord.name}}) are
                      not resolved by this API.
                    type: string
                    minLength: 1
                  computedAttributes:
                    type: object
                    properties:
                      documentsCount:
                        default: 0
                        description: Total number of documents currently associated with this primary
                          record.
                        type: number
                      nonResponsiveSince:
                        description: Timestamp at which the primary record was first considered
                          non-responsive to compliance requests, if applicable.
                        type: string
                        format: date-time
                      lastMessageSentOn:
                        description: Timestamp of the most recent compliance request message sent to
                          this primary record, if any.
                        type: string
                        format: date-time
                    required:
                      - documentsCount
                    additionalProperties: false
                    description: Server-computed, read-only attributes derived from the primary
                      record and its related compliance data. Not writable via
                      this API.
                  contacts:
                    type: array
                    items:
                      type: object
                      properties:
                        email:
                          description: Email address of the contact.
                        contactPersonName:
                          description: Display name of the contact person.
                          type: string
                        primary:
                          default: false
                          description: Whether this contact is the primary point of contact for the
                            record.
                          type: boolean
                      required:
                        - primary
                      additionalProperties: false
                    description: Read-only summary of the contacts attached to this primary record.
                      To manage contacts use the
                      `/v2/primary-records/:id/contacts` endpoints.
                required:
                  - _id
                  - organization
                  - primaryObjectId
                  - status
                  - automationsEnabled
                  - externalCodes
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A primary record — the canonical representation of an entity (e.g.
                  a vendor) tracked in the workspace.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Records
      description: Delete a primary record. The operation is idempotent — deleting a
        record that does not exist (or is already deleted) returns 200 with the
        requested id and is not treated as an error.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            primary record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the primary record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  primary record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/attributes:
    get:
      tags:
        - Primary Records
      description: List attributes (custom field values) embedded in a primary record.
        Attributes are stored as an embedded array on the record — there is no
        separate filtering beyond pagination. Results are scoped to the caller's
        organization.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record whose attributes to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: Identifier of the custom field definition this attribute value is
                            associated with.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        optionIds:
                          description: Selected option identifiers for dropdown / multi-select custom
                            field types.
                          minItems: 1
                          type: array
                          items:
                            $ref: "#/components/schemas/objectId"
                        optionId:
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        value:
                          description: Scalar value for text or numeric custom field types.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: number
                      required:
                        - id
                      additionalProperties: false
                      description: A single attribute (custom field value) embedded in a primary
                        record.
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/attributes/{attributeId}:
    get:
      tags:
        - Primary Records
      description: Read a single attribute (custom field value) embedded in a primary
        record. Returns 404 when the primary record or the specific attribute
        does not exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: A single attribute (custom field value) embedded in a primary record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the custom field definition this attribute value is
                      associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  optionIds:
                    description: Selected option identifiers for dropdown / multi-select custom
                      field types.
                    minItems: 1
                    type: array
                    items:
                      $ref: "#/components/schemas/objectId"
                  optionId:
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  value:
                    description: Scalar value for text or numeric custom field types.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: number
                required:
                  - id
                additionalProperties: false
                description: A single attribute (custom field value) embedded in a primary
                  record.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    put:
      tags:
        - Primary Records
      description: Create or replace an attribute (custom field value) on a primary
        record. This is a full replacement — omitting `value` or `optionIds`
        clears that field. Returns 404 when the primary record or the referenced
        custom field definition does not exist.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                optionIds:
                  description: Selected option identifiers for dropdown / multi-select custom
                    field types.
                  minItems: 1
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                optionId:
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                value:
                  description: Scalar value for text or numeric custom field types.
                  anyOf:
                    - type: string
                      minLength: 1
                    - type: number
              description: Request body for creating or replacing an attribute value on a
                primary record. Provide `value` for text/numeric fields or
                `optionIds` for dropdown fields.
        description: Request body for creating or replacing an attribute value on a
          primary record. Provide `value` for text/numeric fields or `optionIds`
          for dropdown fields.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Records
      description: Delete an attribute (custom field value) from a primary record.
        Returns 404 when the primary record or the specific attribute does not
        exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/request-records:
    get:
      tags:
        - Primary Records
      description: >-
        List request records scoped to a specific primary record. The
        `primaryRecordId` filter is always fixed to the `:id` path parameter and
        cannot be overridden by the caller. Supports pagination via `limit` and
        `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `contextRecordId` (objectId), `name` (string), `status` (string),
        `complianceTracking` (string), `complianceStatus` (string),
        `complianceProfile.complianceProfileId` (objectId). See "List Endpoints"
        in the API overview for operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: contextRecordId, name, status,
            complianceTracking, complianceStatus, createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: contextRecordId
          required: false
          description: Filter by contextRecordId. 24-character hex ObjectId string (e.g.
            `contextRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: status
          required: false
          description: Filter by status. String value (e.g. `status=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceTracking
          required: false
          description: Filter by complianceTracking. String value (e.g.
            `complianceTracking=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceStatus
          required: false
          description: Filter by complianceStatus. String value (e.g.
            `complianceStatus=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceProfile.complianceProfileId
          required: false
          description: Filter by complianceProfile.complianceProfileId. 24-character hex
            ObjectId string (e.g.
            `complianceProfile.complianceProfileId=507f1f77bcf86cd799439011`).
            See "List Endpoints" in the API overview for operator syntax.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record whose request records to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the request record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the request
                            record. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        primaryRecordId:
                          description: Identifier of the primary record this request record is assigned
                            to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        contextRecordId:
                          description: Optional identifier of the context record (e.g. project) this
                            request record is scoped to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          description: Human-readable name of the request record.
                          type: string
                          minLength: 1
                        status:
                          type: string
                          enum:
                            - active
                            - archived
                          description: Lifecycle status of the request record.
                        complianceTracking:
                          type: boolean
                          description: Whether compliance tracking is enabled for this request record.
                        complianceProfile:
                          description: Compliance profile currently assigned to this request record, if
                            any. Manage assignments via the
                            `/compliance-profile` sub-routes.
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                            name:
                              type: string
                          required:
                            - id
                          additionalProperties: false
                        complianceStatus:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this request record, derived from its
                            assigned compliance profile and submitted documents.
                        createdAt:
                          description: Timestamp at which the request record was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the request record was last updated.
                          type: string
                          format: date-time
                        complianceModules:
                          description: Compliance modules expanded by subject and requirement, populated
                            from the assigned compliance profile. Returned only
                            when explicitly requested via
                            `?fields=complianceModules`.
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance module (e.g.
                                  general-liability).
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the module.
                              status:
                                type: string
                                enum:
                                  - non_compliant
                                  - compliant
                                description: Computed compliance status of this module.
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this module.
                              documentsCount:
                                type: number
                                description: Number of documents associated with this module.
                              subjects:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    code:
                                      type: string
                                      minLength: 1
                                      description: Stable identifier of this compliance subject.
                                    label:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the subject (e.g. coverage limit, named
                                        insured).
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                      description: Compliance status of this subject, computed across its
                                        requirements.
                                    notes:
                                      description: Free-form notes attached to the subject (internal use).
                                      type: string
                                      minLength: 1
                                    effectiveDate:
                                      description: Earliest effective date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        begins. May be `AlwaysValid` for
                                        documents with always-valid coverage.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    expirationDate:
                                      description: Latest expiration date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        ends. May be `AlwaysValid` or overridden
                                        by a cancellation.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    cancellation:
                                      description: Active cancellation that overrides this subject's expiration, if
                                        any.
                                      type: object
                                      properties:
                                        effectiveDate:
                                          type: string
                                          format: date-time
                                      required:
                                        - effectiveDate
                                      additionalProperties: false
                                    latestExpirationDate:
                                      description: Furthest future expiration date across all matching documents (not
                                        only those in force).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidEffectiveDate:
                                      description: Most recent valid effective date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidExpirationDate:
                                      description: Most recent valid expiration date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this subject.
                                    resetOn:
                                      description: Timestamp at which the subject's computed state was last reset.
                                      type: string
                                      format: date-time
                                    validityStatus:
                                      type: string
                                      enum:
                                        - active
                                        - expiring
                                        - cancelling
                                        - expired
                                        - cancelled
                                        - inactive
                                      description: Validity classification of the subject (e.g. valid, expiring,
                                        expired).
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this subject.
                                    requirements:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          complianceProfile:
                                            type: object
                                            properties:
                                              id:
                                                $ref: "#/components/schemas/objectId"
                                              name:
                                                type: string
                                            required:
                                              - id
                                            additionalProperties: false
                                            description: Compliance profile this requirement was sourced from at the time
                                              the request record was assigned.
                                          id:
                                            description: Stable identifier of this requirement instance.
                                            allOf:
                                              - $ref: "#/components/schemas/objectId"
                                          moduleCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent compliance module.
                                          moduleLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent compliance module.
                                          subjectCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent subject within the module.
                                          subjectLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent subject.
                                          attributeCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the attribute (specific check) this requirement
                                              evaluates.
                                          attributeLabel:
                                            description: Human-readable label of the attribute.
                                            type: string
                                            minLength: 1
                                          attributeDescription:
                                            description: Free-form description of the attribute, surfaced on document
                                              request pages.
                                            type: string
                                            minLength: 1
                                          matchingCriteria:
                                            type: string
                                            enum:
                                              - matchOnce
                                              - matchRepeatedly
                                            description: How the requirement matches against documents (e.g. once, on every
                                              document).
                                          operator:
                                            type: string
                                            enum:
                                              - must be present
                                              - must be greater or equal to
                                              - must be less than or equal to
                                              - must be completed by
                                              - at least one must be present
                                                from list
                                            description: Comparison operator applied between extracted document values and
                                              `targetValue`.
                                          targetValue:
                                            description: Target value(s) the document data is compared against.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: array
                                                items:
                                                  type: string
                                                  minLength: 1
                                          value:
                                            description: Resolved value extracted from a matching document, when one is
                                              present.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: number
                                          status:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                              - waived
                                              - overridden
                                              - invalidated
                                            description: Computed compliance status of this requirement.
                                          invalidatedBy:
                                            description: Identifiers of conditions that invalidate this requirement, if any.
                                            type: array
                                            items:
                                              type: string
                                              minLength: 1
                                          notes:
                                            description: Internal-only notes (e.g. context for a waiver or override).
                                            type: string
                                            minLength: 1
                                          publicNotes:
                                            description: Public-facing notes shown alongside the requirement on the document
                                              request page.
                                            type: object
                                            properties:
                                              content:
                                                type: string
                                                minLength: 1
                                              removeWhenCompliant:
                                                type: boolean
                                            required:
                                              - content
                                              - removeWhenCompliant
                                            additionalProperties: false
                                          documents:
                                            type: array
                                            items:
                                              type: object
                                              properties:
                                                id:
                                                  $ref: "#/components/schemas/objectId"
                                                origin:
                                                  type: string
                                                  enum:
                                                    - upload
                                                    - makeCompliant
                                                    - connectedDocument
                                                    - fillableForm
                                                    - webForm
                                                complianceStatus:
                                                  type: string
                                                  enum:
                                                    - compliant
                                                    - non_compliant
                                              required:
                                                - id
                                                - complianceStatus
                                              additionalProperties: false
                                            description: Documents associated with this requirement, with each document's
                                              contribution to compliance.
                                          documentsCount:
                                            type: number
                                            description: Number of documents associated with this requirement.
                                          resetOn:
                                            description: Timestamp at which the requirement's computed state was last reset.
                                            type: string
                                            format: date-time
                                          custom:
                                            type: boolean
                                            description: Whether this requirement was added as a workspace customization
                                              rather than from a built-in
                                              module.
                                        required:
                                          - complianceProfile
                                          - moduleCode
                                          - moduleLabel
                                          - subjectCode
                                          - subjectLabel
                                          - attributeCode
                                          - matchingCriteria
                                          - operator
                                          - status
                                          - documents
                                          - documentsCount
                                          - custom
                                        additionalProperties: false
                                        description: A single compliance requirement under a subject of a compliance
                                          module.
                                      description: Requirements grouped under this subject.
                                  required:
                                    - code
                                    - label
                                    - status
                                    - documentsCount
                                    - validityStatus
                                    - documents
                                    - requirements
                                  additionalProperties: false
                                  description: A subject within a compliance module (e.g. coverage limit, named
                                    insured).
                                description: Subjects expanded under this compliance module.
                            required:
                              - code
                              - label
                              - status
                              - documents
                              - documentsCount
                              - subjects
                            additionalProperties: false
                            description: A single compliance module from the assigned profile, expanded with
                              its subjects and requirements.
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Primary Records
      description: Create a new request record and assign it to a primary record. The
        primary record is identified by the `:id` path parameter. Returns the
        new request record's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contextRecordId:
                  description: Optional identifier of the context record (e.g. project) to
                    associate with this request record.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                name:
                  description: Human-readable name for the request record.
                  type: string
                  minLength: 1
                status:
                  default: active
                  description: Initial lifecycle status of the request record. Defaults to
                    `active`.
                  type: string
                  enum:
                    - active
                    - archived
                complianceTracking:
                  default: true
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
                  type: boolean
                complianceProfileId:
                  description: Optional identifier of the compliance profile to assign to this
                    request record.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
              description: Request body for creating a request record scoped to a primary
                record. The `primaryRecordId` is always derived from the `:id`
                path parameter and cannot be set here.
        description: Request body for creating a request record scoped to a primary
          record. The `primaryRecordId` is always derived from the `:id` path
          parameter and cannot be set here.
      parameters:
        - schema:
            type: string
          in: path
          name: id
          required: true
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/request-records/{recordId}:
    get:
      tags:
        - Primary Records
      description: Read a single request record scoped to a specific primary record.
        Returns 404 if the request record does not exist or does not belong to
        the specified primary record.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    type: string
                    enum:
                      - active
                      - archived
                    description: Lifecycle status of the request record.
                  complianceTracking:
                    type: boolean
                    description: Whether compliance tracking is enabled for this request record.
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                          description: Documents associated with this module.
                        documentsCount:
                          type: number
                          description: Number of documents associated with this module.
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                                description: Compliance status of this subject, computed across its
                                  requirements.
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                type: number
                                description: Number of documents associated with this subject.
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this subject.
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                      description: Computed compliance status of this requirement.
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this requirement.
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Primary Records
      description: Partially update a request record scoped to a specific primary
        record. Any subset of the request body fields may be provided; omitted
        fields are left unchanged. Returns the full updated request record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  description: Human-readable name of the request record.
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                    - active
                    - archived
                  description: Lifecycle status of the request record. Defaults to `active` on
                    creation.
                complianceTracking:
                  type: boolean
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
              description: Request body for partially updating a request record. Any subset of
                the writable fields (name, status, complianceTracking) may be
                provided. Re-parenting (primaryRecordId / contextRecordId) and
                compliance-profile assignment are handled via separate
                endpoints.
        description: Request body for partially updating a request record. Any subset of
          the writable fields (name, status, complianceTracking) may be
          provided. Re-parenting (primaryRecordId / contextRecordId) and
          compliance-profile assignment are handled via separate endpoints.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to update.
      responses:
        "200":
          description: A request record — the compliance assignment between a primary
            record (entity) and an optional context record (e.g. project), with
            its own compliance profile and computed status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    default: active
                    description: Lifecycle status of the request record.
                    type: string
                    enum:
                      - active
                      - archived
                  complianceTracking:
                    default: true
                    description: Whether compliance tracking is enabled for this request record.
                    type: boolean
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    default: 2026-07-21T13:34:49.184Z
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.184Z
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          default: []
                          description: Documents associated with this module.
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                        documentsCount:
                          default: 0
                          description: Number of documents associated with this module.
                          type: number
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                default: non_compliant
                                description: Compliance status of this subject, computed across its
                                  requirements.
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                default: 0
                                description: Number of documents associated with this subject.
                                type: number
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                default: []
                                description: Documents associated with this subject.
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      default: matchOnce
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      default: non_compliant
                                      description: Computed compliance status of this requirement.
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          default: false
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      default: []
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                    documentsCount:
                                      default: 0
                                      description: Number of documents associated with this requirement.
                                      type: number
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                  - organization
                  - primaryRecordId
                  - status
                  - complianceTracking
                  - complianceStatus
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A request record — the compliance assignment between a primary
                  record (entity) and an optional context record (e.g. project),
                  with its own compliance profile and computed status.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Records
      description: Delete a request record scoped to a specific primary record.
        Returns 404 if the request record does not exist or does not belong to
        the specified primary record.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
          description: Identifier of the request record to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/contacts:
    get:
      tags:
        - Primary Records
      description: List contacts assigned to a primary record. Contacts are embedded
        within the primary record and are returned in insertion order. Filtering
        by contact fields is not supported — use `limit` and `skip` for
        pagination only.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record whose contacts to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: Identifier of the contact record in the contacts collection.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        email:
                          description: Email address of the contact.
                        primary:
                          default: false
                          description: Whether this contact is the primary point of contact for the
                            primary record.
                          type: boolean
                        defaultRequestRecipient:
                          default: false
                          description: Whether this contact should receive compliance request
                            notifications by default.
                          type: boolean
                        externalCode:
                          description: Optional external identifier for this contact assignment (e.g. an
                            ERP code).
                          type: string
                          minLength: 1
                      required:
                        - id
                        - primary
                        - defaultRequestRecipient
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Primary Records
      description: Assign a contact to a primary record. The contact must already
        exist in the contacts collection. If the contact is already assigned,
        the existing assignment is updated with the provided fields.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  description: Identifier of the contact record to assign to this primary record.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                primary:
                  default: false
                  description: Set to true to mark this contact as the primary point of contact.
                  type: boolean
                defaultRequestRecipient:
                  default: false
                  description: Set to true to include this contact in compliance request
                    notifications by default.
                  type: boolean
                externalCode:
                  description: Optional external identifier for this contact assignment (e.g. an
                    ERP code).
                  type: string
                  minLength: 1
              required:
                - id
              description: Request body for assigning a contact to a primary record. The
                contact must already exist in the contacts collection.
        description: Request body for assigning a contact to a primary record. The
          contact must already exist in the contacts collection.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record to assign the contact to.
      responses:
        "201":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the contact that was created, updated, or deleted.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/contacts/{contactId}:
    get:
      tags:
        - Primary Records
      description: Read a single contact assigned to a primary record. Both the
        primary record and the contact assignment must exist within the caller's
        organization.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: contactId
          required: true
          description: Identifier of the contact to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the contact record in the contacts collection.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  email:
                    description: Email address of the contact.
                  primary:
                    default: false
                    description: Whether this contact is the primary point of contact for the
                      primary record.
                    type: boolean
                  defaultRequestRecipient:
                    default: false
                    description: Whether this contact should receive compliance request
                      notifications by default.
                    type: boolean
                  externalCode:
                    description: Optional external identifier for this contact assignment (e.g. an
                      ERP code).
                    type: string
                    minLength: 1
                required:
                  - id
                  - primary
                  - defaultRequestRecipient
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Primary Records
      description: Partially update a contact assignment on a primary record. Only the
        fields provided in the request body are changed; omitted fields retain
        their current values. Both the primary record and the contact assignment
        must exist within the caller's organization.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primary:
                  type: boolean
                  description: Set to true to mark this contact as the primary point of contact.
                defaultRequestRecipient:
                  type: boolean
                  description: Set to true to include this contact in compliance request
                    notifications by default.
                externalCode:
                  description: Optional external identifier for this contact assignment (e.g. an
                    ERP code).
                  type: string
                  minLength: 1
              description: Request body for partially updating a contact assignment on a
                primary record. Any subset of the writable fields may be
                provided.
        description: Request body for partially updating a contact assignment on a
          primary record. Any subset of the writable fields may be provided.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: contactId
          required: true
          description: Identifier of the contact assignment to update.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the contact that was created, updated, or deleted.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Records
      description: Remove a contact assignment from a primary record. The contact
        record itself is not deleted — only the assignment to this primary
        record is removed. Both the primary record and the contact assignment
        must exist within the caller's organization.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: contactId
          required: true
          description: Identifier of the contact assignment to remove.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the contact that was created, updated, or deleted.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/tags:
    get:
      tags:
        - Primary Records
      description: List tag assignments on a primary record. Tags are stored as an
        embedded array on the parent record, so server-side filtering, sorting,
        and field projection are not supported — only `limit` and `skip` apply.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record whose tag assignments to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: Identifier of the tag (reference to the Tag catalogue entry).
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        expiresAt:
                          description: Optional expiry date for this tag assignment. When set, the tag is
                            considered expired after this timestamp.
                          type: string
                          format: date-time
                      required:
                        - id
                      additionalProperties: false
                      description: A tag assigned to a primary record, optionally with an expiry date.
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Primary Records
      description: Assign a tag to a primary record. The body references an existing
        tag in the same organization by id; an optional `expiresAt` controls
        when the assignment expires.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  description: Identifier of the tag to assign. Must reference an existing tag in
                    the same organization.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                expiresAt:
                  description: Optional expiry date for this tag assignment.
              required:
                - id
              description: Request body for assigning a tag to a primary record.
        description: Request body for assigning a tag to a primary record.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record to assign the tag to.
      responses:
        "201":
          description: Lightweight response carrying only the tag identifier.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the tag.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
                description: Lightweight response carrying only the tag identifier.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/tags/{tagId}:
    get:
      tags:
        - Primary Records
      description: Read a single tag assignment on a primary record by tag id.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: tagId
          required: true
          description: Identifier of the tag assignment on the primary record.
      responses:
        "200":
          description: A tag assigned to a primary record, optionally with an expiry date.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the tag (reference to the Tag catalogue entry).
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  expiresAt:
                    description: Optional expiry date for this tag assignment. When set, the tag is
                      considered expired after this timestamp.
                    type: string
                    format: date-time
                required:
                  - id
                additionalProperties: false
                description: A tag assigned to a primary record, optionally with an expiry date.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Primary Records
      description: Partially update a tag assignment on a primary record (currently
        only the `expiresAt` field can be updated).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                expiresAt:
                  description: Optional expiry date for this tag assignment.
              description: Request body for partially updating a tag assignment. Only
                `expiresAt` is patchable.
        description: Request body for partially updating a tag assignment. Only
          `expiresAt` is patchable.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: tagId
          required: true
          description: Identifier of the tag assignment on the primary record.
      responses:
        "200":
          description: A tag assigned to a primary record, optionally with an expiry date.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the tag (reference to the Tag catalogue entry).
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  expiresAt:
                    description: Optional expiry date for this tag assignment. When set, the tag is
                      considered expired after this timestamp.
                    type: string
                    format: date-time
                required:
                  - id
                additionalProperties: false
                description: A tag assigned to a primary record, optionally with an expiry date.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Primary Records
      description: Remove a tag assignment from a primary record. The operation is
        idempotent — removing a tag that is not assigned (or no longer assigned)
        returns 200 with the requested tag id and is not treated as an error.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: tagId
          required: true
          description: Identifier of the tag assignment on the primary record.
      responses:
        "200":
          description: Lightweight response carrying only the tag identifier.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the tag.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
                description: Lightweight response carrying only the tag identifier.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/compliance-certificates:
    post:
      tags:
        - Primary Records
      description: Create compliance certificates for all request records under a
        primary record. Accepts application/json with optional effectiveDate and
        expirationDate fields, or multipart/form-data with the same date fields
        plus a document file in the `document` field. Returns one certificate
        identifier per request record processed.
      requestBody:
        required: true
        content:
          application/json: &a3
            schema:
              type: object
              properties:
                effectiveDate:
                  description: ISO date-time for certificate effective date. It will default to
                    today if not provided
                expirationDate:
                  description: ISO date-time for certificate expiration date. It will default to
                    the earliest expiration date found in currently active
                    documents; if no document is present, it will default to 1
                    year from the effective date
                document:
                  description: The document file to upload. Only applicable when using
                    multipart/form-data content type.
              description: Request body for creating compliance certificates. Accepts
                application/json (effectiveDate, expirationDate) or
                multipart/form-data (same fields plus a document file upload in
                the `document` field). All fields are optional.
          multipart/form-data: *a3
        description: Request body for creating compliance certificates. Accepts
          application/json (effectiveDate, expirationDate) or
          multipart/form-data (same fields plus a document file upload in the
          `document` field). All fields are optional.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the primary record whose request records will receive
            compliance certificates.
      responses:
        "201":
          description: Array of created compliance certificate documents, one per request
            record under the primary record. Each item contains only the
            document identifier.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    _id:
                      description: Unique identifier of the created compliance certificate document.
                      allOf:
                        - $ref: "#/components/schemas/objectId"
                  additionalProperties: false
                description: Array of created compliance certificate documents, one per request
                  record under the primary record. Each item contains only the
                  document identifier.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /primary-records/{id}/request-records/{recordId}/compliance-certificate:
    get:
      tags: &a4
        - Primary Records
      description: Get compliance certificate for a request record in a primary record
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id, name,
            processing, url, flag, organization, reviewedAt, archivedAt, notes,
            status, expirationDate, appliesToAllProjects, data, metadata,
            issueDate, createdAt, updatedAt"
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the document.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the document, derived from its source.
                  processing:
                    nullable: true
                    description: Whether the document is still being processed (data extraction,
                      classification). When `true`, extracted fields may not yet
                      be populated.
                    type: boolean
                  url:
                    description: Signed URL to download the document file. Time-limited; regenerated
                      on each read.
                    nullable: true
                    type: string
                  flag:
                    nullable: true
                    description: Compliance flag attached to the document, if any. `null` when the
                      document is not flagged.
                    type: object
                    properties:
                      addedOn:
                        nullable: true
                        description: Timestamp at which the flag was added to the document.
                        type: string
                        format: date-time
                      level:
                        nullable: true
                        description: Severity of the flag (e.g. warning, error).
                        type: string
                      subjects:
                        nullable: true
                        description: Compliance subjects affected by this flag.
                        type: array
                        items: {}
                      notes:
                        nullable: true
                        description: Free-form notes attached to the flag.
                        type: string
                    required:
                      - addedOn
                      - level
                      - subjects
                      - notes
                    additionalProperties: false
                  organization:
                    description: Identifier of the organization (workspace) that owns the document.
                      Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  reviewedAt:
                    nullable: true
                    description: Timestamp at which the document was reviewed, if it has been
                      reviewed.
                    type: string
                    format: date-time
                  archivedAt:
                    nullable: true
                    description: Timestamp at which the document was archived, if archived. `null`
                      for active documents.
                    type: string
                    format: date-time
                  notes:
                    nullable: true
                    description: Free-form notes attached to the document.
                    type: string
                  status:
                    nullable: true
                    description: Lifecycle status of the document.
                    type: string
                  expirationDate:
                    nullable: true
                    description: Expiration date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  appliesToAllProjects:
                    nullable: true
                    description: Whether the document applies to all projects (context records) for
                      the owning primary record, rather than a specific one.
                    type: boolean
                  data:
                    description: Structured data extracted from the document during processing.
                    nullable: true
                  metadata:
                    description: Document metadata (e.g. originating upload source, file
                      properties).
                    nullable: true
                  issueDate:
                    nullable: true
                    description: Issue date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  createdAt:
                    description: Timestamp at which the document was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the document was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags: *a4
      description: Create compliance certificate for a request record in a primary record
      requestBody:
        required: true
        content:
          application/json: &a5
            schema:
              type: object
              properties:
                effectiveDate:
                  description: ISO date-time for certificate effective date. It will default to
                    today if not provided
                expirationDate:
                  description: ISO date-time for certificate expiration date. It will default to
                    the earliest expiration date found in currently active
                    documents; if no document is present, it will default to 1
                    year from the effective date
                document:
                  description: The document to upload, only with multipart/form-data
          multipart/form-data: *a5
      parameters:
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: recordId
          required: true
      responses:
        "201":
          description: "{ _id } of compliance certificate document"
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                additionalProperties: false
                description: "{ _id } of compliance certificate document"
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records:
    get:
      tags:
        - Request Records
      description: >-
        List request records in the caller's organization. Results are always
        scoped to the caller's organization. Supports pagination via `limit` and
        `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `primaryRecordId` (objectId), `contextRecordId` (objectId), `name`
        (string), `status` (string), `complianceTracking` (string),
        `complianceStatus` (string), `complianceProfile.complianceProfileId`
        (objectId). See "List Endpoints" in the API overview for operator syntax
        and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: contextRecordId, name, status,
            complianceTracking, complianceStatus, createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: primaryRecordId
          required: false
          description: Filter by primaryRecordId. 24-character hex ObjectId string (e.g.
            `primaryRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: contextRecordId
          required: false
          description: Filter by contextRecordId. 24-character hex ObjectId string (e.g.
            `contextRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: status
          required: false
          description: Filter by status. String value (e.g. `status=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceTracking
          required: false
          description: Filter by complianceTracking. String value (e.g.
            `complianceTracking=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceStatus
          required: false
          description: Filter by complianceStatus. String value (e.g.
            `complianceStatus=example`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: complianceProfile.complianceProfileId
          required: false
          description: Filter by complianceProfile.complianceProfileId. 24-character hex
            ObjectId string (e.g.
            `complianceProfile.complianceProfileId=507f1f77bcf86cd799439011`).
            See "List Endpoints" in the API overview for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the request record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the request
                            record. Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        primaryRecordId:
                          description: Identifier of the primary record this request record is assigned
                            to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        contextRecordId:
                          description: Optional identifier of the context record (e.g. project) this
                            request record is scoped to.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          description: Human-readable name of the request record.
                          type: string
                          minLength: 1
                        status:
                          type: string
                          enum:
                            - active
                            - archived
                          description: Lifecycle status of the request record.
                        complianceTracking:
                          type: boolean
                          description: Whether compliance tracking is enabled for this request record.
                        complianceProfile:
                          description: Compliance profile currently assigned to this request record, if
                            any. Manage assignments via the
                            `/compliance-profile` sub-routes.
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                            name:
                              type: string
                          required:
                            - id
                          additionalProperties: false
                        complianceStatus:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this request record, derived from its
                            assigned compliance profile and submitted documents.
                        createdAt:
                          description: Timestamp at which the request record was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the request record was last updated.
                          type: string
                          format: date-time
                        complianceModules:
                          description: Compliance modules expanded by subject and requirement, populated
                            from the assigned compliance profile. Returned only
                            when explicitly requested via
                            `?fields=complianceModules`.
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance module (e.g.
                                  general-liability).
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the module.
                              status:
                                type: string
                                enum:
                                  - non_compliant
                                  - compliant
                                description: Computed compliance status of this module.
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this module.
                              documentsCount:
                                type: number
                                description: Number of documents associated with this module.
                              subjects:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    code:
                                      type: string
                                      minLength: 1
                                      description: Stable identifier of this compliance subject.
                                    label:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the subject (e.g. coverage limit, named
                                        insured).
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                      description: Compliance status of this subject, computed across its
                                        requirements.
                                    notes:
                                      description: Free-form notes attached to the subject (internal use).
                                      type: string
                                      minLength: 1
                                    effectiveDate:
                                      description: Earliest effective date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        begins. May be `AlwaysValid` for
                                        documents with always-valid coverage.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    expirationDate:
                                      description: Latest expiration date among documents in force for this subject —
                                        when the subject's continuous coverage
                                        ends. May be `AlwaysValid` or overridden
                                        by a cancellation.
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    cancellation:
                                      description: Active cancellation that overrides this subject's expiration, if
                                        any.
                                      type: object
                                      properties:
                                        effectiveDate:
                                          type: string
                                          format: date-time
                                      required:
                                        - effectiveDate
                                      additionalProperties: false
                                    latestExpirationDate:
                                      description: Furthest future expiration date across all matching documents (not
                                        only those in force).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidEffectiveDate:
                                      description: Most recent valid effective date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    latestValidExpirationDate:
                                      description: Most recent valid expiration date recorded for this subject
                                        (last-known value preserved across
                                        recomputes).
                                      anyOf:
                                        - type: string
                                          enum:
                                            - always_valid
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this subject.
                                    resetOn:
                                      description: Timestamp at which the subject's computed state was last reset.
                                      type: string
                                      format: date-time
                                    validityStatus:
                                      type: string
                                      enum:
                                        - active
                                        - expiring
                                        - cancelling
                                        - expired
                                        - cancelled
                                        - inactive
                                      description: Validity classification of the subject (e.g. valid, expiring,
                                        expired).
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this subject.
                                    requirements:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          complianceProfile:
                                            type: object
                                            properties:
                                              id:
                                                $ref: "#/components/schemas/objectId"
                                              name:
                                                type: string
                                            required:
                                              - id
                                            additionalProperties: false
                                            description: Compliance profile this requirement was sourced from at the time
                                              the request record was assigned.
                                          id:
                                            description: Stable identifier of this requirement instance.
                                            allOf:
                                              - $ref: "#/components/schemas/objectId"
                                          moduleCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent compliance module.
                                          moduleLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent compliance module.
                                          subjectCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the parent subject within the module.
                                          subjectLabel:
                                            type: string
                                            minLength: 1
                                            description: Human-readable label of the parent subject.
                                          attributeCode:
                                            type: string
                                            minLength: 1
                                            description: Identifier of the attribute (specific check) this requirement
                                              evaluates.
                                          attributeLabel:
                                            description: Human-readable label of the attribute.
                                            type: string
                                            minLength: 1
                                          attributeDescription:
                                            description: Free-form description of the attribute, surfaced on document
                                              request pages.
                                            type: string
                                            minLength: 1
                                          matchingCriteria:
                                            type: string
                                            enum:
                                              - matchOnce
                                              - matchRepeatedly
                                            description: How the requirement matches against documents (e.g. once, on every
                                              document).
                                          operator:
                                            type: string
                                            enum:
                                              - must be present
                                              - must be greater or equal to
                                              - must be less than or equal to
                                              - must be completed by
                                              - at least one must be present
                                                from list
                                            description: Comparison operator applied between extracted document values and
                                              `targetValue`.
                                          targetValue:
                                            description: Target value(s) the document data is compared against.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: array
                                                items:
                                                  type: string
                                                  minLength: 1
                                          value:
                                            description: Resolved value extracted from a matching document, when one is
                                              present.
                                            anyOf:
                                              - type: string
                                                minLength: 1
                                              - type: number
                                          status:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                              - waived
                                              - overridden
                                              - invalidated
                                            description: Computed compliance status of this requirement.
                                          invalidatedBy:
                                            description: Identifiers of conditions that invalidate this requirement, if any.
                                            type: array
                                            items:
                                              type: string
                                              minLength: 1
                                          notes:
                                            description: Internal-only notes (e.g. context for a waiver or override).
                                            type: string
                                            minLength: 1
                                          publicNotes:
                                            description: Public-facing notes shown alongside the requirement on the document
                                              request page.
                                            type: object
                                            properties:
                                              content:
                                                type: string
                                                minLength: 1
                                              removeWhenCompliant:
                                                type: boolean
                                            required:
                                              - content
                                              - removeWhenCompliant
                                            additionalProperties: false
                                          documents:
                                            type: array
                                            items:
                                              type: object
                                              properties:
                                                id:
                                                  $ref: "#/components/schemas/objectId"
                                                origin:
                                                  type: string
                                                  enum:
                                                    - upload
                                                    - makeCompliant
                                                    - connectedDocument
                                                    - fillableForm
                                                    - webForm
                                                complianceStatus:
                                                  type: string
                                                  enum:
                                                    - compliant
                                                    - non_compliant
                                              required:
                                                - id
                                                - complianceStatus
                                              additionalProperties: false
                                            description: Documents associated with this requirement, with each document's
                                              contribution to compliance.
                                          documentsCount:
                                            type: number
                                            description: Number of documents associated with this requirement.
                                          resetOn:
                                            description: Timestamp at which the requirement's computed state was last reset.
                                            type: string
                                            format: date-time
                                          custom:
                                            type: boolean
                                            description: Whether this requirement was added as a workspace customization
                                              rather than from a built-in
                                              module.
                                        required:
                                          - complianceProfile
                                          - moduleCode
                                          - moduleLabel
                                          - subjectCode
                                          - subjectLabel
                                          - attributeCode
                                          - matchingCriteria
                                          - operator
                                          - status
                                          - documents
                                          - documentsCount
                                          - custom
                                        additionalProperties: false
                                        description: A single compliance requirement under a subject of a compliance
                                          module.
                                      description: Requirements grouped under this subject.
                                  required:
                                    - code
                                    - label
                                    - status
                                    - documentsCount
                                    - validityStatus
                                    - documents
                                    - requirements
                                  additionalProperties: false
                                  description: A subject within a compliance module (e.g. coverage limit, named
                                    insured).
                                description: Subjects expanded under this compliance module.
                            required:
                              - code
                              - label
                              - status
                              - documents
                              - documentsCount
                              - subjects
                            additionalProperties: false
                            description: A single compliance module from the assigned profile, expanded with
                              its subjects and requirements.
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    post:
      tags:
        - Request Records
      description: Create a new request record and assign it to a primary record. The
        owning organization is derived from the access token. Optionally pass a
        `complianceProfileId` to attach a compliance profile at creation time.
        Returns the new record's identifier.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryRecordId:
                  description: Identifier of the primary record (entity, e.g. a vendor) this
                    request record is assigned to.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                contextRecordId:
                  description: Optional identifier of the context record (e.g. project) this
                    request record is scoped to.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                name:
                  description: Human-readable name of the request record.
                  type: string
                  minLength: 1
                status:
                  default: active
                  description: Lifecycle status of the request record. Defaults to `active` on
                    creation.
                  type: string
                  enum:
                    - active
                    - archived
                complianceTracking:
                  default: true
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
                  type: boolean
                complianceProfileId:
                  description: Optional identifier of the compliance profile to assign to this
                    request record at creation. Use the `/compliance-profile`
                    sub-routes to manage the assignment after the fact.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
              required:
                - primaryRecordId
              description: Request body for creating a request record. The owning organization
                is always derived from the caller's token and cannot be set
                here.
        description: Request body for creating a request record. The owning organization
          is always derived from the caller's token and cannot be set here.
      responses:
        "201":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}:
    get:
      tags:
        - Request Records
      description: Read a single request record by id, scoped to the caller's
        organization. Use the `fields` query parameter to limit the response to
        a projection of the record. Use `?fields=complianceModules` to include
        the expanded compliance modules from the assigned profile.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, primaryRecordId, contextRecordId, name, status,
            complianceTracking, complianceProfile, complianceStatus, createdAt,
            updatedAt, complianceModules"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    type: string
                    enum:
                      - active
                      - archived
                    description: Lifecycle status of the request record.
                  complianceTracking:
                    type: boolean
                    description: Whether compliance tracking is enabled for this request record.
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                          description: Documents associated with this module.
                        documentsCount:
                          type: number
                          description: Number of documents associated with this module.
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                                description: Compliance status of this subject, computed across its
                                  requirements.
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                type: number
                                description: Number of documents associated with this subject.
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                                description: Documents associated with this subject.
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                      description: Computed compliance status of this requirement.
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                    documentsCount:
                                      type: number
                                      description: Number of documents associated with this requirement.
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    patch:
      tags:
        - Request Records
      description: Partially update a request record. Any subset of writable fields
        may be provided; omitted fields are left unchanged. Status changes are
        accepted on this endpoint and are applied via the dedicated status
        workflow. Returns the full updated record.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  description: Human-readable name of the request record.
                  type: string
                  minLength: 1
                status:
                  type: string
                  enum:
                    - active
                    - archived
                  description: Lifecycle status of the request record. Defaults to `active` on
                    creation.
                complianceTracking:
                  type: boolean
                  description: Whether compliance tracking is enabled for this request record.
                    Defaults to `true`.
              description: Request body for partially updating a request record. Any subset of
                the writable fields (name, status, complianceTracking) may be
                provided. Re-parenting (primaryRecordId / contextRecordId) and
                compliance-profile assignment are handled via separate
                endpoints.
        description: Request body for partially updating a request record. Any subset of
          the writable fields (name, status, complianceTracking) may be
          provided. Re-parenting (primaryRecordId / contextRecordId) and
          compliance-profile assignment are handled via separate endpoints.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record to update.
      responses:
        "200":
          description: A request record — the compliance assignment between a primary
            record (entity) and an optional context record (e.g. project), with
            its own compliance profile and computed status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the request record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the request
                      record. Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record this request record is assigned
                      to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  contextRecordId:
                    description: Optional identifier of the context record (e.g. project) this
                      request record is scoped to.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    description: Human-readable name of the request record.
                    type: string
                    minLength: 1
                  status:
                    default: active
                    description: Lifecycle status of the request record.
                    type: string
                    enum:
                      - active
                      - archived
                  complianceTracking:
                    default: true
                    description: Whether compliance tracking is enabled for this request record.
                    type: boolean
                  complianceProfile:
                    description: Compliance profile currently assigned to this request record, if
                      any. Manage assignments via the `/compliance-profile`
                      sub-routes.
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                      name:
                        type: string
                    required:
                      - id
                    additionalProperties: false
                  complianceStatus:
                    type: string
                    enum:
                      - non_compliant
                      - compliant
                    description: Computed compliance status of this request record, derived from its
                      assigned compliance profile and submitted documents.
                  createdAt:
                    default: 2026-07-21T13:34:49.201Z
                    description: Timestamp at which the request record was created.
                    type: string
                    format: date-time
                  updatedAt:
                    default: 2026-07-21T13:34:49.201Z
                    description: Timestamp at which the request record was last updated.
                    type: string
                    format: date-time
                  complianceModules:
                    description: Compliance modules expanded by subject and requirement, populated
                      from the assigned compliance profile. Returned only when
                      explicitly requested via `?fields=complianceModules`.
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable identifier of this compliance module (e.g.
                            general-liability).
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label of the module.
                        status:
                          type: string
                          enum:
                            - non_compliant
                            - compliant
                          description: Computed compliance status of this module.
                        documents:
                          default: []
                          description: Documents associated with this module.
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                $ref: "#/components/schemas/objectId"
                              origin:
                                type: string
                                enum:
                                  - upload
                                  - makeCompliant
                                  - connectedDocument
                                  - fillableForm
                                  - webForm
                              complianceStatus:
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                            required:
                              - id
                              - complianceStatus
                            additionalProperties: false
                        documentsCount:
                          default: 0
                          description: Number of documents associated with this module.
                          type: number
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable identifier of this compliance subject.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label of the subject (e.g. coverage limit, named
                                  insured).
                              status:
                                default: non_compliant
                                description: Compliance status of this subject, computed across its
                                  requirements.
                                type: string
                                enum:
                                  - compliant
                                  - non_compliant
                                  - waived
                                  - overridden
                              notes:
                                description: Free-form notes attached to the subject (internal use).
                                type: string
                                minLength: 1
                              effectiveDate:
                                description: Earliest effective date among documents in force for this subject —
                                  when the subject's continuous coverage begins.
                                  May be `AlwaysValid` for documents with
                                  always-valid coverage.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              expirationDate:
                                description: Latest expiration date among documents in force for this subject —
                                  when the subject's continuous coverage ends.
                                  May be `AlwaysValid` or overridden by a
                                  cancellation.
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              cancellation:
                                description: Active cancellation that overrides this subject's expiration, if
                                  any.
                                type: object
                                properties:
                                  effectiveDate:
                                    type: string
                                    format: date-time
                                required:
                                  - effectiveDate
                                additionalProperties: false
                              latestExpirationDate:
                                description: Furthest future expiration date across all matching documents (not
                                  only those in force).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidEffectiveDate:
                                description: Most recent valid effective date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              latestValidExpirationDate:
                                description: Most recent valid expiration date recorded for this subject
                                  (last-known value preserved across
                                  recomputes).
                                anyOf:
                                  - type: string
                                    enum:
                                      - always_valid
                              documentsCount:
                                default: 0
                                description: Number of documents associated with this subject.
                                type: number
                              resetOn:
                                description: Timestamp at which the subject's computed state was last reset.
                                type: string
                                format: date-time
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Validity classification of the subject (e.g. valid, expiring,
                                  expired).
                              documents:
                                default: []
                                description: Documents associated with this subject.
                                type: array
                                items:
                                  type: object
                                  properties:
                                    id:
                                      $ref: "#/components/schemas/objectId"
                                    origin:
                                      type: string
                                      enum:
                                        - upload
                                        - makeCompliant
                                        - connectedDocument
                                        - fillableForm
                                        - webForm
                                    complianceStatus:
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                  required:
                                    - id
                                    - complianceStatus
                                  additionalProperties: false
                              requirements:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    complianceProfile:
                                      type: object
                                      properties:
                                        id:
                                          $ref: "#/components/schemas/objectId"
                                        name:
                                          type: string
                                      required:
                                        - id
                                      additionalProperties: false
                                      description: Compliance profile this requirement was sourced from at the time
                                        the request record was assigned.
                                    id:
                                      description: Stable identifier of this requirement instance.
                                      allOf:
                                        - $ref: "#/components/schemas/objectId"
                                    moduleCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent compliance module.
                                    moduleLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent compliance module.
                                    subjectCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the parent subject within the module.
                                    subjectLabel:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label of the parent subject.
                                    attributeCode:
                                      type: string
                                      minLength: 1
                                      description: Identifier of the attribute (specific check) this requirement
                                        evaluates.
                                    attributeLabel:
                                      description: Human-readable label of the attribute.
                                      type: string
                                      minLength: 1
                                    attributeDescription:
                                      description: Free-form description of the attribute, surfaced on document
                                        request pages.
                                      type: string
                                      minLength: 1
                                    matchingCriteria:
                                      default: matchOnce
                                      description: How the requirement matches against documents (e.g. once, on every
                                        document).
                                      type: string
                                      enum:
                                        - matchOnce
                                        - matchRepeatedly
                                    operator:
                                      type: string
                                      enum:
                                        - must be present
                                        - must be greater or equal to
                                        - must be less than or equal to
                                        - must be completed by
                                        - at least one must be present from list
                                      description: Comparison operator applied between extracted document values and
                                        `targetValue`.
                                    targetValue:
                                      description: Target value(s) the document data is compared against.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: array
                                          items:
                                            type: string
                                            minLength: 1
                                    value:
                                      description: Resolved value extracted from a matching document, when one is
                                        present.
                                      anyOf:
                                        - type: string
                                          minLength: 1
                                        - type: number
                                    status:
                                      default: non_compliant
                                      description: Computed compliance status of this requirement.
                                      type: string
                                      enum:
                                        - compliant
                                        - non_compliant
                                        - waived
                                        - overridden
                                        - invalidated
                                    invalidatedBy:
                                      description: Identifiers of conditions that invalidate this requirement, if any.
                                      type: array
                                      items:
                                        type: string
                                        minLength: 1
                                    notes:
                                      description: Internal-only notes (e.g. context for a waiver or override).
                                      type: string
                                      minLength: 1
                                    publicNotes:
                                      description: Public-facing notes shown alongside the requirement on the document
                                        request page.
                                      type: object
                                      properties:
                                        content:
                                          type: string
                                          minLength: 1
                                        removeWhenCompliant:
                                          default: false
                                          type: boolean
                                      required:
                                        - content
                                        - removeWhenCompliant
                                      additionalProperties: false
                                    documents:
                                      default: []
                                      description: Documents associated with this requirement, with each document's
                                        contribution to compliance.
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          id:
                                            $ref: "#/components/schemas/objectId"
                                          origin:
                                            type: string
                                            enum:
                                              - upload
                                              - makeCompliant
                                              - connectedDocument
                                              - fillableForm
                                              - webForm
                                          complianceStatus:
                                            type: string
                                            enum:
                                              - compliant
                                              - non_compliant
                                        required:
                                          - id
                                          - complianceStatus
                                        additionalProperties: false
                                    documentsCount:
                                      default: 0
                                      description: Number of documents associated with this requirement.
                                      type: number
                                    resetOn:
                                      description: Timestamp at which the requirement's computed state was last reset.
                                      type: string
                                      format: date-time
                                    custom:
                                      type: boolean
                                      description: Whether this requirement was added as a workspace customization
                                        rather than from a built-in module.
                                  required:
                                    - complianceProfile
                                    - moduleCode
                                    - moduleLabel
                                    - subjectCode
                                    - subjectLabel
                                    - attributeCode
                                    - matchingCriteria
                                    - operator
                                    - status
                                    - documents
                                    - documentsCount
                                    - custom
                                  additionalProperties: false
                                  description: A single compliance requirement under a subject of a compliance
                                    module.
                                description: Requirements grouped under this subject.
                            required:
                              - code
                              - label
                              - status
                              - documentsCount
                              - validityStatus
                              - documents
                              - requirements
                            additionalProperties: false
                            description: A subject within a compliance module (e.g. coverage limit, named
                              insured).
                          description: Subjects expanded under this compliance module.
                      required:
                        - code
                        - label
                        - status
                        - documents
                        - documentsCount
                        - subjects
                      additionalProperties: false
                      description: A single compliance module from the assigned profile, expanded with
                        its subjects and requirements.
                required:
                  - _id
                  - organization
                  - primaryRecordId
                  - status
                  - complianceTracking
                  - complianceStatus
                  - createdAt
                  - updatedAt
                additionalProperties: false
                description: A request record — the compliance assignment between a primary
                  record (entity) and an optional context record (e.g. project),
                  with its own compliance profile and computed status.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Request Records
      description: Delete a request record. Returns the deleted record id on success.
        Deleting a record that does not exist returns 404 with the shared
        NotFoundError shape.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record to delete.
      responses:
        "200":
          description: Identifier-only response. Returned when an endpoint mutates a
            request record but does not need to return the full document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
                description: Identifier-only response. Returned when an endpoint mutates a
                  request record but does not need to return the full document.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}/attributes:
    get:
      tags:
        - Request Records
      description: List attributes (custom field values) embedded in a request record.
        Attributes are stored as an embedded array on the record — there is no
        separate filtering beyond pagination. Results are scoped to the caller's
        organization.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record whose attributes to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          description: Identifier of the custom field definition this attribute value is
                            associated with.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        optionIds:
                          description: Selected option identifiers for dropdown / multi-select custom
                            field types.
                          minItems: 1
                          type: array
                          items:
                            $ref: "#/components/schemas/objectId"
                        optionId:
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        value:
                          description: Scalar value for text or numeric custom field types.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: number
                      required:
                        - id
                      additionalProperties: false
                      description: A single attribute (custom field value) embedded in a request
                        record.
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}/attributes/{attributeId}:
    get:
      tags:
        - Request Records
      description: Read a single attribute (custom field value) embedded in a request
        record. Returns 404 when the request record or the specific attribute
        does not exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: A single attribute (custom field value) embedded in a request record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the custom field definition this attribute value is
                      associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  optionIds:
                    description: Selected option identifiers for dropdown / multi-select custom
                      field types.
                    minItems: 1
                    type: array
                    items:
                      $ref: "#/components/schemas/objectId"
                  optionId:
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  value:
                    description: Scalar value for text or numeric custom field types.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: number
                required:
                  - id
                additionalProperties: false
                description: A single attribute (custom field value) embedded in a request
                  record.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    put:
      tags:
        - Request Records
      description: Create or replace an attribute (custom field value) on a request
        record. This is a full replacement — omitting `value` or `optionIds`
        clears that field. Returns 404 when the request record or the referenced
        custom field definition does not exist.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                optionIds:
                  description: Selected option identifiers for dropdown / multi-select custom
                    field types.
                  minItems: 1
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                optionId:
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                value:
                  description: Scalar value for text or numeric custom field types.
                  anyOf:
                    - type: string
                      minLength: 1
                    - type: number
              description: Request body for creating or replacing an attribute value on a
                request record. Provide `value` for text/numeric fields or
                `optionIds` for dropdown fields.
        description: Request body for creating or replacing an attribute value on a
          request record. Provide `value` for text/numeric fields or `optionIds`
          for dropdown fields.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Request Records
      description: Delete an attribute (custom field value) from a request record.
        Returns 404 when the request record or the specific attribute does not
        exist.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: attributeId
          required: true
          description: Identifier of the attribute (custom field) to target.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    description: Identifier of the attribute (custom field definition) that was
                      affected.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}/compliance-profile:
    post:
      tags:
        - Request Records
      description: Assign a compliance profile to a request record, scoped to the
        caller's organization. Assigning a profile attaches its rules and
        document checklists to the request record and starts compliance
        evaluation against it. If the request record has a context record and
        the supplied profile is `Global`, a `Project`-scoped compliance profile
        is created or reused automatically.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                complianceProfileId:
                  description: Identifier of the compliance profile to assign to the request
                    record. If the request record is associated with a context
                    record and the supplied profile is `Global`, a corresponding
                    `Project`-scoped compliance profile will be created or
                    reused automatically.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
              required:
                - complianceProfileId
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record to assign the compliance profile to.
      responses:
        "204":
          description: Compliance profile assigned successfully.
          content:
            application/json:
              schema:
                type: string
                nullable: true
                enum:
                  - null
                description: Compliance profile assigned successfully.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    delete:
      tags:
        - Request Records
      description: "Unassign the compliance profile from a request record, scoped to
        the caller's organization. Removes the previously assigned profile and
        stops compliance tracking for the request record. The operation is
        idempotent: calling it on a request record that has no compliance
        profile assigned returns 204 without error."
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record to unassign the compliance profile
            from.
      responses:
        "204":
          description: Compliance profile unassigned successfully.
          content:
            application/json:
              schema:
                type: string
                nullable: true
                enum:
                  - null
                description: Compliance profile unassigned successfully.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}/compliance-certificate:
    post:
      tags:
        - Request Records
      description: Create a compliance certificate for a request record. Accepts
        application/json with optional effectiveDate and expirationDate fields,
        or multipart/form-data with the same date fields plus a document file in
        the `document` field. Returns the identifier of the created compliance
        certificate document.
      requestBody:
        required: true
        content:
          application/json: &a6
            schema:
              type: object
              properties:
                effectiveDate:
                  description: ISO date-time for certificate effective date. It will default to
                    today if not provided
                expirationDate:
                  description: ISO date-time for certificate expiration date. It will default to
                    the earliest expiration date found in currently active
                    documents; if no document is present, it will default to 1
                    year from the effective date
                document:
                  description: The document to upload, only with multipart/form-data
          multipart/form-data: *a6
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record that will receive the compliance
            certificate.
      responses:
        "201":
          description: "{ _id } of compliance certificate document"
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                additionalProperties: false
                description: "{ _id } of compliance certificate document"
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Request Records
      description: Get the latest non-archived compliance certificate document for a
        request record. Returns the document with a signed URL when present;
        returns 404 when no compliance certificate has been generated for the
        request record.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id, name,
            processing, url, flag, organization, reviewedAt, archivedAt, notes,
            status, expirationDate, appliesToAllProjects, data, metadata,
            issueDate, createdAt, updatedAt"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the request record whose latest compliance
            certificate document will be returned.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the document.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the document, derived from its source.
                  processing:
                    nullable: true
                    description: Whether the document is still being processed (data extraction,
                      classification). When `true`, extracted fields may not yet
                      be populated.
                    type: boolean
                  url:
                    description: Signed URL to download the document file. Time-limited; regenerated
                      on each read.
                    nullable: true
                    type: string
                  flag:
                    nullable: true
                    description: Compliance flag attached to the document, if any. `null` when the
                      document is not flagged.
                    type: object
                    properties:
                      addedOn:
                        nullable: true
                        description: Timestamp at which the flag was added to the document.
                        type: string
                        format: date-time
                      level:
                        nullable: true
                        description: Severity of the flag (e.g. warning, error).
                        type: string
                      subjects:
                        nullable: true
                        description: Compliance subjects affected by this flag.
                        type: array
                        items: {}
                      notes:
                        nullable: true
                        description: Free-form notes attached to the flag.
                        type: string
                    required:
                      - addedOn
                      - level
                      - subjects
                      - notes
                    additionalProperties: false
                  organization:
                    description: Identifier of the organization (workspace) that owns the document.
                      Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  reviewedAt:
                    nullable: true
                    description: Timestamp at which the document was reviewed, if it has been
                      reviewed.
                    type: string
                    format: date-time
                  archivedAt:
                    nullable: true
                    description: Timestamp at which the document was archived, if archived. `null`
                      for active documents.
                    type: string
                    format: date-time
                  notes:
                    nullable: true
                    description: Free-form notes attached to the document.
                    type: string
                  status:
                    nullable: true
                    description: Lifecycle status of the document.
                    type: string
                  expirationDate:
                    nullable: true
                    description: Expiration date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  appliesToAllProjects:
                    nullable: true
                    description: Whether the document applies to all projects (context records) for
                      the owning primary record, rather than a specific one.
                    type: boolean
                  data:
                    description: Structured data extracted from the document during processing.
                    nullable: true
                  metadata:
                    description: Document metadata (e.g. originating upload source, file
                      properties).
                    nullable: true
                  issueDate:
                    nullable: true
                    description: Issue date extracted from the document, if applicable.
                    type: string
                    format: date-time
                  createdAt:
                    description: Timestamp at which the document was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the document was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /request-records/{id}/requirements:
    post:
      tags:
        - Request Records
      description: Add requirements to the request record's compliance profile. Each
        item is either a library reference (`libraryRequirementId` or
        `attributeCode`) or a custom definition (module/subject by code or
        label, attribute by label). Idempotent on the (moduleLabel,
        subjectLabel, attributeLabel) tuple.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                requirements:
                  minItems: 1
                  maxItems: 200
                  type: array
                  items:
                    anyOf:
                      - type: object
                        properties:
                          libraryRequirementId:
                            description: Identifier of the requirement entry in the workspace's requirements
                              library to clone onto this request record.
                            allOf:
                              - $ref: "#/components/schemas/objectIdInput"
                        required:
                          - libraryRequirementId
                        additionalProperties: false
                        description: Reference an existing library requirement by its identifier. The
                          persisted requirement is a verbatim copy
                          (matchingCriteria, operator, targetValue,
                          attributeType, etc.) of the library entry.
                      - type: object
                        properties:
                          attributeCode:
                            type: string
                            minLength: 1
                            description: Stable code of the library requirement to clone (alternative to
                              `libraryRequirementId`). Must match an entry in
                              the workspace requirements library.
                          matchingCriteria:
                            type: string
                            enum:
                              - matchOnce
                              - matchRepeatedly
                            description: How the requirement matches against documents (e.g. `MatchOnce`,
                              `MatchRepeatedly`). Overrides the library entry
                              value.
                          operator:
                            type: string
                            enum:
                              - must be present
                              - must be greater or equal to
                              - must be less than or equal to
                              - must be completed by
                              - at least one must be present from list
                            description: Comparison operator applied between the document data and
                              `targetValue`. Must be valid for the library
                              entry's `attributeType`.
                          targetValue:
                            description: Value(s) the document data is compared against. String for scalar
                              attribute types; string array for `list` attribute
                              types. Optional for operators that do not require
                              a target value (e.g. `MustBePresent`).
                            anyOf:
                              - type: string
                                minLength: 1
                              - type: array
                                items:
                                  type: string
                                  minLength: 1
                        required:
                          - attributeCode
                          - matchingCriteria
                          - operator
                        additionalProperties: false
                        description: Reference an existing library requirement by its `attributeCode`.
                          The persisted requirement uses the library entry for
                          module/subject/attribute identity but takes
                          `matchingCriteria`, `operator`, and `targetValue` from
                          the input — supports requirement-template entries that
                          may not carry these fields.
                      - oneOf:
                          - allOf:
                              - type: object
                                properties:
                                  subjectCode:
                                    type: string
                                    minLength: 1
                                    description: Existing subject code on the compliance profile. When provided, the
                                      subject (and its parent module) are
                                      reused. Mutually exclusive with
                                      `subjectLabel`.
                                  moduleCode:
                                    description: Existing module code on the compliance profile. When provided, the
                                      requirement is anchored to that module.
                                      Mutually exclusive with `moduleLabel`.
                                    type: string
                                    minLength: 1
                                  subjectLabel:
                                    not: {}
                                  moduleLabel:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectCode
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                          - allOf:
                              - type: object
                                properties:
                                  subjectLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new subject. The subject code is derived
                                      from this label. Requires `moduleCode` or
                                      `moduleLabel` so the new subject can be
                                      attached to a module. Mutually exclusive
                                      with `subjectCode`.
                                  moduleCode:
                                    type: string
                                    minLength: 1
                                    description: Existing module code on the compliance profile. When provided, the
                                      requirement is anchored to that module.
                                      Mutually exclusive with `moduleLabel`.
                                  subjectCode:
                                    not: {}
                                  moduleLabel:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectLabel
                                  - moduleCode
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                          - allOf:
                              - type: object
                                properties:
                                  subjectLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new subject. The subject code is derived
                                      from this label. Requires `moduleCode` or
                                      `moduleLabel` so the new subject can be
                                      attached to a module. Mutually exclusive
                                      with `subjectCode`.
                                  moduleLabel:
                                    type: string
                                    minLength: 1
                                    description: Human-readable label for a new compliance module. The module code
                                      is derived from this label. Mutually
                                      exclusive with `moduleCode`.
                                  subjectCode:
                                    not: {}
                                  moduleCode:
                                    not: {}
                                  matchingCriteria:
                                    type: string
                                    enum:
                                      - matchOnce
                                      - matchRepeatedly
                                    description: How the requirement matches against documents (e.g. `MatchOnce`,
                                      `MatchRepeatedly`).
                                  attributeCode:
                                    not: {}
                                required:
                                  - subjectLabel
                                  - moduleLabel
                                  - matchingCriteria
                              - type: object
                                properties:
                                  attributeCode:
                                    type: string
                                    minLength: 1
                                  attributeLabel:
                                    type: string
                                    minLength: 1
                                  attributeDescription:
                                    type: string
                                    minLength: 1
                                required:
                                  - attributeLabel
                              - oneOf:
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - number
                                          - amBestRating
                                          - amBestFinancialStrength
                                      operator:
                                        type: string
                                        enum:
                                          - must be greater or equal to
                                          - must be less than or equal to
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - boolean
                                          - connected
                                          - ai
                                      operator:
                                        type: string
                                        enum:
                                          - must be present
                                      targetValue:
                                        not: {}
                                    required:
                                      - attributeType
                                      - operator
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - fillableForm
                                      operator:
                                        type: string
                                        enum:
                                          - must be completed by
                                      targetValue:
                                        type: string
                                        minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                                  - type: object
                                    properties:
                                      attributeType:
                                        type: string
                                        enum:
                                          - list
                                      operator:
                                        type: string
                                        enum:
                                          - at least one must be present from
                                            list
                                      targetValue:
                                        minItems: 1
                                        type: array
                                        items:
                                          type: string
                                          minLength: 1
                                    required:
                                      - attributeType
                                      - operator
                                      - targetValue
                        description: Define a brand-new requirement inline. The module/subject can
                          target an existing slot on the compliance profile
                          (`moduleCode` / `subjectCode`) or create a new one
                          (`moduleLabel` / `subjectLabel`). The attribute side
                          (`attributeType`, `attributeLabel`, `operator`,
                          `targetValue`, …) follows the shared
                          RequirementAttribute discriminated union — invalid
                          `(attributeType, operator, targetValue)` combinations
                          are rejected.
                    description: "A single requirement to add to the request record. One of: library
                      reference by id, library reference by attributeCode (with
                      overrides), or a custom definition."
                  description: Requirements to add. Items are processed in order; library lookups
                    happen at the workspace scope. Duplicates against existing
                    rules or earlier items in the same batch are silently
                    dropped (idempotent on the (moduleLabel, subjectLabel,
                    attributeLabel) tuple).
              required:
                - requirements
              description: Request body for adding requirements to a request record's
                compliance profile in a single batch.
        description: Request body for adding requirements to a request record's
          compliance profile in a single batch.
      parameters:
        - schema:
            $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
      responses:
        "200":
          description: Requirements that were newly added by this call. Items deduplicated
            against existing profile rules and within the input batch are not
            included; the array is empty when every input was a duplicate.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    _id:
                      description: Unique identifier of the persisted requirement rule.
                      allOf:
                        - $ref: "#/components/schemas/objectId"
                    moduleCode:
                      type: string
                      minLength: 1
                      description: Identifier of the parent compliance module the requirement is
                        attached to.
                    moduleLabel:
                      type: string
                      minLength: 1
                      description: Human-readable label of the parent compliance module.
                    subjectCode:
                      type: string
                      minLength: 1
                      description: Identifier of the parent subject within the module.
                    subjectLabel:
                      type: string
                      minLength: 1
                      description: Human-readable label of the parent subject.
                    attributeCode:
                      type: string
                      minLength: 1
                      description: Identifier of the attribute (specific check) this requirement
                        evaluates.
                    attributeType:
                      type: string
                      enum:
                        - boolean
                        - number
                        - fillableForm
                        - amBestRating
                        - amBestFinancialStrength
                        - connected
                        - list
                        - ai
                      description: Kind of attribute being evaluated (e.g. `number`, `boolean`,
                        `list`). Determines which operators and targetValue
                        shapes are valid.
                    attributeLabel:
                      type: string
                      minLength: 1
                      description: Human-readable label of the attribute.
                    attributeDescription:
                      description: Free-form description of the attribute, surfaced on document
                        request pages.
                      type: string
                      minLength: 1
                    matchingCriteria:
                      type: string
                      enum:
                        - matchOnce
                        - matchRepeatedly
                      description: How the requirement matches against documents (e.g. `MatchOnce`,
                        `MatchRepeatedly`).
                    operator:
                      type: string
                      enum:
                        - must be present
                        - must be greater or equal to
                        - must be less than or equal to
                        - must be completed by
                        - at least one must be present from list
                      description: Comparison operator applied between extracted document values and
                        `targetValue`.
                    targetValue:
                      description: Target value(s) the document data is compared against. Empty for
                        operators that do not require one (e.g.
                        `MustBePresent`).
                      anyOf:
                        - type: string
                          minLength: 1
                        - type: array
                          items:
                            type: string
                            minLength: 1
                  required:
                    - moduleCode
                    - moduleLabel
                    - subjectCode
                    - subjectLabel
                    - attributeCode
                    - attributeType
                    - attributeLabel
                    - matchingCriteria
                    - operator
                  additionalProperties: false
                  description: A requirement rule freshly persisted on the request record's
                    compliance profile.
                description: Requirements that were newly added by this call. Items deduplicated
                  against existing profile rules and within the input batch are
                  not included; the array is empty when every input was a
                  duplicate.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /requirements:
    post:
      tags:
        - Requirements
      description: Create a library requirement in the caller's organization (derived
        from the access token). Define the module/subject by label to create new
        ones, or by code/label to reuse an existing template, plus the attribute
        definition. Returns the persisted requirement.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - allOf:
                    - type: object
                      properties:
                        subjectCode:
                          type: string
                          minLength: 1
                          description: Existing subject code on the compliance profile. When provided, the
                            subject (and its parent module) are reused. Mutually
                            exclusive with `subjectLabel`.
                        moduleCode:
                          description: Existing module code on the compliance profile. When provided, the
                            requirement is anchored to that module. Mutually
                            exclusive with `moduleLabel`.
                          type: string
                          minLength: 1
                        subjectLabel:
                          not: {}
                        moduleLabel:
                          not: {}
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: How the requirement matches against documents (e.g. `MatchOnce`,
                            `MatchRepeatedly`).
                        attributeCode:
                          not: {}
                      required:
                        - subjectCode
                        - matchingCriteria
                    - type: object
                      properties:
                        attributeCode:
                          type: string
                          minLength: 1
                        attributeLabel:
                          type: string
                          minLength: 1
                        attributeDescription:
                          type: string
                          minLength: 1
                      required:
                        - attributeLabel
                    - oneOf:
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - number
                                - amBestRating
                                - amBestFinancialStrength
                            operator:
                              type: string
                              enum:
                                - must be greater or equal to
                                - must be less than or equal to
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - boolean
                                - connected
                                - ai
                            operator:
                              type: string
                              enum:
                                - must be present
                            targetValue:
                              not: {}
                          required:
                            - attributeType
                            - operator
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - fillableForm
                            operator:
                              type: string
                              enum:
                                - must be completed by
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - list
                            operator:
                              type: string
                              enum:
                                - at least one must be present from list
                            targetValue:
                              minItems: 1
                              type: array
                              items:
                                type: string
                                minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                - allOf:
                    - type: object
                      properties:
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Human-readable label for a new subject. The subject code is derived
                            from this label. Requires `moduleCode` or
                            `moduleLabel` so the new subject can be attached to
                            a module. Mutually exclusive with `subjectCode`.
                        moduleCode:
                          type: string
                          minLength: 1
                          description: Existing module code on the compliance profile. When provided, the
                            requirement is anchored to that module. Mutually
                            exclusive with `moduleLabel`.
                        subjectCode:
                          not: {}
                        moduleLabel:
                          not: {}
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: How the requirement matches against documents (e.g. `MatchOnce`,
                            `MatchRepeatedly`).
                        attributeCode:
                          not: {}
                      required:
                        - subjectLabel
                        - moduleCode
                        - matchingCriteria
                    - type: object
                      properties:
                        attributeCode:
                          type: string
                          minLength: 1
                        attributeLabel:
                          type: string
                          minLength: 1
                        attributeDescription:
                          type: string
                          minLength: 1
                      required:
                        - attributeLabel
                    - oneOf:
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - number
                                - amBestRating
                                - amBestFinancialStrength
                            operator:
                              type: string
                              enum:
                                - must be greater or equal to
                                - must be less than or equal to
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - boolean
                                - connected
                                - ai
                            operator:
                              type: string
                              enum:
                                - must be present
                            targetValue:
                              not: {}
                          required:
                            - attributeType
                            - operator
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - fillableForm
                            operator:
                              type: string
                              enum:
                                - must be completed by
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - list
                            operator:
                              type: string
                              enum:
                                - at least one must be present from list
                            targetValue:
                              minItems: 1
                              type: array
                              items:
                                type: string
                                minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                - allOf:
                    - type: object
                      properties:
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Human-readable label for a new subject. The subject code is derived
                            from this label. Requires `moduleCode` or
                            `moduleLabel` so the new subject can be attached to
                            a module. Mutually exclusive with `subjectCode`.
                        moduleLabel:
                          type: string
                          minLength: 1
                          description: Human-readable label for a new compliance module. The module code
                            is derived from this label. Mutually exclusive with
                            `moduleCode`.
                        subjectCode:
                          not: {}
                        moduleCode:
                          not: {}
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: How the requirement matches against documents (e.g. `MatchOnce`,
                            `MatchRepeatedly`).
                        attributeCode:
                          not: {}
                      required:
                        - subjectLabel
                        - moduleLabel
                        - matchingCriteria
                    - type: object
                      properties:
                        attributeCode:
                          type: string
                          minLength: 1
                        attributeLabel:
                          type: string
                          minLength: 1
                        attributeDescription:
                          type: string
                          minLength: 1
                      required:
                        - attributeLabel
                    - oneOf:
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - number
                                - amBestRating
                                - amBestFinancialStrength
                            operator:
                              type: string
                              enum:
                                - must be greater or equal to
                                - must be less than or equal to
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - boolean
                                - connected
                                - ai
                            operator:
                              type: string
                              enum:
                                - must be present
                            targetValue:
                              not: {}
                          required:
                            - attributeType
                            - operator
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - fillableForm
                            operator:
                              type: string
                              enum:
                                - must be completed by
                            targetValue:
                              type: string
                              minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
                        - type: object
                          properties:
                            attributeType:
                              type: string
                              enum:
                                - list
                            operator:
                              type: string
                              enum:
                                - at least one must be present from list
                            targetValue:
                              minItems: 1
                              type: array
                              items:
                                type: string
                                minLength: 1
                          required:
                            - attributeType
                            - operator
                            - targetValue
              description: Request body for creating a library requirement. Define the
                module/subject by label (to create new ones) or by code/label
                (to reuse an existing template), plus the attribute definition;
                invalid `(attributeType, operator, targetValue)` combinations
                are rejected.
        description: Request body for creating a library requirement. Define the
          module/subject by label (to create new ones) or by code/label (to
          reuse an existing template), plus the attribute definition; invalid
          `(attributeType, operator, targetValue)` combinations are rejected.
      responses:
        "201":
          description: A single library requirement.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the library requirement.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  moduleCode:
                    type: string
                    minLength: 1
                    description: Identifier of the module this requirement belongs to.
                  moduleLabel:
                    type: string
                    minLength: 1
                    description: Display label of the module.
                  subjectCode:
                    type: string
                    minLength: 1
                    description: Identifier of the subject this requirement belongs to.
                  subjectLabel:
                    type: string
                    minLength: 1
                    description: Display label of the subject.
                  attributeCode:
                    type: string
                    minLength: 1
                    description: Attribute code (maps to attributeId in storage).
                  attributeType:
                    type: string
                    enum:
                      - boolean
                      - number
                      - fillableForm
                      - amBestRating
                      - amBestFinancialStrength
                      - connected
                      - list
                      - ai
                    description: Data type of the attribute (e.g. boolean, number).
                  attributeLabel:
                    type: string
                    minLength: 1
                    description: Display label of the attribute.
                  attributeDescription:
                    description: Optional description of the attribute.
                    type: string
                    minLength: 1
                  matchingCriteria:
                    type: string
                    enum:
                      - matchOnce
                      - matchRepeatedly
                    description: Matching criteria for the requirement.
                  operator:
                    description: Comparison operator for the requirement.
                    type: string
                    enum:
                      - must be present
                      - must be greater or equal to
                      - must be less than or equal to
                      - must be completed by
                      - at least one must be present from list
                  targetValue:
                    description: Target value for the requirement comparison.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: array
                        items:
                          type: string
                          minLength: 1
                required:
                  - _id
                  - moduleCode
                  - moduleLabel
                  - subjectCode
                  - subjectLabel
                  - attributeCode
                  - attributeType
                  - attributeLabel
                  - matchingCriteria
                additionalProperties: false
                description: A single library requirement.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Requirements
      description: >-
        List library requirements in the caller's organization. Results are
        always scoped to the caller's organization and never include system
        requirement templates. Supports pagination via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `attributeCode` (string), `attributeLabel` (string). See "List
        Endpoints" in the API overview for operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: moduleCode, moduleLabel, subjectCode,
            subjectLabel, attributeCode, attributeType, attributeLabel,
            attributeDescription, matchingCriteria, operator"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            moduleCode, moduleLabel, subjectCode, subjectLabel, attributeCode,
            attributeType, attributeLabel, attributeDescription,
            matchingCriteria, operator, targetValue"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: attributeCode
          required: false
          description: Filter by attributeCode. String value (e.g.
            `attributeCode=example`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: attributeLabel
          required: false
          description: Filter by attributeLabel. String value (e.g.
            `attributeLabel=example`). See "List Endpoints" in the API overview
            for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the library requirement.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        moduleCode:
                          type: string
                          minLength: 1
                          description: Identifier of the module this requirement belongs to.
                        moduleLabel:
                          type: string
                          minLength: 1
                          description: Display label of the module.
                        subjectCode:
                          type: string
                          minLength: 1
                          description: Identifier of the subject this requirement belongs to.
                        subjectLabel:
                          type: string
                          minLength: 1
                          description: Display label of the subject.
                        attributeCode:
                          type: string
                          minLength: 1
                          description: Attribute code (maps to attributeId in storage).
                        attributeType:
                          type: string
                          enum:
                            - boolean
                            - number
                            - fillableForm
                            - amBestRating
                            - amBestFinancialStrength
                            - connected
                            - list
                            - ai
                          description: Data type of the attribute (e.g. boolean, number).
                        attributeLabel:
                          type: string
                          minLength: 1
                          description: Display label of the attribute.
                        attributeDescription:
                          description: Optional description of the attribute.
                          type: string
                          minLength: 1
                        matchingCriteria:
                          type: string
                          enum:
                            - matchOnce
                            - matchRepeatedly
                          description: Matching criteria for the requirement.
                        operator:
                          description: Comparison operator for the requirement.
                          type: string
                          enum:
                            - must be present
                            - must be greater or equal to
                            - must be less than or equal to
                            - must be completed by
                            - at least one must be present from list
                        targetValue:
                          description: Target value for the requirement comparison.
                          anyOf:
                            - type: string
                              minLength: 1
                            - type: array
                              items:
                                type: string
                                minLength: 1
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /requirements/{id}:
    patch:
      tags:
        - Requirements
      description: Partially update a library requirement in the caller's
        organization. Supply any subset of the editable fields; omitted fields
        keep their stored values, and the merged (attributeType, operator,
        targetValue) triple must be internally consistent. The immutable
        attributeCode cannot be changed. Triggers the requirement-sync cascade.
        Returns the updated requirement.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                attributeType:
                  description: Data type of the attribute (e.g. boolean, number).
                  type: string
                  enum:
                    - boolean
                    - number
                    - fillableForm
                    - amBestRating
                    - amBestFinancialStrength
                    - connected
                    - list
                    - ai
                attributeLabel:
                  description: Display label of the attribute.
                  type: string
                  minLength: 1
                attributeDescription:
                  description: Optional description of the attribute.
                  type: string
                  minLength: 1
                matchingCriteria:
                  description: Matching criteria for the requirement.
                  type: string
                  enum:
                    - matchOnce
                    - matchRepeatedly
                operator:
                  description: Comparison operator for the requirement.
                  type: string
                  enum:
                    - must be present
                    - must be greater or equal to
                    - must be less than or equal to
                    - must be completed by
                    - at least one must be present from list
                targetValue:
                  description: Target value for the requirement comparison.
                  anyOf:
                    - type: string
                      minLength: 1
                    - type: array
                      items:
                        type: string
                        minLength: 1
              description: Request body for partially updating a library requirement. Supply
                any subset of the editable fields; omitted fields keep their
                stored values, and the merged (attributeType, operator,
                targetValue) triple must be internally consistent. The immutable
                identity keys (attributeCode/moduleCode/subjectCode) are ignored
                if supplied.
        description: Request body for partially updating a library requirement. Supply
          any subset of the editable fields; omitted fields keep their stored
          values, and the merged (attributeType, operator, targetValue) triple
          must be internally consistent. The immutable identity keys
          (attributeCode/moduleCode/subjectCode) are ignored if supplied.
      parameters:
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the library requirement to update.
      responses:
        "200":
          description: A single library requirement.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the library requirement.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  moduleCode:
                    type: string
                    minLength: 1
                    description: Identifier of the module this requirement belongs to.
                  moduleLabel:
                    type: string
                    minLength: 1
                    description: Display label of the module.
                  subjectCode:
                    type: string
                    minLength: 1
                    description: Identifier of the subject this requirement belongs to.
                  subjectLabel:
                    type: string
                    minLength: 1
                    description: Display label of the subject.
                  attributeCode:
                    type: string
                    minLength: 1
                    description: Attribute code (maps to attributeId in storage).
                  attributeType:
                    type: string
                    enum:
                      - boolean
                      - number
                      - fillableForm
                      - amBestRating
                      - amBestFinancialStrength
                      - connected
                      - list
                      - ai
                    description: Data type of the attribute (e.g. boolean, number).
                  attributeLabel:
                    type: string
                    minLength: 1
                    description: Display label of the attribute.
                  attributeDescription:
                    description: Optional description of the attribute.
                    type: string
                    minLength: 1
                  matchingCriteria:
                    type: string
                    enum:
                      - matchOnce
                      - matchRepeatedly
                    description: Matching criteria for the requirement.
                  operator:
                    description: Comparison operator for the requirement.
                    type: string
                    enum:
                      - must be present
                      - must be greater or equal to
                      - must be less than or equal to
                      - must be completed by
                      - at least one must be present from list
                  targetValue:
                    description: Target value for the requirement comparison.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: array
                        items:
                          type: string
                          minLength: 1
                required:
                  - _id
                  - moduleCode
                  - moduleLabel
                  - subjectCode
                  - subjectLabel
                  - attributeCode
                  - attributeType
                  - attributeLabel
                  - matchingCriteria
                additionalProperties: false
                description: A single library requirement.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
    get:
      tags:
        - Requirements
      description: Read a single library requirement by id, scoped to the caller's
        organization.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            moduleCode, moduleLabel, subjectCode, subjectLabel, attributeCode,
            attributeType, attributeLabel, attributeDescription,
            matchingCriteria, operator, targetValue"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the library requirement to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the library requirement.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  moduleCode:
                    type: string
                    minLength: 1
                    description: Identifier of the module this requirement belongs to.
                  moduleLabel:
                    type: string
                    minLength: 1
                    description: Display label of the module.
                  subjectCode:
                    type: string
                    minLength: 1
                    description: Identifier of the subject this requirement belongs to.
                  subjectLabel:
                    type: string
                    minLength: 1
                    description: Display label of the subject.
                  attributeCode:
                    type: string
                    minLength: 1
                    description: Attribute code (maps to attributeId in storage).
                  attributeType:
                    type: string
                    enum:
                      - boolean
                      - number
                      - fillableForm
                      - amBestRating
                      - amBestFinancialStrength
                      - connected
                      - list
                      - ai
                    description: Data type of the attribute (e.g. boolean, number).
                  attributeLabel:
                    type: string
                    minLength: 1
                    description: Display label of the attribute.
                  attributeDescription:
                    description: Optional description of the attribute.
                    type: string
                    minLength: 1
                  matchingCriteria:
                    type: string
                    enum:
                      - matchOnce
                      - matchRepeatedly
                    description: Matching criteria for the requirement.
                  operator:
                    description: Comparison operator for the requirement.
                    type: string
                    enum:
                      - must be present
                      - must be greater or equal to
                      - must be less than or equal to
                      - must be completed by
                      - at least one must be present from list
                  targetValue:
                    description: Target value for the requirement comparison.
                    anyOf:
                      - type: string
                        minLength: 1
                      - type: array
                        items:
                          type: string
                          minLength: 1
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /documents/{id}/amendments:
    get:
      tags:
        - Documents
      description: List amendments (cancellations and reinstatements) attached to a
        document. Each amendment is either a cancellation that reduces or ends
        the policy coverage referenced by the document, or a reinstatement that
        restores it. Supports pagination (`limit`, `skip`). Results are scoped
        to the caller's organization.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the document whose amendments to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        primaryRecordId:
                          $ref: "#/components/schemas/objectId"
                        policyNumber:
                          type: string
                          minLength: 1
                        status:
                          type: string
                          enum:
                            - pending
                            - active
                          description: "Lifecycle status of the amendment: Active means it currently
                            affects policy validity; Cancelled means it was
                            revoked."
                        type:
                          type: string
                          enum:
                            - cancellation
                            - reinstatement
                          description: Whether this amendment is a cancellation (ends or reduces policy
                            coverage) or a reinstatement (restores coverage
                            after a cancellation).
                        subjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                            required:
                              - code
                            additionalProperties: false
                        matchingSubjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                              label:
                                type: string
                                minLength: 1
                            required:
                              - code
                              - label
                            additionalProperties: false
                        effectiveDate:
                          type: string
                          format: date-time
                        issueDate:
                          type: string
                          format: date-time
                        notes:
                          type: string
                          minLength: 1
                        document:
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                          required:
                            - id
                          additionalProperties: false
                      required:
                        - _id
                        - primaryRecordId
                        - policyNumber
                        - status
                        - type
                        - subjects
                        - matchingSubjects
                        - effectiveDate
                        - issueDate
                        - document
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /document-types:
    get:
      tags:
        - Document Types
      description: >-
        List document types in the caller's workspace. Results are always scoped
        to the caller's workspace and exclude deleted document types. Supports
        sorting by name and pagination via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `name` (string), `pluralName` (string), `description` (string). See
        "List Endpoints" in the API overview for operator syntax and combining
        rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: name"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id, name,
            pluralName, description, extractionFields, isSystemType"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: pluralName
          required: false
          description: Filter by pluralName. String value (e.g. `pluralName=example`). See
            "List Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: description
          required: false
          description: Filter by description. String value (e.g. `description=example`).
            See "List Endpoints" in the API overview for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier document type.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          nullable: true
                          description: Human-readable singular document type name.
                          type: string
                        pluralName:
                          nullable: true
                          description: Human-readable plural document type name.
                          type: string
                        description:
                          type: string
                          description: Document type description.
                        extractionFields:
                          type: array
                          items:
                            type: object
                            properties:
                              fieldKey:
                                type: string
                                minLength: 1
                              name:
                                type: string
                                minLength: 1
                              valueSchema: {}
                              instructions:
                                type: string
                                maxLength: 2000
                              enabled:
                                default: true
                                type: boolean
                            additionalProperties: false
                        isSystemType:
                          nullable: true
                          description: Whether document type is system-managed.
                          type: boolean
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}:
    get:
      tags:
        - Policies
      description: Read a policy by its policy number. A policy aggregates one or more
        coverage lines (subjects) under a shared number; each subject carries
        its own effective/expiration dates and computed validity status. Scoped
        to the caller's organization. Use the policy sub-routes (`/amendments`,
        `/documents`, `/cancellations`, `/reinstatements`) for related entities.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number that uniquely identifies the policy to read.
      responses:
        "200":
          description: A policy aggregates one or more coverage lines (subjects) under a
            shared policy number, with computed validity per subject.
          content:
            application/json:
              schema:
                type: object
                properties:
                  number:
                    type: string
                    minLength: 1
                    description: Policy number that uniquely identifies the policy within the
                      caller's organization.
                  subjects:
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable code identifying the policy subject (coverage line) within
                            the policy.
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label for the policy subject.
                        validityStatus:
                          type: string
                          enum:
                            - active
                            - expiring
                            - cancelling
                            - expired
                            - cancelled
                            - inactive
                          description: Computed validity status of this subject based on its
                            effective/expiration dates and any active
                            cancellations.
                        effectiveDate:
                          description: Date on which the subject becomes effective.
                          type: string
                          format: date-time
                        expirationDate:
                          description: Date on which the subject expires.
                          type: string
                          format: date-time
                        cancellation:
                          description: If the subject is currently cancelled, the active cancellation
                            record. Absent for active subjects.
                          type: object
                          properties:
                            id:
                              description: Optional identifier of the cancellation record. Absent on policies
                                that pre-date the modern Cancellations &
                                Reinstatements implementation.
                              allOf:
                                - $ref: "#/components/schemas/objectId"
                            originalValidity:
                              type: object
                              properties:
                                effectiveDate:
                                  description: Effective date the subject had before the cancellation was applied.
                                  type: string
                                  format: date-time
                                expirationDate:
                                  description: Expiration date the subject had before the cancellation was
                                    applied.
                                  type: string
                                  format: date-time
                              required:
                                - effectiveDate
                                - expirationDate
                              additionalProperties: false
                              description: Validity window of the subject before the cancellation took effect
                                — useful for showing the original coverage span.
                            effectiveDate:
                              description: Date on which the cancellation takes effect.
                              type: string
                              format: date-time
                            issueDate:
                              description: Date on which the cancellation was issued. Absent on policies that
                                pre-date the modern Cancellations &
                                Reinstatements implementation.
                              type: string
                              format: date-time
                            notes:
                              description: Free-form notes attached to the cancellation.
                              type: string
                              minLength: 1
                          required:
                            - originalValidity
                            - effectiveDate
                          additionalProperties: false
                        reinstatement:
                          description: "If the subject was previously cancelled and later reinstated, the
                            reinstatement that restored its original validity
                            window. A historical marker: the subject is active
                            again with its dates restored. Cleared when a new
                            cancellation is logged for the subject."
                          type: object
                          properties:
                            id:
                              description: Identifier of the reinstatement record that restored the subject.
                              allOf:
                                - $ref: "#/components/schemas/objectId"
                            effectiveDate:
                              description: Date on which the reinstatement takes effect, as reported on the
                                reinstatement notice. The subject's validity
                                dates are restored to their original window, not
                                from this date forward.
                              type: string
                              format: date-time
                            issueDate:
                              description: Date on which the reinstatement was issued.
                              type: string
                              format: date-time
                            notes:
                              description: Free-form notes attached to the reinstatement.
                              type: string
                              minLength: 1
                          required:
                            - id
                            - effectiveDate
                            - issueDate
                          additionalProperties: false
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              _id:
                                description: Unique identifier of the document.
                                allOf:
                                  - $ref: "#/components/schemas/objectId"
                              name:
                                description: Display name of the document, if set.
                                type: string
                              issueDate:
                                description: Date the document was issued, if known.
                                nullable: true
                                type: string
                                format: date-time
                              primaryRecordId:
                                description: Identifier of the primary record (e.g. vendor or contractor)
                                  associated with this document.
                                allOf:
                                  - $ref: "#/components/schemas/objectId"
                            required:
                              - _id
                              - primaryRecordId
                            additionalProperties: false
                          description: Documents that report this subject, each with its associated
                            primary record. More than one entry means the same
                            coverage line appears on multiple documents.
                      required:
                        - code
                        - label
                        - validityStatus
                        - effectiveDate
                        - expirationDate
                        - documents
                      additionalProperties: false
                    description: Coverage lines (subjects) attached to this policy.
                required:
                  - number
                  - subjects
                additionalProperties: false
                description: A policy aggregates one or more coverage lines (subjects) under a
                  shared policy number, with computed validity per subject.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/documents:
    get:
      tags:
        - Policies
      description: >-
        List documents that support a policy's coverage. Each document may carry
        one or more coverage subjects whose validity is evaluated against the
        policy. Supports filtering by primary record to retrieve only documents
        associated with a specific vendor or contractor.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `primaryRecordId` (objectId), `name` (string), `issueDate` (date). See
        "List Endpoints" in the API overview for operator syntax and combining
        rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: not supported on this endpoint — full record is
        returned.
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: name, issueDate"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: primaryRecordId
          required: false
          description: Filter by primaryRecordId. 24-character hex ObjectId string (e.g.
            `primaryRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: issueDate
          required: false
          description: Filter by issueDate. ISO-8601 date or datetime string (e.g.
            `issueDate=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: The policy number identifying the insurance policy whose documents
            should be listed.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the document.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          description: Display name of the document, if set.
                          type: string
                        issueDate:
                          description: Date the document was issued, if known.
                          nullable: true
                          type: string
                          format: date-time
                        primaryRecordId:
                          description: Identifier of the primary record (e.g. vendor or contractor)
                            associated with this document.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable code identifying the policy subject (coverage line) within
                                  the policy.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label for the policy subject.
                              validityStatus:
                                type: string
                                enum:
                                  - active
                                  - expiring
                                  - cancelling
                                  - expired
                                  - cancelled
                                  - inactive
                                description: Computed validity status of this subject based on its
                                  effective/expiration dates and any active
                                  cancellations.
                              effectiveDate:
                                description: Date on which the subject becomes effective.
                                type: string
                                format: date-time
                              expirationDate:
                                description: Date on which the subject expires.
                                type: string
                                format: date-time
                              cancellation:
                                description: If the subject is currently cancelled, the active cancellation
                                  record. Absent for active subjects.
                                type: object
                                properties:
                                  id:
                                    description: Optional identifier of the cancellation record. Absent on policies
                                      that pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  originalValidity:
                                    type: object
                                    properties:
                                      effectiveDate:
                                        description: Effective date the subject had before the cancellation was applied.
                                        type: string
                                        format: date-time
                                      expirationDate:
                                        description: Expiration date the subject had before the cancellation was
                                          applied.
                                        type: string
                                        format: date-time
                                    required:
                                      - effectiveDate
                                      - expirationDate
                                    additionalProperties: false
                                    description: Validity window of the subject before the cancellation took effect
                                      — useful for showing the original coverage
                                      span.
                                  effectiveDate:
                                    description: Date on which the cancellation takes effect.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the cancellation was issued. Absent on policies that
                                      pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the cancellation.
                                    type: string
                                    minLength: 1
                                required:
                                  - originalValidity
                                  - effectiveDate
                                additionalProperties: false
                              reinstatement:
                                description: "If the subject was previously cancelled and later reinstated, the
                                  reinstatement that restored its original
                                  validity window. A historical marker: the
                                  subject is active again with its dates
                                  restored. Cleared when a new cancellation is
                                  logged for the subject."
                                type: object
                                properties:
                                  id:
                                    description: Identifier of the reinstatement record that restored the subject.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  effectiveDate:
                                    description: Date on which the reinstatement takes effect, as reported on the
                                      reinstatement notice. The subject's
                                      validity dates are restored to their
                                      original window, not from this date
                                      forward.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the reinstatement was issued.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the reinstatement.
                                    type: string
                                    minLength: 1
                                required:
                                  - id
                                  - effectiveDate
                                  - issueDate
                                additionalProperties: false
                            required:
                              - code
                              - label
                              - validityStatus
                              - effectiveDate
                              - expirationDate
                            additionalProperties: false
                            description: A coverage line (subject) on a policy, including its validity
                              window, any active cancellation, and any
                              reinstatement that restored it.
                          description: Coverage subjects carried by this document for the policy,
                            including their current validity status.
                      required:
                        - _id
                        - primaryRecordId
                        - subjects
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/amendments:
    get:
      tags:
        - Policies
      description: >-
        List amendments (cancellations and reinstatements) for a policy
        identified by its number. Each amendment is either a cancellation that
        reduces or ends coverage, or a reinstatement that restores coverage
        after a cancellation. Supports pagination via `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `primaryRecordId` (objectId), `type` (string), `effectiveDate` (date),
        `issueDate` (date), `notes` (string). See "List Endpoints" in the API
        overview for operator syntax and combining rules.


        **Sorting**: not supported on this endpoint.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            primaryRecordId, policyNumber, status, type, subjects,
            matchingSubjects, effectiveDate, issueDate, notes, document"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: primaryRecordId
          required: false
          description: Filter by primaryRecordId. 24-character hex ObjectId string (e.g.
            `primaryRecordId=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: type
          required: false
          description: Filter by type. String value (e.g. `type=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: effectiveDate
          required: false
          description: Filter by effectiveDate. ISO-8601 date or datetime string (e.g.
            `effectiveDate=2024-01-15`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: issueDate
          required: false
          description: Filter by issueDate. ISO-8601 date or datetime string (e.g.
            `issueDate=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: notes
          required: false
          description: Filter by notes. String value (e.g. `notes=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: The policy number identifying the policy whose amendments to list.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        primaryRecordId:
                          $ref: "#/components/schemas/objectId"
                        policyNumber:
                          type: string
                          minLength: 1
                        status:
                          type: string
                          enum:
                            - pending
                            - active
                          description: "Lifecycle status of the amendment: Active means it currently
                            affects policy validity; Cancelled means it was
                            revoked."
                        type:
                          type: string
                          enum:
                            - cancellation
                            - reinstatement
                          description: Whether this amendment is a cancellation (ends or reduces policy
                            coverage) or a reinstatement (restores coverage
                            after a cancellation).
                        subjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                            required:
                              - code
                            additionalProperties: false
                        matchingSubjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                              label:
                                type: string
                                minLength: 1
                            required:
                              - code
                              - label
                            additionalProperties: false
                        effectiveDate:
                          type: string
                          format: date-time
                        issueDate:
                          type: string
                          format: date-time
                        notes:
                          type: string
                          minLength: 1
                        document:
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                          required:
                            - id
                          additionalProperties: false
                      required:
                        - _id
                        - primaryRecordId
                        - policyNumber
                        - status
                        - type
                        - subjects
                        - matchingSubjects
                        - effectiveDate
                        - issueDate
                        - document
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/cancellations:
    post:
      tags:
        - Policies
      description: "Apply a cancellation to a policy, suppressing the validity of the
        targeted subjects from the effective date forward. This is a
        state-changing operation: it creates a cancellation modifier and updates
        subject validity accordingly. To record an externally-issued
        cancellation without altering platform state, use the `/log` variant
        instead."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                documentId:
                  description: Identifier of the cancellation notice document attached to this
                    cancellation.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                subjectCodes:
                  description: Subject codes from the cancellation notice. When omitted the
                    cancellation is treated as blanket and applies to all
                    subjects on the policy.
                  type: array
                  items:
                    type: string
                    minLength: 1
                effectiveDate:
                  description: Date on which the cancellation takes effect, as reported on the
                    notice.
                issueDate:
                  description: Date the cancellation notice was issued. Optional; include when
                    available from the notice.
                notes:
                  description: Free-form notes about the cancellation.
                  type: string
                  minLength: 1
              required:
                - documentId
                - effectiveDate
              description: Request body for creating a policy cancellation. Applying a
                cancellation updates subject validity statuses immediately.
        description: Request body for creating a policy cancellation. Applying a
          cancellation updates subject validity statuses immediately.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy to cancel.
      responses:
        "200":
          description: The newly created cancellation record, including any pre-existing
            cancellations that cover overlapping subjects.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the policy cancellation.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record (insured entity) whose policy this
                      cancellation affects.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  policyNumber:
                    type: string
                    minLength: 1
                    description: Policy number of the policy being cancelled.
                  status:
                    type: string
                    enum:
                      - pending
                      - active
                    description: Lifecycle status of the cancellation. Active cancellations suppress
                      subject validity.
                  type:
                    type: string
                    enum:
                      - cancellation
                  subjects:
                    default: []
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                      required:
                        - code
                      additionalProperties: false
                  matchingSubjects:
                    default: []
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                        label:
                          type: string
                          minLength: 1
                      required:
                        - code
                        - label
                      additionalProperties: false
                  effectiveDate:
                    type: string
                    format: date-time
                  issueDate:
                    type: string
                    format: date-time
                  notes:
                    type: string
                    minLength: 1
                  document:
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                    required:
                      - id
                    additionalProperties: false
                  conflicts:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the policy cancellation.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        status:
                          type: string
                          enum:
                            - pending
                            - active
                          description: Lifecycle status of the cancellation. Active cancellations suppress
                            subject validity.
                        type:
                          type: string
                          enum:
                            - cancellation
                        subjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                            required:
                              - code
                            additionalProperties: false
                        matchingSubjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                              label:
                                type: string
                                minLength: 1
                            required:
                              - code
                              - label
                            additionalProperties: false
                        effectiveDate:
                          type: string
                          format: date-time
                        issueDate:
                          type: string
                          format: date-time
                        notes:
                          type: string
                          minLength: 1
                        document:
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                          required:
                            - id
                          additionalProperties: false
                        conflictingSubjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable code identifying the policy subject (coverage line) within
                                  the policy.
                            required:
                              - code
                            additionalProperties: false
                          description: Subject codes from the existing cancellation that overlap with the
                            newly created one.
                      required:
                        - _id
                        - status
                        - type
                        - subjects
                        - matchingSubjects
                        - effectiveDate
                        - issueDate
                        - document
                        - conflictingSubjects
                      additionalProperties: false
                    description: Other cancellations that cover overlapping subjects. Present when a
                      blanket or overlapping cancellation already exists; the
                      new cancellation is still created.
                required:
                  - _id
                  - primaryRecordId
                  - policyNumber
                  - status
                  - type
                  - subjects
                  - matchingSubjects
                  - effectiveDate
                  - issueDate
                  - document
                  - conflicts
                additionalProperties: false
                description: The newly created cancellation record, including any pre-existing
                  cancellations that cover overlapping subjects.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/cancellations/{id}/log:
    post:
      tags:
        - Policies
      description: "Record an externally-issued cancellation against an existing
        cancellation request. This is an audit-only operation: no policy state
        is changed. Use the non-`/log` POST variant if you need to apply a new
        cancellation that updates subject validity."
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy whose cancellation is being
            logged.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the cancellation request being recorded as logged.
      responses:
        "200":
          description: The audit log entry created by recording an externally-issued
            cancellation. No policy state is changed; this call only records
            that a cancellation occurred outside the platform.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the logged cancellation.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record (insured party) this cancellation
                      is associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  policyNumber:
                    type: string
                    minLength: 1
                    description: Policy number this cancellation was logged against.
                  subjects:
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable code identifying the policy subject (coverage line) within
                            the policy.
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label for the policy subject.
                      required:
                        - code
                        - label
                      additionalProperties: false
                    description: Subjects affected by the cancellation at log time, identified by
                      code and display label.
                  documents:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        name:
                          type: string
                        issueDate:
                          nullable: true
                          type: string
                          format: date-time
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable code identifying the policy subject (coverage line) within
                                  the policy.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label for the policy subject.
                              effectiveDate:
                                description: Date on which the subject becomes effective.
                                type: string
                                format: date-time
                              expirationDate:
                                description: Date on which the subject expires.
                                type: string
                                format: date-time
                              cancellation:
                                description: If the subject is currently cancelled, the active cancellation
                                  record. Absent for active subjects.
                                type: object
                                properties:
                                  id:
                                    description: Optional identifier of the cancellation record. Absent on policies
                                      that pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  originalValidity:
                                    type: object
                                    properties:
                                      effectiveDate:
                                        description: Effective date the subject had before the cancellation was applied.
                                        type: string
                                        format: date-time
                                      expirationDate:
                                        description: Expiration date the subject had before the cancellation was
                                          applied.
                                        type: string
                                        format: date-time
                                    required:
                                      - effectiveDate
                                      - expirationDate
                                    additionalProperties: false
                                    description: Validity window of the subject before the cancellation took effect
                                      — useful for showing the original coverage
                                      span.
                                  effectiveDate:
                                    description: Date on which the cancellation takes effect.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the cancellation was issued. Absent on policies that
                                      pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the cancellation.
                                    type: string
                                    minLength: 1
                                required:
                                  - originalValidity
                                  - effectiveDate
                                additionalProperties: false
                              reinstatement:
                                description: "If the subject was previously cancelled and later reinstated, the
                                  reinstatement that restored its original
                                  validity window. A historical marker: the
                                  subject is active again with its dates
                                  restored. Cleared when a new cancellation is
                                  logged for the subject."
                                type: object
                                properties:
                                  id:
                                    description: Identifier of the reinstatement record that restored the subject.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  effectiveDate:
                                    description: Date on which the reinstatement takes effect, as reported on the
                                      reinstatement notice. The subject's
                                      validity dates are restored to their
                                      original window, not from this date
                                      forward.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the reinstatement was issued.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the reinstatement.
                                    type: string
                                    minLength: 1
                                required:
                                  - id
                                  - effectiveDate
                                  - issueDate
                                additionalProperties: false
                            required:
                              - code
                              - label
                              - effectiveDate
                              - expirationDate
                            additionalProperties: false
                      required:
                        - _id
                        - subjects
                      additionalProperties: false
                    description: Documents associated with the cancellation, including the
                      cancellation notice.
                  effectiveDate:
                    description: Date the cancellation takes effect, as recorded on the external
                      document.
                    type: string
                    format: date-time
                  issueDate:
                    description: Date the cancellation document was issued.
                    type: string
                    format: date-time
                  notes:
                    description: Optional free-form notes about this cancellation.
                    type: string
                    minLength: 1
                required:
                  - _id
                  - primaryRecordId
                  - policyNumber
                  - subjects
                  - documents
                  - effectiveDate
                  - issueDate
                additionalProperties: false
                description: The audit log entry created by recording an externally-issued
                  cancellation. No policy state is changed; this call only
                  records that a cancellation occurred outside the platform.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/cancellations/{id}:
    delete:
      tags:
        - Policies
      description: Delete a previously-applied policy cancellation. Removing the
        cancellation reverses its effect on subject validity, so any subjects it
        was suppressing become valid again subject to the policy's other
        modifiers.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy from which the cancellation
            should be removed.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the cancellation to delete.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Identifier of the deleted cancellation.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/reinstatements:
    post:
      tags:
        - Policies
      description: "Apply a reinstatement to a policy, reversing an active
        cancellation and restoring the covered subjects to compliant status.
        This is a state-changing operation: it creates a reinstatement modifier
        and updates subject validity accordingly. To record an externally-issued
        reinstatement without altering platform state, use the `/log` variant
        instead."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                documentId:
                  description: Identifier of the insurance document (certificate or policy) that
                    evidences the reinstatement.
                  allOf:
                    - $ref: "#/components/schemas/objectIdInput"
                subjectCodes:
                  description: Subset of subject codes to reinstate. Omit to reinstate all
                    subjects covered by matching active cancellations (blanket
                    reinstatement).
                  type: array
                  items:
                    type: string
                    minLength: 1
                effectiveDate:
                  description: Date the reinstatement takes effect, as reported on the
                    reinstatement notice. The affected subjects' validity dates
                    are restored to their original window, not from this date
                    forward.
                issueDate:
                  description: Date the reinstatement document was issued. Defaults to
                    effectiveDate when omitted.
                notes:
                  description: Optional free-form notes about this reinstatement.
                  type: string
                  minLength: 1
              required:
                - documentId
                - effectiveDate
              description: Request body for applying a reinstatement to a policy. A
                reinstatement reverses an active cancellation, restoring the
                policy subjects to compliant status.
        description: Request body for applying a reinstatement to a policy. A
          reinstatement reverses an active cancellation, restoring the policy
          subjects to compliant status.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy to reinstate.
      responses:
        "200":
          description: The newly created reinstatement record, including any pre-existing
            reinstatements that cover overlapping subjects.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the reinstatement record.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record (insured party) this reinstatement
                      is associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  policyNumber:
                    type: string
                    minLength: 1
                    description: Policy number this reinstatement applies to.
                  status:
                    type: string
                    enum:
                      - pending
                      - active
                    description: Lifecycle status of the reinstatement. Active reinstatements are in
                      effect.
                  type:
                    type: string
                    enum:
                      - reinstatement
                  subjects:
                    default: []
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                      required:
                        - code
                      additionalProperties: false
                  matchingSubjects:
                    default: []
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                        label:
                          type: string
                          minLength: 1
                      required:
                        - code
                        - label
                      additionalProperties: false
                  effectiveDate:
                    type: string
                    format: date-time
                  issueDate:
                    type: string
                    format: date-time
                  notes:
                    type: string
                    minLength: 1
                  document:
                    type: object
                    properties:
                      id:
                        $ref: "#/components/schemas/objectId"
                    required:
                      - id
                    additionalProperties: false
                  conflicts:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the reinstatement record.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        status:
                          type: string
                          enum:
                            - pending
                            - active
                          description: Lifecycle status of the reinstatement. Active reinstatements are in
                            effect.
                        type:
                          type: string
                          enum:
                            - reinstatement
                        subjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                            required:
                              - code
                            additionalProperties: false
                        matchingSubjects:
                          default: []
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                              label:
                                type: string
                                minLength: 1
                            required:
                              - code
                              - label
                            additionalProperties: false
                        effectiveDate:
                          type: string
                          format: date-time
                        issueDate:
                          type: string
                          format: date-time
                        notes:
                          type: string
                          minLength: 1
                        document:
                          type: object
                          properties:
                            id:
                              $ref: "#/components/schemas/objectId"
                          required:
                            - id
                          additionalProperties: false
                        conflictingSubjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable code identifying the policy subject (coverage line) within
                                  the policy.
                            required:
                              - code
                            additionalProperties: false
                          description: Subject codes from the existing reinstatement that overlap with the
                            newly created one.
                      required:
                        - _id
                        - status
                        - type
                        - subjects
                        - matchingSubjects
                        - effectiveDate
                        - issueDate
                        - document
                        - conflictingSubjects
                      additionalProperties: false
                    description: Other reinstatements that cover overlapping subjects. Present when
                      a blanket or overlapping reinstatement already exists; the
                      new reinstatement is still created.
                required:
                  - _id
                  - primaryRecordId
                  - policyNumber
                  - status
                  - type
                  - subjects
                  - matchingSubjects
                  - effectiveDate
                  - issueDate
                  - document
                  - conflicts
                additionalProperties: false
                description: The newly created reinstatement record, including any pre-existing
                  reinstatements that cover overlapping subjects.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/reinstatements/{id}/log:
    post:
      tags:
        - Policies
      description: "Record an externally-issued reinstatement against an existing
        reinstatement request. This is an audit-only operation: no policy state
        is changed. Use the non-`/log` POST variant if you need to apply a new
        reinstatement that updates subject validity."
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy whose reinstatement is being
            logged.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the reinstatement request being recorded as logged.
      responses:
        "200":
          description: The audit log entry created by recording an externally-issued
            reinstatement. No policy state is changed; this call only records
            that a reinstatement occurred outside the platform.
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the logged reinstatement.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  primaryRecordId:
                    description: Identifier of the primary record (insured party) this reinstatement
                      is associated with.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  policyNumber:
                    type: string
                    minLength: 1
                    description: Policy number this reinstatement was logged against.
                  subjects:
                    type: array
                    items:
                      type: object
                      properties:
                        code:
                          type: string
                          minLength: 1
                          description: Stable code identifying the policy subject (coverage line) within
                            the policy.
                        label:
                          type: string
                          minLength: 1
                          description: Human-readable label for the policy subject.
                      required:
                        - code
                        - label
                      additionalProperties: false
                    description: Subjects resolved from the matched cancellation(s) at log time,
                      identified by code and display label.
                  documents:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        name:
                          type: string
                        issueDate:
                          nullable: true
                          type: string
                          format: date-time
                        subjects:
                          type: array
                          items:
                            type: object
                            properties:
                              code:
                                type: string
                                minLength: 1
                                description: Stable code identifying the policy subject (coverage line) within
                                  the policy.
                              label:
                                type: string
                                minLength: 1
                                description: Human-readable label for the policy subject.
                              effectiveDate:
                                description: Date on which the subject becomes effective.
                                type: string
                                format: date-time
                              expirationDate:
                                description: Date on which the subject expires.
                                type: string
                                format: date-time
                              cancellation:
                                description: If the subject is currently cancelled, the active cancellation
                                  record. Absent for active subjects.
                                type: object
                                properties:
                                  id:
                                    description: Optional identifier of the cancellation record. Absent on policies
                                      that pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  originalValidity:
                                    type: object
                                    properties:
                                      effectiveDate:
                                        description: Effective date the subject had before the cancellation was applied.
                                        type: string
                                        format: date-time
                                      expirationDate:
                                        description: Expiration date the subject had before the cancellation was
                                          applied.
                                        type: string
                                        format: date-time
                                    required:
                                      - effectiveDate
                                      - expirationDate
                                    additionalProperties: false
                                    description: Validity window of the subject before the cancellation took effect
                                      — useful for showing the original coverage
                                      span.
                                  effectiveDate:
                                    description: Date on which the cancellation takes effect.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the cancellation was issued. Absent on policies that
                                      pre-date the modern Cancellations &
                                      Reinstatements implementation.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the cancellation.
                                    type: string
                                    minLength: 1
                                required:
                                  - originalValidity
                                  - effectiveDate
                                additionalProperties: false
                              reinstatement:
                                description: "If the subject was previously cancelled and later reinstated, the
                                  reinstatement that restored its original
                                  validity window. A historical marker: the
                                  subject is active again with its dates
                                  restored. Cleared when a new cancellation is
                                  logged for the subject."
                                type: object
                                properties:
                                  id:
                                    description: Identifier of the reinstatement record that restored the subject.
                                    allOf:
                                      - $ref: "#/components/schemas/objectId"
                                  effectiveDate:
                                    description: Date on which the reinstatement takes effect, as reported on the
                                      reinstatement notice. The subject's
                                      validity dates are restored to their
                                      original window, not from this date
                                      forward.
                                    type: string
                                    format: date-time
                                  issueDate:
                                    description: Date on which the reinstatement was issued.
                                    type: string
                                    format: date-time
                                  notes:
                                    description: Free-form notes attached to the reinstatement.
                                    type: string
                                    minLength: 1
                                required:
                                  - id
                                  - effectiveDate
                                  - issueDate
                                additionalProperties: false
                            required:
                              - code
                              - label
                              - effectiveDate
                              - expirationDate
                            additionalProperties: false
                      required:
                        - _id
                        - subjects
                      additionalProperties: false
                    description: Documents associated with the reinstatement, including the
                      reinstating certificate.
                  effectiveDate:
                    description: Date the reinstatement takes effect, as recorded on the external
                      document.
                    type: string
                    format: date-time
                  issueDate:
                    description: Date the reinstatement document was issued.
                    type: string
                    format: date-time
                  notes:
                    description: Optional free-form notes about this reinstatement.
                    type: string
                    minLength: 1
                required:
                  - _id
                  - primaryRecordId
                  - policyNumber
                  - subjects
                  - documents
                  - effectiveDate
                  - issueDate
                additionalProperties: false
                description: The audit log entry created by recording an externally-issued
                  reinstatement. No policy state is changed; this call only
                  records that a reinstatement occurred outside the platform.
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/reinstatements/{id}:
    delete:
      tags:
        - Policies
      description: Delete a previously-applied policy reinstatement. Removing the
        reinstatement re-asserts the underlying cancellation, so subjects that
        were restored by it return to suppressed status (subject to the policy's
        other modifiers).
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
          description: Policy number identifying the policy from which the reinstatement
            should be removed.
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the reinstatement to delete.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Identifier of the deleted reinstatement.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/log-cancellation:
    post:
      tags:
        - Policies
      description: Log a policy cancellation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryRecordIds:
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                subjectCodes:
                  type: array
                  items:
                    type: string
                    minLength: 1
                effectiveDate: {}
                issueDate: {}
                notes:
                  type: string
                  minLength: 1
              required:
                - effectiveDate
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
      deprecated: true
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  policyNumber:
                    type: string
                    minLength: 1
                  primaryRecords:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              _id:
                                $ref: "#/components/schemas/objectId"
                              subjects:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    code:
                                      type: string
                                      minLength: 1
                                      description: Stable code identifying the policy subject (coverage line) within
                                        the policy.
                                    label:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label for the policy subject.
                                  required:
                                    - code
                                    - label
                                  additionalProperties: false
                            required:
                              - _id
                              - subjects
                            additionalProperties: false
                      required:
                        - _id
                        - documents
                      additionalProperties: false
                  effectiveDate:
                    type: string
                    format: date-time
                  issueDate:
                    type: string
                    format: date-time
                  notes:
                    type: string
                    minLength: 1
                required:
                  - policyNumber
                  - primaryRecords
                  - effectiveDate
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /policies/{number}/log-reinstatement:
    post:
      tags:
        - Policies
      description: Log a policy reinstatement
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                primaryRecordIds:
                  type: array
                  items:
                    $ref: "#/components/schemas/objectIdInput"
                subjectCodes:
                  type: array
                  items:
                    type: string
                    minLength: 1
                issueDate: {}
                notes:
                  type: string
                  minLength: 1
      parameters:
        - schema:
            type: string
            minLength: 1
          in: path
          name: number
          required: true
      deprecated: true
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  policyNumber:
                    type: string
                    minLength: 1
                  primaryRecords:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          $ref: "#/components/schemas/objectId"
                        documents:
                          type: array
                          items:
                            type: object
                            properties:
                              _id:
                                $ref: "#/components/schemas/objectId"
                              subjects:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    code:
                                      type: string
                                      minLength: 1
                                      description: Stable code identifying the policy subject (coverage line) within
                                        the policy.
                                    label:
                                      type: string
                                      minLength: 1
                                      description: Human-readable label for the policy subject.
                                  required:
                                    - code
                                    - label
                                  additionalProperties: false
                            required:
                              - _id
                              - subjects
                            additionalProperties: false
                      required:
                        - _id
                        - documents
                      additionalProperties: false
                  issueDate:
                    type: string
                    format: date-time
                  notes:
                    type: string
                    minLength: 1
                required:
                  - policyNumber
                  - primaryRecords
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /views:
    get:
      tags:
        - Views
      description: >-
        List views in the caller's organization. Results are scoped to the
        caller's organization and to views the caller has read access to (own
        private views plus workspace-visible views). Supports pagination via
        `limit` and `skip`.


        <!-- qs2mongo:list-summary -->


        **Filtering**: `_id` (objectId), `createdAt` (date), `updatedAt` (date),
        `name` (string), `type` (string), `visibility` (string), `readonly`
        (string), `userId` (objectId), `reference.id` (objectId),
        `reference.type` (string). See "List Endpoints" in the API overview for
        operator syntax and combining rules.


        **Sorting**: supported via `sort` (see the parameter's description for
        allowed fields). Prefix with `-` for descending.


        **Field projection**: supported via `fields` (see the parameter's
        description for allowed fields).
      parameters:
        - schema:
            default: 20
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 100
          in: query
          name: limit
          required: false
        - schema:
            default: 0
            type: integer
            minimum: 0
            maximum: 9007199254740991
          in: query
          name: skip
          required: false
        - schema:
            type: string
            minLength: 1
          in: query
          name: sort
          required: false
          description: "Comma-separated list of fields to sort by. Example:
            '_id,-createdAt'. Allowed: name, type, visibility, readonly,
            createdAt, updatedAt"
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, name, type, reference, filter, userId, visibility,
            readonly, createdAt, updatedAt"
        - schema:
            type: string
          in: query
          name: _id
          required: false
          description: Filter by _id. 24-character hex ObjectId string (e.g.
            `_id=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: createdAt
          required: false
          description: Filter by createdAt. ISO-8601 date or datetime string (e.g.
            `createdAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: updatedAt
          required: false
          description: Filter by updatedAt. ISO-8601 date or datetime string (e.g.
            `updatedAt=2024-01-15`). See "List Endpoints" in the API overview
            for operator syntax.
        - schema:
            type: string
          in: query
          name: name
          required: false
          description: Filter by name. String value (e.g. `name=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: type
          required: false
          description: Filter by type. String value (e.g. `type=example`). See "List
            Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: visibility
          required: false
          description: Filter by visibility. String value (e.g. `visibility=example`). See
            "List Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: readonly
          required: false
          description: Filter by readonly. String value (e.g. `readonly=example`). See
            "List Endpoints" in the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: userId
          required: false
          description: Filter by userId. 24-character hex ObjectId string (e.g.
            `userId=507f1f77bcf86cd799439011`). See "List Endpoints" in the API
            overview for operator syntax.
        - schema:
            type: string
          in: query
          name: reference.id
          required: false
          description: Filter by reference.id. 24-character hex ObjectId string (e.g.
            `reference.id=507f1f77bcf86cd799439011`). See "List Endpoints" in
            the API overview for operator syntax.
        - schema:
            type: string
          in: query
          name: reference.type
          required: false
          description: Filter by reference.type. String value (e.g.
            `reference.type=example`). See "List Endpoints" in the API overview
            for operator syntax.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          description: Unique identifier of the view.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        organization:
                          description: Identifier of the organization (workspace) that owns the view.
                            Always equal to the caller's organization.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        name:
                          type: string
                          minLength: 1
                          description: Human-readable name of the view.
                        type:
                          type: string
                          enum:
                            - PrimaryRecord
                            - ContextRecord
                            - Document
                          description: Domain entity this view targets (e.g. primary records, request
                            records).
                        reference:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - PrimaryObject
                                - ContextObject
                            id:
                              allOf:
                                - $ref: "#/components/schemas/objectId"
                          additionalProperties: false
                          description: Optional reference linking this view to a specific entity (e.g. a
                            primary object). Sub-fields may be absent under
                            field projection.
                        filter:
                          type: object
                          additionalProperties: {}
                          description: Filter tree applied to view results. Tree nodes use the `and`/`or`
                            operators.
                        userId:
                          description: Identifier of the user who owns this view.
                          allOf:
                            - $ref: "#/components/schemas/objectId"
                        visibility:
                          type: string
                          enum:
                            - private
                            - workspace
                          description: Visibility of the view (private to the owner or shared with the
                            workspace).
                        readonly:
                          type: boolean
                          description: Whether the view is read-only (cannot be edited or deleted).
                        createdAt:
                          description: Timestamp at which the view was created.
                          type: string
                          format: date-time
                        updatedAt:
                          description: Timestamp at which the view was last updated.
                          type: string
                          format: date-time
                      required:
                        - _id
                      additionalProperties: false
                  meta:
                    type: object
                    properties:
                      count:
                        type: number
                        description: Total number of records matching the query, across all pages.
                      next:
                        description: Relative URL for the next page of results, preserving
                          filters/sort/projection. Omitted on the last page.
                        type: string
                      prev:
                        description: Relative URL for the previous page of results, preserving
                          filters/sort/projection. Omitted on the first page.
                        type: string
                    required:
                      - count
                    additionalProperties: false
                required:
                  - data
                  - meta
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /views/{id}:
    get:
      tags:
        - Views
      description: Read a single view by id, scoped to the caller's organization and
        to views the caller has read access to. Use the `fields` query parameter
        to limit the response to a projection of the view.
      parameters:
        - schema:
            type: string
            minLength: 1
          in: query
          name: fields
          required: false
          description: "Comma-separated list of fields to project. Allowed: _id,
            organization, name, type, reference, filter, userId, visibility,
            readonly, createdAt, updatedAt"
        - schema:
            allOf:
              - $ref: "#/components/schemas/objectIdInput"
          in: path
          name: id
          required: true
          description: Identifier of the view to read.
      responses:
        "200":
          description: Default Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the view.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  organization:
                    description: Identifier of the organization (workspace) that owns the view.
                      Always equal to the caller's organization.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    minLength: 1
                    description: Human-readable name of the view.
                  type:
                    type: string
                    enum:
                      - PrimaryRecord
                      - ContextRecord
                      - Document
                    description: Domain entity this view targets (e.g. primary records, request
                      records).
                  reference:
                    type: object
                    properties:
                      type:
                        type: string
                        enum:
                          - PrimaryObject
                          - ContextObject
                      id:
                        allOf:
                          - $ref: "#/components/schemas/objectId"
                    additionalProperties: false
                    description: Optional reference linking this view to a specific entity (e.g. a
                      primary object). Sub-fields may be absent under field
                      projection.
                  filter:
                    type: object
                    additionalProperties: {}
                    description: Filter tree applied to view results. Tree nodes use the `and`/`or`
                      operators.
                  userId:
                    description: Identifier of the user who owns this view.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  visibility:
                    type: string
                    enum:
                      - private
                      - workspace
                    description: Visibility of the view (private to the owner or shared with the
                      workspace).
                  readonly:
                    type: boolean
                    description: Whether the view is read-only (cannot be edited or deleted).
                  createdAt:
                    description: Timestamp at which the view was created.
                    type: string
                    format: date-time
                  updatedAt:
                    description: Timestamp at which the view was last updated.
                    type: string
                    format: date-time
                required:
                  - _id
                additionalProperties: false
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
  /workspace:
    get:
      tags:
        - Workspaces
      description: Get the current workspace for the access token, including its API version
      responses:
        "200":
          description: Current workspace for the access token
          content:
            application/json:
              schema:
                type: object
                properties:
                  _id:
                    description: Unique identifier of the workspace.
                    allOf:
                      - $ref: "#/components/schemas/objectId"
                  name:
                    type: string
                    description: Human-readable name of the workspace.
                  slugifyName:
                    type: string
                    description: URL-safe slug derived from the workspace name.
                  description:
                    default: ""
                    description: Free-text description of the workspace, if set by the customer.
                    nullable: true
                    type: string
                  apiVersion:
                    anyOf:
                      - type: number
                        enum:
                          - 1
                      - type: number
                        enum:
                          - 2
                    description: "Public API version the workspace operates under: `1` (legacy
                      experience) or `2` (new experience). Use this to route
                      subsequent calls to the matching API surface."
                required:
                  - name
                  - slugifyName
                  - description
                  - apiVersion
                additionalProperties: false
                description: Current workspace for the access token
        "400":
          description: The request could not be understood — invalid body payload, unknown
            querystring filter, or a schema validation failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 400
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The request could not be understood — invalid body payload, unknown
                  querystring filter, or a schema validation failure.
        "401":
          description: Authentication failed — the bearer token is missing, malformed,
            expired, or does not grant access to this resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 401
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: Authentication failed — the bearer token is missing, malformed,
                  expired, or does not grant access to this resource.
        "403":
          description: The authenticated caller does not have permission to perform this
            action on the target resource.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 403
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The authenticated caller does not have permission to perform this
                  action on the target resource.
        "404":
          description: The requested resource does not exist or is not visible to the
            authenticated caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 404
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The requested resource does not exist or is not visible to the
                  authenticated caller.
        "500":
          description: The server encountered an unexpected failure while processing the
            request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                    enum:
                      - 500
                  applicationCode:
                    type: string
                    description: Machine-readable error code. For application errors this is the
                      ApplicationError.applicationCode (e.g. "FLOW.006"); for
                      schema validation failures it is "FLOW.002" (InvalidData);
                      for unexpected server failures it is "LIB.000".
                  message:
                    type: string
                    description: Human-readable error message.
                  details:
                    description: Optional additional context about the error.
                required:
                  - statusCode
                  - applicationCode
                  - message
                additionalProperties: false
                description: The server encountered an unexpected failure while processing the
                  request.
servers:
  - url: https://api.trustlayer.io/v2
    description: Production Server
  - url: https://api.integration.trustlayer.dev/v2
    description: Integration Server
  - url: https://api.demo.trustlayer.dev/v2
    description: Demo Server
security:
  - Bearer Auth: []
tags:
  - name: Auth
    description: ""
  - name: Primary Records
    description: ""
  - name: Request Records
    description: ""
  - name: Primary Objects
    description: ""
  - name: Workspaces
    description: ""
